# 改动记录（第 12 段）

> 承接 `docs/progress-log-11.md`（该文件写到约 172KB，超过 150KB 的分段阈值，按 `CLAUDE.md` 开头的规则新开一段）。

- **CORE-146 手机横屏真机复查的四项跟进（自己的座位卡 / 回合徽章 / 状态横幅 / 顶部重复行）**：用户拿 844×390 附近的真机截图复查上一轮的横屏改动，对手卡 OK，但发现四个遗漏。逐条记录根因和修法：
  - **P0-1 自己的座位卡完全没被修到（根因是 CSS 特异性，不是漏写规则）**：上一轮那批覆盖全部写成 `.seat-gen-name{...}`、`.seat-hp-col{...}` 这种 (0,1,0) 的单类选择器，而基础样式里"我"的卡早就有一整套 `.seat.me .seat-gen-name{...}`(0,2,0) 的放大版覆盖（那是为 180~260px 的大卡设计的）。**(0,1,0) 永远压不过 (0,2,0)，和源码顺序无关**——所以整张"我"的卡一直停在桌面尺寸（标题 13px、势力标 18px），在 91×121 的横屏小卡上必然溢出/重叠。修法是在手机横屏块里补一整段 ⑦ `.seat.me .X` 的等价覆盖（同为 0,2,0，且写在后面），把对手卡那批改动（≥9px 字号、加粗单字势力标、装备简称、尺寸预算）逐条按"我"的卡的实际布局重做一遍，不是简单复用对手卡的数值。**这次能漏这么久的直接原因是验证脚本从头到尾只查 `#oppRow .seat`，压根没有覆盖 `#meSeat .seat`**，见下面的验证部分。
  - **P0-2「回合」徽章遮挡武将名**：`.seat-title-tags .tag.turn` 在窄标题栏里和玩家名抢位置（实测 `seat-title-name("我自己") ↔ tag turn("回合")` 重叠 8×13px）。**没有另造一个角标**——`.seat.active{border-color:var(--jade);box-shadow:0 0 0 2px var(--jade) inset}` 这条既有规则本来就已经用绿色边框表达了"当前回合"，文字标签在小卡上是纯冗余，直接 `display:none` 即可，信息没有丢失。
  - **P1-1 状态横幅"一行内两处截断"是误判，如实记录**：实测 `.banner` 的 `whiteSpace:normal` / `overflow:visible` / `scrollWidth<=clientWidth`，横幅是满宽的、放得下，**没有任何布局截断**。用户看到的省略号是 `renderResponseCountdown` 里**写死在文案里**的（`'⏱ ' + n + 's 后自动…'`）。所以不是改 CSS，而是把文案补全成 `'s 后自动替你决定'`——原文案确实没说清会替你做什么。`run_ai_timeout_test.js` 里那条正则断言同步更新。
  - **P1-2 顶部 chrome 压掉一行**：`#myGeneral`（"你的武将：…"）和座位卡上已有的武将名完全重复，手机横屏下 `display:none`，省出的高度还给座位区。
  - **额外修掉的重叠（矩阵测出来的，不在用户清单里）**：
    - **体力心 ↔ 手牌数图标**（对手卡和"我"的卡都有）：左侧 `.seat-left` 是一条绝对定位竖列（势力标 → 竖排武将名 → 体力心），右下 `.seat-equip-row` 里有手牌数图标，两者**横向落在同一条窄带上**（都在 x≈12~28）；卡片只有 121px 高时竖列一路延伸到 y≈325、图标从 y≈314 起，必然相撞。**先试过把手牌图标 `position:absolute` 挪到右侧，那是个假修复**——`.seat-bottom` 自己就是 `position:absolute` 的定位上下文，图标不是相对 `.seat` 而是相对 `.seat-bottom` 定位，直接被推到卡片外面（实测 hi-count 落在 y=361~380、而卡底是 368），重叠"消失"只是因为它飞出了卡；CSS 没法跨过 `.seat-bottom` 这层定位上下文。压缩心的排列也算过三种都不成立：①横排——武将**最大 maxHp=6**，6 颗心约 48px 宽，会撞 x=32 起的装备槽标签；②2 列 wrap——高度只能减到 22px，而体力列起点到手牌图标之间只有约 16px 余量，仍然重叠；③只显示实心不显示空心——血满时颗数不变，没解决最坏情况。**最终做法**是这个断点下体力改成"当前/上限"数字（`render.js` 的 `heartsHtml` 额外输出一个 `data-hp="3/4"` 属性，CSS 用 `::before` 读它并隐藏下面的心 div），1 行 11px，比竖排 6 心的 66px 省 4 倍纵向空间，信息完全等价。桌面/平板仍然渲染心——那些断点上没有任何规则读 `data-hp`。
    - **身份猜测标记 🔖 ↔ 装备条**（只在最窄的 667×375 / 9人局出现，卡片仅 67×90）：基础规则是 `top:58%` + `clamp(18px,16cqw,30px)`，在这么窄的卡上 `16cqw` 才算出 10.7px、被 clamp 下限顶回 18px，58% 居中后下沿落到 y≈61，而装备条顶只在 y≈43。缩到 14px 并上移到 `top:38%`，让它在最矮的一档也留在装备条上方。**这个缺陷单点样本测不到，是矩阵化验证抓出来的。**
  - **验证（用户明确要求的三条都做了）**：① `tools/verify_mobile_landscape.js` 新增 `inspectSeat`，对 `#oppRow .seat` 和 `#meSeat .seat` **两种卡**各查四项：无元素重叠（两两求叶子节点矩形交集，容差 2px 吸收 line-box 的名义交叠）、最小字号 ≥9px、装备名无截断、卡片未被视口裁切。② **重叠断言做了鉴别力实测**：临时移除体力数字化那条修复（体力恢复竖排心），断言确实变红——`对手卡 2 处重叠(8×4px / 9×10px)` + `我的卡 1 处重叠(9×10px)`，装回后全绿，证明这条断言不是"永远绿"（CLAUDE.md 规则 20）。③ 体检从单点 844×390/8人局扩成 **5 种人数(2/4/6/8/9，覆盖 MIN_PLAYERS 下限到 SEATS 上限) × 3 种横屏视口(844×390 / 667×375 / 932×430)** 的矩阵，对手一律用**颜良文丑**（4 字是全表最长武将名，竖排最容易撞下面的体力/装备）、"我"用左慈带化身（有化身行）+ 身份局（有 🔖）+ 轮到我（有 `.active`）。矩阵共 167 条断言全绿；桌面/平板断点的既有断言同时保持不变（44px 触控目标、装备全名）。全量套件 `run_all_tests.js` 137/137。
  - **顺带修掉的一条脆弱断言**：`run_discard_reveal_test.js` 里 `assert.ok(indexSource.includes('render-discard.js?v=395'))` 把 cache-bust 版本号**写死**成 395，于是**往后任何一次 cache-bust 都会让它变红**（这次就是这么红的）。它真正想守的是"这个文件带了 `?v=` 且不低于引入弃牌展示时的那一版"，不是"恰好等于 395"，改成正则取值 + `>=395` 的阈值式断言。

- **CORE-146 合并后的三项跟进：桌面端体力表现回归确认 / 最极限一格的余量普扫 / 新建 progress-log 索引**：
  - **① 桌面端仍渲染竖排红心，已用断言钉住**。手机横屏把体力换成 `data-hp` 数字是**信息表达形式的改变**（竖排红心可余光扫读，数字要聚焦阅读），所以必须确认没有顺带波及桌面。规则上 `.seat-hp-col > div{display:none}` 和 `::before` 只写在 `@media (max-height:520px) and (orientation:landscape) and (pointer:coarse)` 块内，桌面/平板没有任何规则读 `data-hp`（属性本身是 `render.js` 无条件输出的，但没人读就是纯数据）。**实测**：桌面 1440×900 / 1280×800、平板 1024×768 / 768×1024 四档全部 `hearts=4 ("❤❤❤♡") before=""`，手机横屏四档全部 `hearts=0 before="3/4"`。**并做了鉴别力实测**：把那两条规则临时挪出 media 块（模拟"漏到全局"），桌面/平板四条断言全部变红（`体力表达被改变! hearts=0 before="3/4"`），装回后全绿——证明这条断言不是永远绿（CLAUDE.md 规则 20）。**用户保留了退回红心或做折中（如 ≤2 血时仍显示红心）的可能，真机打一局后再定，当前先不动。**
  - **② 最极限一格（667×375 / 9人局）的余量普扫**。这一格实际卡片是**对手 76×102、"我" 87×116**。结果：**没有元素溢出卡片边界**，页面 `docH=375=vh`、`docW=667=vw`（无溢出），最小字号 9px。三个"内容大于自身盒子"的元素（`.seat-info-badge`/`.seat-identity-mark`/`.seat-hand-icon .hi-count`）经查全部 `overflow:visible` 且**内容宽度都小于盒宽**（4.3<12、9<14、5.3<16），只有高度 13>12/14——那是 9px 字 line-box 的名义高度，字形不会被裁，所以不是真问题（`inspectSeat` 的 `clip` 判据只对装备名生效，是对的）。**元素两两之间最紧的间隙**：相邻装备行的 `B("武")↔B("防")` 等为 **-1.2px**（9px 字、`line-height:1.2`=10.8px 行距 vs 约 9px 墨迹，line-box 名义重叠而字形不碰，正是重叠断言 2px 容差要吸收的）；`seat-faction ↔ seat-gen-name` 间隙 0px（对手卡）/1px（"我"的卡），是全卡最紧的一对（**⚠️ 当时据此判断"这是最脆弱的一处、字号 +1px 就会撞上"，后续做鉴别力实测时证明这个判断是错的，纠正见下一条记录**：这两个元素是 `.seat-left` 这个 flex column 里的流式兄弟，间隙就是 `gap` 本身，长高只会把下面的推开，**结构上不可能重叠**；而且 `.seat-faction` 是固定 `12px×12px` 的盒子，改字号根本不改变几何。真正可能重叠的是**跨容器的绝对定位元素**），以后动势力标或武将名的尺寸要专门回头量这一格；刚修好的 `🔖↔B("武")` 余量 3.7px、`hi-count↔B("武")` 4px。结论：除已修的 🔖，这一格没有其他真实的余量不足。
  - **③ 新建 `docs/progress-log-index.md`**。12 个分段文件合计约 **1.6MB / 578 条**，已经既不可能通读也不可能按时间回溯——**经验不被索引等于没记**。索引刻意**不做成目录**（578 条的目录本身就远超一页），而是按「根因家族」+「方法论教训」组织的检索入口，63 行、一页以内：第一节把出现 ≥2 次的根因归成 10 个家族（`normalize` 误杀合法中间态 ≥5 次、Firebase 序列化限制、链结束忘置空 pending、裸 `g` 的 `ReferenceError` 3 次、缺身份守卫 ≥4 次、pending 被覆盖（旋风连修 8 轮）、机器人永久死循环、CSS 特异性/源码顺序 ≥5 次、本回合标志清除时机、同一 UI 症状连修三次），每行给出**识别症状**和**可 grep 的关键词**；第二节收方法论教训（假修复识别、两次代理指标选错、断言必须能变色、别漏掉整个维度、刁钻样本、测量差数量级先怀疑工具、"看起来能复用"必须先读完整实现、规则数值别凭记忆）；第三节列"刻意不做"的决定，防止以后把它们当新 bug 重新排查一遍。**索引自身也做了核对**：17 个第一节关键词 + 8 个第二节案例关键词逐个实测 grep 命中数，全部 ≥2 处；并先用一个不存在的词确认核对脚本本身会报 0（否则这轮核对就是个永远绿的检查）。索引顶部写明了维护规则：**新增普通记录不需要更新索引**，只有"又踩了一遍某家族"或"开出新家族/新教训"时才回来加一行。

- **CORE-146 收尾两项：`.gitignore` 按工件类别补全 / 把"零余量"从 log 记录升级成规则化断言**：
  - **① `.gitignore` 不再只堵单个洞**。上一条只加了 `node_modules`，那是堵掉已经发生的那一个洞。**真正的根因是"本项目没有 package.json、长期也就没有真正的 `.gitignore` 兜底，而改动收尾惯用 `git add -A`"——两者相加的结果是任何落在项目根的临时工件迟早会被误提交**，与具体是哪一个无关。所以按**工件类别**一次列全这几轮实际产生过的：依赖软链（`node_modules`）、验证脚本的截图/录像输出（`*.png`/`*.jpeg`/`screenshots/` 等，**但用 `!assets/**/*.png` 保留真正入库的站点素材**）、Playwright 自己的产物（`test-results/`/`playwright-report/`/`blob-report/`/`playwright/.cache/`）、一次性验证/排查脚本及其输出（`scratchpad/`/`tmp/`/`diag.js`/`edge.js`/`*.out.txt`/`*.dump.json`）。**验证**：`git ls-files` 逐个过 `git check-ignore`，确认**没有任何已入库文件被新规则误伤**；并确认例外规则真的生效（`assets/foo.png` → 可入库，根目录 `foo.png` → 忽略）。
  - **② 零余量位置改成规则化断言，不再依赖"人记得回来查"**。上一条只是把 `seat-faction ↔ seat-gen-name` 的 0px 间隙**写进 log**，那是被动的。现在在 `inspectSeat` 里加了 `minGap`：枚举卡内所有叶子元素两两的间隙（两轴取较大者，只要有一轴分开就不算重叠），**豁免"双方都在 `.seat-equip-bar` 内"的对**（相邻装备行 9px 字、`line-height:1.2` → 行距 10.8px vs 行盒 13px，名义重叠约 1.2px，但墨迹只有约 9px、实际不相碰，是排版的正常产物），其余一律要求 **间隙 ≥0**。**刻意不做成"脆弱位置清单"**——清单要人维护，等于把"依赖记忆"从一个地方搬到另一个地方；规则化之后将来新增的元素自动被覆盖。矩阵 30 格（2/4/6/8/9 人 × 844×390 / 667×375 / 932×430 × 对手卡/我的卡）全部通过，且**每一格最紧的一对都恰好是 `seat-faction↔seat-gen-name`**，说明规则自动锁定了全局最紧处，不需要任何人工指定。
  - **③ 鉴别力实测过程中推翻了自己上一条的结论（如实记录）**。先按上一条 log 里写的"字号 +1px 就会撞上"去扰动，把 `.seat-faction` 的 `font-size` 从 9px 调到 10px——**断言没红**；再把盒高 `12px→14px`——**还是没红**。查下来是我判断错了：`.seat-faction` 和 `.seat-gen-name` 是 `.seat-left` 这个 **flex column** 里的流式兄弟，间隙就是 `gap` 本身，前者长高只会把后者往下推，**结构上不可能重叠**；而且 `.seat-faction` 有固定的 `width/height`，改字号根本不改变几何。**所以那个 0px 根本不是脆弱点**，上一条记录里的告警是错的，已回头改正。真正可能重叠的是**跨容器的绝对定位元素**（🔖 撞装备条那次就是这一类）。改用正确的扰动重测：把 `.seat-identity-mark` 的 `top` 从 38% 调到 41% → 仍全绿（间隙尚未归零）；调到 **42% → `seat-identity-mark("🔖")↔B("武")` 间隙 -0.3px，新断言变红，而旧的重叠断言（容差 2px）仍然全绿**。这一组对照精确地证明了新断言的**增量价值**：它在重叠 0.3px 时就报，旧断言要到 2px 才报；并且它只在 667×375/9人局这一格触发，同时印证了矩阵化验证的必要性。
  - **教训（已补进 `docs/progress-log-index.md` 第二节）**：**"这里余量只剩 0，很危险"这句话本身也是一个需要验证的断言，不能凭数值小就下结论**——同样是 0px，flex 流式兄弟之间是结构性安全的，绝对定位元素之间才是真风险。**看到一个危险的数字时，先问它所处的布局结构允不允许它变负**，否则会像这次一样，把一个结构上不可能出问题的地方当成最脆弱处写进文档，同时真正的风险位置反而没被识别出来。

- **CORE-146 再收尾三项：豁免判据从"按位置"改成"按布局关系" / `.gitignore` 图片规则改为根目录锚定 / 报告需区分实测与推断**：
  - **① 严格间隙断言的豁免判据重构（按位置 → 按结构）**。上一版写的是「双方都在 `.seat-equip-bar` 里就豁免」——**那是按元素在卡里的位置豁免，本质上是清单的变体**：将来别处再出现 9px 堆叠文字（判定区标签、化身行、状态标签……）还得回来手动加一条。真正的判据其实是上一条记录自己得出来的那个**结构**判据：两个元素若是同一格式化上下文里的**流式兄弟分支**，相对位置由正常流决定，前一个长大只会把后一个推开，**结构上不可能重叠**——量到的负间隙必然只是 line-box 名义重叠（行盒高 > 行距），不是布局缺陷；而**绝对定位**的元素脱离正常流、位置由 `top/left` 硬指定，才是真会撞上的那一类。改成 `flowSiblings(a,b)`：找共同祖先 → 取双方在其下的直接子分支（分支相同或缺失＝嵌套关系，不豁免）→ 检查从叶子到分支的整条路径都在正常流里。**新增元素自动归类，不需要维护任何清单。**
  - **负 margin 等要不要排除在豁免外——判断是排除，并已实测验证**。"流式兄弟结构性安全"这个前提只在元素老实待在正常流里时成立，下面几种写法都能让流式兄弟真正重叠，一旦出现前提就不成立、必须收回豁免：负 margin（最典型）、`transform`（translate 可平移到任意位置）、`position:relative` 且带偏移（保留原位空间但渲染位置任意）、`float`（会和后续行盒重叠）、父容器是 `grid`（`grid-area` 可让子项显式占同一格）。`inFlow(e)` 逐条排掉。**这几条不是假想的边界**：它们都是"看起来还在流里、其实已经能自由移动"的写法，一并豁免掉的话断言会在真出问题时保持沉默。
  - **重构后的实测结果**：矩阵 30 格全绿；每一格最紧的三对**全部变成绝对定位元素之间**——`seat-identity-mark("🔖")↔B("武") 3.7px`、`seat-identity("主")↔seat-info-badge("?") 3px`、`hi-count↔B("武") 4px`，而上一版报的 `seat-faction↔seat-gen-name 0px`（flex 流式兄弟）和装备行的 `-1.2px` 都被正确豁免掉了。**两项鉴别力实测**：①把 `.seat-identity-mark` 的 `top` 从 38% 调到 42%（绝对定位元素逼近装备条）→ 间隙 -0.3px，新断言红、旧的重叠断言（容差 2px）仍绿，**证明豁免范围扩大后断言没有变弱**；②给 `.seat-gen-name` 加 `margin-top:-8px` 把流式兄弟真拉重叠 → **30 格全部变红**（没被豁免掉），证明负 margin 那条排除确实生效。
  - **② `.gitignore` 的图片规则从"全局忽略 + assets 例外"改成"根目录锚定"**。上一版写的是 `*.png` + `!assets/**/*.png`：`*.png` 是**递归**忽略（仓库任何深度都中招），再开一个洞把 `assets/` 补回来——等于把"图片默认不入库"设成全局默认，只对当前已知的一个目录网开一面。**将来一旦新增 `assets/` 之外的素材目录（`img/`、`public/`、`docs/` 里的示意图），会被静默忽略**：`git add` 不报错、`git status` 不显示，文件永远进不了库，这种失败没有任何提示，比误提交更难发现。改成 `/*.png`（开头的 `/` 把匹配锚定在仓库根、不递归），正好覆盖"临时截图散落在项目根"这个真实场景（验证脚本都在根目录跑），子目录素材一律不受影响，**不需要任何例外规则**。**实测**：根目录 `shot.png` 仍被忽略；`assets/foo.png`、`img/new.png`、`public/logo.png`、`docs/design/diagram.png`、`sub/dir/deep.png` 全部可入库；`git ls-files` 逐个过 `check-ignore` 确认无已入库文件被误伤。
  - **③ 新增一条工作方式要求：报告时把「实测」和「推断」分开标注**。上一条记录里的 `seat-faction↔seat-gen-name 间隙 0px` 是 Playwright 实测，而「任何一方字号 +1px 就会立刻撞上」是推断——**两者用了同样的确定度报给用户**，用户照单全收、据此要求把它断言化，断言写完做鉴别力实测才发现推断是错的。用户原话：「两者同样确定度报给我，我照单全收让你断言化，断言写完才发现推断错了。分开标注我就能只对推断部分要求先验证。」以后报告里对每条结论标明来源（`实测：…（工具/样本）` / `推断（未验证）：…`），尤其是「这里很危险 / 将来会坏 / 改 X 就会 Y」这类**关于尚未发生的事**的判断，几乎都是推断。

- **PWA 支持（目标：消掉浏览器地址栏占的竖向空间）**：手机横屏视口只有 390~430px 高，而横屏布局是按 390px 逐像素抠出来的（座位卡高度、装备条行数、手牌尺寸都卡着这个预算），地址栏吃掉的几十像素占比很可观。
  - **`manifest.json`（新增）**：`display:"fullscreen"` + `display_override:["fullscreen","standalone"]`、`orientation:"landscape"`、`name`/`short_name`/`description`、`background_color`/`theme_color` 都取 `--ink` 的 `#1a1714`（和页面底色一致，启动闪屏不闪白）、`start_url:"./index.html"`、`scope:"./"`。图标 192/512 两档，各自再声明一份 `purpose:"maskable"`。
  - **图标是从现有 `favicon.png`（256×256）生成的，512 是放大产物**：192 是 256→192 的缩小重采样（无损方向，可直接用）；**512 只能从 256 放大 2 倍，清晰度不可能超过源图，属于占位**。需要补一张 **512×512 的原始高清图**（有 1024 更好，可同时覆盖未来的 maskable 安全区需求）。`favicon-180.png`（apple-touch-icon）项目里本来就有，未改动。
  - **`<head>`**：`<link rel="manifest">` + `theme-color`，以及 iOS 专用的 `apple-mobile-web-app-capable` / `mobile-web-app-capable` / `apple-mobile-web-app-status-bar-style="black-translucent"` / `apple-mobile-web-app-title` / `apple-touch-icon`（Safari 不读 manifest 的 display/orientation，必须走这套私有 meta）。`black-translucent` 让状态栏叠在页面上方，配合本来就有的 `viewport-fit=cover` 和 body 的 `env(safe-area-inset-*)` padding，刘海区域已有避让。
  - **刻意不注册 Service Worker**：本项目纯静态无构建流程，对局状态全部实时依赖 Firebase，离线缓存没有意义，反而会带来"用户拿到旧版本 JS"的风险（cache-bust 的 `?v=N` 是手动维护的）。没有 SW 不影响"添加到主屏幕"和 `display:fullscreen` 生效，只影响 Chrome 是否弹自动安装横幅（`beforeinstallprompt`），那条路由手动引导覆盖。
  - **`pwa.js`（新增，144 行）**：`pwaIsStandalone()`（三路检测：`display-mode:fullscreen`/`standalone`/`minimal-ui` 的 matchMedia + iOS 私有的 `navigator.standalone`）、`pwaIsMobile()`（**不用 UA 做主判据**，用「触摸 + 短边 ≤640」的能力特征，阈值对齐 `LANDSCAPE_GATE_MAX_WIDTH` 和 `game-bg.js` 的 `isPhoneLayout`）、`pwaFullscreenSupported()`、`pwaToggleFullscreen()`、`pwaSyncFullscreenBtn()`、`pwaSyncHint()`/`pwaDismissHint()`。
  - **全屏按钮做能力检测，不支持的平台不显示**（而不是显示了点了没反应）：**iPhone Safari 既没有 `document.fullscreenEnabled`、也没有 `documentElement.webkitRequestFullscreen`**（iOS 上只有 `<video>` 有 `webkitEnterFullscreen`），检测会返回 false；iPad Safari 有 webkit 前缀版，返回 true。**已从主屏启动时也不显示**——地址栏本来就没了，再放一个全屏按钮是纯噪音，而手机横屏的工具栏寸土寸金。`requestFullscreen` 的 Promise 一定要 `.catch`，否则用户拒绝时控制台报 unhandled rejection。
  - **首次访问引导**：底部一条 `#pwaHint`，显示条件是「移动端 + 非 standalone + 未永久关闭」三者同时满足（尤其"非 standalone"，否则已装好的用户每次进来都被劝一遍）。文案按平台分开（iOS 是「分享→添加到主屏幕」，Android 是「菜单⋮→添加到主屏幕/安装应用」，两者入口位置完全不同，混着写等于没说清）。关闭写 `localStorage['sgs_pwa_hint_dismissed']`，永久不再出现。关闭按钮给了 32×32 的触控下限。
  - **与 `landscapeGate` 的关系（用户点名要实测）**：gate 判据是 `innerHeight > innerWidth`，和 manifest 无耦合。**实测**三种组合：standalone+横屏 → gate 未误触发；standalone+竖屏 → gate 仍正确拦截（**这是期望行为不是 bug**：iOS 不支持 manifest 的 orientation 锁定，这个组合真实存在，软引导遮罩因此必须保留、没有被 PWA 取代）；standalone+竖屏+大厅 → 仍放行（CORE-119 的行为未被破坏）。`render.js` 里那段"这个项目是普通网页、不是 PWA"的注释需要在下次碰到时更新。
  - **验证与两次踩坑（都是"假绿/假红"，如实记录）**：新建 `tools/verify_pwa.js`（41 条断言）。① **CDP 的 `Emulation.setEmulatedMedia` 对 `display-mode` 在这个 Chromium 上完全不生效**（features-only / 带 media / goto 之后设置三种写法实测都拿到 `browser=true`），真正能触发 standalone 的 `--app=` 需要 headed 模式而本机没有 xvfb —— 所以改成注入 matchMedia 替身对象，并在注释和报告里**明确标注这几条验的是"我的检测逻辑在给定 display-mode 下是否正确响应"，不是"浏览器会不会因为 manifest 进入该 display-mode"**，后者只能真机验证。（替身要返回结构相同的新对象，不能 `Object.assign` 覆盖 `MediaQueryList.matches`——那是只读 getter。）② **空白断言第一版恒为 0.0%、四档视口完全一样**，是典型的永远绿：`#game` 的直接子元素里有 `gameBgCanvas`/`deathFxVideo`/`lightningFxVideo`/`movieFxVideo` 四个**绝对定位、覆盖整个视口高度**的背景层，没排除它们的话游标一上来就被推到视口底部。修正为只算正常流里的内容元素，**并补了一条"render 真的生效"的前置断言**（内容总高 > 视口一半、座位卡高 > 20px），否则元素高度全 0 时空白也会算成 0，又是一次假绿。
  - **视口变高后的布局表现（实测）**：844×{390,414,430,446} 四档均无纵向溢出；座位卡随 `33dvh` 增长（129→137→142→147px，与 `0.33×vh` 完全吻合）；**但底部空白同步增长：39→48→53→59px（10.0%→11.6%→12.3%→13.2%）**。原因是座位卡按 dvh 伸缩、而中央出牌区/手牌等是固定像素，多出来的高度只有一部分被吃掉。13.2% 仍在阈值内且空白全部集中在底部（不影响任何操作区），本次**不调 dvh 系数**——那会同时改变已经调好的 390px 档，属于本任务范围之外。
  - **两条按源码文本匹配的测试被真实带偏，一并记下**：① `run_core121_hand_scroll_test.js` 用 `indexOf` 取**第一个**同条件断点块再在块内找 `.hand{flex-wrap:nowrap}`，第一版把引导条样式另起了一个条件完全相同、位置更靠前的块，那个 `indexOf` 就被劫持，断言跑去一个根本没有 `.hand` 规则的块里查找，表现为"手牌滚动规则凭空消失"这种和本次改动毫不相干的失败（注：**该条件的重复块在 HEAD 里本来就有两个**，已用 `git show HEAD:index.html` 确认不是这次引入的）。② `run_core146_equip_short_name_test.js` 从 `content:attr(data-s)` 往回 `lastIndexOf('@media')` 找最近的断点声明，而我的注释里**原样写了带 at-media 关键字的断点条件**，于是它往回找到的是注释文本而不是真正的规则行，同样报假失败。结论：这类样式要并进既有块，且**注释里不要出现裸的 at-media 关键字**。

- **PWA 图标补齐（512/1024）+ 修掉一个"图标根本没进库"的真实缺陷**：
  - **发现的真实缺陷（线上图标 404）**：上一条 PWA 记录里说图标已生成，但 `git ls-files` 一查，`icon-192.png`/`icon-512.png` **根本没进库**——当时 `.gitignore` 还是 `*.png` + `!assets/**/*.png`，这两个文件在**仓库根**，被规则静默忽略，`git add -A` 直接跳过（不报错、`git status` 也不显示）。结果线上 `manifest.json` 引用的两个图标路径全是 404。**这正是我在改 `.gitignore` 时自己写下的那个预警**（「将来新增 assets 之外的素材目录会被静默忽略……这种失败没有任何提示，比误提交更难发现」）——预警写完当轮就踩中了，而且 `tools/verify_pwa.js` 当时是**全绿**的，因为那条断言只检查「文件在本地文件系统存在」。
  - **修法**：图标移到 `assets/icons/`（符合项目既有结构，且天然不在根目录规则的射程内），`manifest.json` 路径同步更新。**并补一条真正能抓住这类问题的断言**：从 `git ls-files` 取已跟踪文件集合，检查 manifest 引用的每个图标都在其中——「文件存在」和「文件会被部署」是两件事，只验前者就是这次假绿的根源。**鉴别力实测**：在图标尚未 `git add` 的状态下跑（＝上次那个缺陷的真实状态）→ 断言变红并直接点出 `.gitignore` 这条线索；`git add` 后 → 转绿。
  - **图标生成方式（如实说明这不是新设计稿）**：环境里没有 potrace/ImageMagick/PIL，中文字体只有 Noto Sans CJK 黑体、**没有毛笔/手写体**，用它重绘会改掉品牌字形。先尝试自己实现 marching squares 轮廓矢量化，追踪循环没能正确收敛、直接 OOM（4M 点/轮廓），放弃。最终用**超采样 + 分段 smoothstep 边缘锐化**：先 lanczos 放大到 2048，再把边缘过渡带重新映射压窄，最后下采样到 1024/512/192。
  - **中途量错过一次指标（又是代理指标选错）**：第一版锐度指标统计「亮度处于底色与字色之间的连续像素」，结果测出「原图边缘过渡 55px」这种明显荒谬的值——因为**内描边色 `#c04020`（占 13.8%）的亮度恰好落在阈值区间内**，整圈描边被当成了过渡像素，我量到的其实是描边宽度。改成只取图像**中心 56%** 区域（那里只有字和底色、没有描边）后数据才合理。
  - **第一版还丢了内描边**：只按亮度做 smoothstep 时，亮度约 89 的描边色被压到底色一侧，**整圈内描边消失＝改掉了原设计**。改成先按**饱和度**分离「字」（接近中性色，饱和度 0.09）和「红色部分」（两种红都在 0.6 左右），再在红色部分内部按亮度区分底色与描边，三色各自保留。校验色彩占比：成品 512 为 `#b03018/#b83818` 合计 56.9% / `#e8e0d0` 22.7% / `#c04020` 13.9% / 透明 2.2%，与原图的 57.5% / 23.2% / 13.8% / 2.3% 高度吻合。
  - **锐度实测**（归一化到「相当于 256 图上多少像素」，越小越锐；原图基准 1.30）：直接 lanczos 放大到 512 是 **1.46**（比原图还糊）、1024 是 1.49；成品 `icon-512` **0.79**、`icon-1024` **0.58**。即成品在两个目标尺寸上的边缘锐度都优于原图。
  - **仍需注意的边界（不是这次能解决的）**：这是从 256 源图重建的，**smoothstep 只能让已有边缘变锐，无法恢复原设计稿在 256 下就已丢失的笔锋细节**。如果原始设计稿（AI/PSD/SVG）还在，用它直接导出 512/1024 会比这个结果更好；这批图标应当视为「可用的高质量重建」，不是「新的高清设计稿」。

- **修复「从主屏启动时画面是放大的」（`pwaResetZoom`）**：用户真机反馈从 standalone 启动时画面带着上次退出时的放大状态。**明确不是「退出后保留对局状态」那件事**——那是期望行为，本次改动完全不碰对局状态，不会跳回大厅。
  - **排查掉的两个方向（都不是原因，如实记录避免以后重查）**：① **代码里恢复了 `transform:scale`/`zoom`：没有。** 全项目 grep `transform:scale` / `style.zoom` / `scale(` **零命中**；`localStorage`/`sessionStorage` 只用于身份猜测标记（CORE-115）、AI 密钥、PWA 提示，没有任何缩放相关的存取。② **viewport meta 带了非 1.0 的 `initial-scale`：没有**，一直是 `initial-scale=1.0`。但它同时**没有** `maximum-scale`/`user-scalable` 限制，双指缩放完全开放——这是「能被放大」的前提，不是「启动时就是放大的」的原因。
  - **真正的原因（方向 3）**：iOS 的 standalone web app 把**缩放级别当作 app 状态由系统保留**，下次从主屏启动时一并恢复。所以只要在游戏中双指误触放大过一次，之后每次启动都带着那个放大。页面这边无法阻止系统保留，只能在启动/恢复时主动归零。
  - **为什么不用 `user-scalable=no`/`maximum-scale=1` 一禁了之**：① iOS 10 起 Safari **故意忽略** `user-scalable=no`（无障碍考虑），靠它并不可靠；② 这个项目手机横屏下最小字号只有 9px，彻底禁用缩放会让看不清的人失去唯一的补救手段。所以保留「用户可主动放大看细节」的能力，只在**启动/恢复**这一刻归零。
  - **归零手法**：WebKit 只在 viewport meta 的 `content` **发生变化**时才重新应用缩放约束，所以先临时切到带 `maximum-scale=1.0, user-scalable=no` 的锁定态逼它把缩放压回 1，再在随后一帧（双层 `requestAnimationFrame`）撤回可缩放的基础值。**只设一次相同的值不会触发重新应用**——这一点已用鉴别力实测钉住。
  - **触发时机**：`pwaInit()`（启动）+ `window.addEventListener('pageshow')`（覆盖普通加载与 bfcache/后台恢复）。**刻意不挂 `visibilitychange`**：那个事件每次切回前台都触发，用户主动放大想看清某处细节时切出去接个消息再回来就被强行复位，反而是干扰。**并且带 `pwaIsStandalone()` 守卫**：普通浏览器里页面本来就不保留缩放，复位只会打断用户正在进行的缩放。
  - **验证（4 条新断言 + 2 次鉴别力实测）**：standalone 下先进入锁定态、随后撤回可缩放态（两个阶段都断言——只看最终态根本看不出它做过归零）；普通浏览器下 viewport 一字不动；**归零不影响对局状态**（仍在对局中、7 张对手卡、`phase=play`、没跳回大厅——这是用户点名纠正过的点，专门钉住）。鉴别力：①把锁定态那一步改成「设同样的值」→ 断言变红；②去掉 standalone 守卫 → 普通浏览器那条变红。
  - **⚠️ 真机验证仍未完成**：本机没有真机，且 Playwright 的 viewport 不存在「系统保留的缩放级别」这个概念，**上述断言验的是「归零逻辑是否按预期执行」，不是「iOS 是否真的因此复位了」**。后者必须真机：退出前记录缩放比例 → 从主屏重新打开 → 确认缩放已复位且对局内容/状态没丢。

- **「主屏冷启动画面被放大」重新排查：假设被真机推翻，移除错误修复 + 加真机诊断入口**：
  - **真机结果推翻了上一条的假设**：上一条按「iOS standalone 保留上次退出时的缩放级别」实现了 `pwaResetZoom`。用户真机验证：**即使退出前完全没有双指缩放过（缩放一直是 1.0），从主屏冷启动后画面依然是放大的**。所以放大与上次的缩放状态**无关**，是每次冷启动都会发生的事，那个假设不成立。
  - **已移除 `pwaResetZoom` 及其 `pageshow` 监听**。理由：①依据的假设已被推翻；②它从未被真机证实有效；③它本身是「会不会是这段 toggle 人为造成了一次缩放跳动」这个怀疑方向的嫌疑对象，移除后真机再测一次就能把这一层干净地排除掉。断言同步换成「确认已移除、且 viewport 未被任何代码改动」。
  - **排查方向 2 拿到了本地证据（但还不能下结论）**：页面里确实有多处直接吃视口尺寸的布局计算，而 standalone 去掉地址栏后视口高度会变大：
    - `.seat` 高度是 `clamp(70px, 33dvh, 150px)`（`index.html`），随视口高度连续变化；
    - `cardMetricsForViewport()`（`render-hand.js:363`）按 `window.innerHeight` 分档决定手牌卡宽度，**`h<=520 && coarse` 这一档给 60，跳出后给 88**。
    实测（844 宽、8 人局）：视口高 340→460 时**座位卡从 84×112 长到 113×150（+36%），而手牌卡恒为 66×94 完全不变**；高度跨过 521 时**手牌卡突变 66×94 → 88×126（+33%）**、座位卡反而缩小。iPhone 横屏高度在 390~430 区间，standalone 比 Safari 高出的那几十像素**不会跨过 520 断点**，但足以让座位卡长大十几个百分点，而手牌卡纹丝不动——**视觉上就是「上半部分被放大、比例失调」**。这与「每次冷启动都发生、与上次缩放无关」完全吻合。
    **但这仍然只是一个候选解释，不是结论**：它无法排除另一种可能——iOS standalone 横屏冷启动时 viewport 按竖屏宽度计算再整体拉大填满横屏（那会是真正的等比缩放，`visualViewport.scale ≠ 1`）。两者在肉眼上都表现为「画面被放大」，**必须靠真机数值区分**。
  - **新增 `pwaDiagnostics()` / `pwaDiagnosticsHtml()`，接进 `?` 帮助弹窗底部**：手机上没有开发者控制台、远程调试也不总是有条件，而这个问题只在真机 standalone 下复现——**继续按推断改代码是错的，必须先拿到真机数值**。诊断一次性列出 12 项：运行形态与 `navigator.standalone`、三个 `display-mode` 媒体查询、`window.inner`、`documentElement.client`（layout viewport）、`visualViewport` 的宽高/`scale`/`offsetTop`、`screen`/`avail`/`dpr`、viewport meta 原文、safe-area、关键断点命中情况、`cardMetricsForViewport()` 的计算值、手牌卡与对手座位卡的**实测**尺寸（含座位卡占视口高的百分比）。选 `?` 帮助弹窗是因为它是手机上唯一随时可点、且**不依赖房间**的入口。
    **判读方法（写在函数注释里）**：普通浏览器看一次、主屏冷启动看一次，两组对比——`visualViewport.scale ≠ 1` → 真的是页面被缩放（浏览器层）；layout viewport 宽高不同 → 视口尺寸本身变了（布局层，dvh/断点会跟着变）；`scale=1` 但 `innerWidth` 明显小于 screen 长边 → viewport 按竖屏宽度算了再被拉大填满横屏；上面都正常但 `cardWidth`/座位卡实测尺寸跳档 → 是布局计算吃了不同的视口值，不是缩放。
  - **验证边界**：Playwright 里既没有 iOS 的 viewport 行为、也没有系统状态保留，**测不出那个现象本身**。新加的断言只保证「诊断函数不报错、关键量齐备、确实接进了帮助弹窗」，**数值必须由真机读取**。

- **「主屏冷启动画面被放大」定案：不是缩放，是 `33dvh` 与固定像素手牌卡的比例失配**：
  - **真机数值（iPhone 16 横屏，standalone）**：`window.inner` / `layoutViewport` / `visualViewport` 三者全是 **852×393**，**`visualViewport.scale = 1.000`**，`screen` 393×852（设备自然方向，正常），viewport meta 原文就是 `width=device-width, initial-scale=1.0, viewport-fit=cover`，断点 `max-height:520+landscape+coarse` 命中 ✓，`cardWidth=60`、手牌卡实测 66×94，对手座位卡实测 97×130 = **33.0% 视口高**。
  - **结论：三个排查方向里，1 和 3 被数据直接排除**。`scale=1.000` 说明页面根本没有被缩放（方向 1 的 `initial-scale` 正常生效、方向 3 的「toggle 造成缩放跳动」也不存在——何况那段代码上一轮已经移除）；`inner`/`layout`/`visual` 三者完全一致，也排除了「viewport 按竖屏宽度算再拉大填满横屏」那种等比缩放。**所有数值都完全符合设计**：33.0% 正是 `33dvh`，60/66×94 正是 `h<=520 && coarse` 档的正确值。
  - **真正的原因（方向 2 坐实）**：standalone 去掉地址栏后视口高度从 Safari 的约 310~346 变成满屏 393，而 **`.seat` 是 `clamp(70px, 33dvh, 150px)`（按比例伸缩）、手牌卡是固定像素 66×94（`cardMetricsForViewport` 在 `h<=520` 档恒返回 60）**——两者伸缩方式不同步。用真机的确切参数（852 宽、8 人局）本地复现，结果与真机**逐像素一致**（97×130、33.0%），据此推算 Safari 侧：座位卡 86×114（-12.3%）/ 82×109（-16.2%）/ 77×102（-21.5%），对应地址栏占 47/63/83px。**即 standalone 下座位卡比 Safari 大 12~22%，而手牌卡一个像素没变**——视觉上就是「上半部分被放大、和手牌比例失调」，用户感知为「画面被放大」。这与「每次冷启动都发生、与上次缩放状态无关」完全吻合。
  - **本质是一个设计上的不一致，不是渲染 bug**：座位卡按视口比例伸缩、手牌卡按断点取固定值，视口高度一变两者就失配。三条可选方向（**均未实施，`33dvh` 与 `cardMetricsForViewport` 仍在用户要求的冻结中**）：①什么都不改——standalone 空间本来更大，座位卡大一点是合理的；②收紧 `.seat` 的 clamp（降低 `33dvh` 系数或 150px 上限），让它在高视口下别长那么快；③让手牌卡也随视口伸缩，两者保持同一套比例——这条最治本，但改动面最大（`cardMetricsForViewport` 是分档返回固定值的，要改成连续函数）。同一份数据也解释了此前记录的「底部空白随视口增高从 39px 涨到 59px」，两件事同源，应当一起决定。
  - **顺带修掉诊断自身的一个缺陷**：`safe-area` 那项在真机上四个值全是 `?`——第一版读的是 `--sa-top` 这类 CSS 变量，而项目里**从来没有定义过**这几个变量，等于一条读不到任何东西的诊断项。`env()` 只能在 CSS 属性值里求值、拿不到 JS 变量，改为造一个临时元素把四个 `env()` 写进 `padding`、再读回 computed 值（JS 侧唯一能拿到 safe-area 实际像素的办法），并补了一条「必须是可解析的长度值」的断言钉住。

- **手牌卡改为按视口比例伸缩，与座位卡统一伸缩逻辑（「主屏启动画面被放大」的最终修复）**：
  - **根因回顾**：`.seat` 是 `clamp(70px, 33dvh, 150px)`（按比例伸缩），手牌卡却是写死的 `66px × 94px`（固定像素）——**两者伸缩逻辑不统一**。真机实测 iPhone 16 横屏：Safari 的 layout viewport 是 852×303、从主屏启动（standalone，无地址栏）是 852×393，差 90px；于是座位卡按比例长大 21.5%，手牌卡一个像素没动（算上 Safari 自身 1.066 的缩放，手牌卡在屏幕上反而**小了 6%**）——一个变大一个变小、差 27 个百分点，看起来就是「上半部分被放大了」。**不是缩放问题**：standalone 的 `visualViewport.scale` 实测 = 1.000，且 `inner`/`layout`/`visual` 三者完全一致。
  - **用户选定的基准（重要，决定了改法方向）**：**不压座位卡去迁就 Safari，而是让手牌卡跟上**。理由是 standalone 多出来的 90px 是真实可用空间，应该被利用而不是裁掉；根因是两者伸缩逻辑不统一，就该把这个不一致修掉，而不是让 standalone 去将就 Safari 那份更局促的高度预算。硬约束：**Safari 侧数值一个像素都不许变。**
  - **实现**：`.hand .card` 改成 `--card-h:clamp(70px, 31dvh, 132px)` + `height:var(--card-h)` + `width:auto` + `aspect-ratio:66/94`，`--badge` 改成 `calc(var(--card-h) * 0.17)`（= 16/94）。**系数取 31dvh 是从「Safari 档不变」反推的**：31dvh × 303 = 93.9 ≈ 原来的 94px。宽度用 `aspect-ratio` 而不是再写一条 clamp——两条 clamp 各自取值时在边界处可能对不上，宽高比会悄悄漂移。
  - **`cardMetricsForViewport()` 同步改成连续函数**（只改 `h<=520 && coarse` 这一档，其余各档维持原有的「按视口分档返回固定值」写法），公式与 CSS 严格一一对应。**顺带修掉一处早就存在的 JS/CSS 漂移**：这一档改动前恒返回 `cardWidth:60`，而 CSS 实际是 `66px`——`fitFontSize` 一直按一个偏小的宽度在算牌名字号。改成同源公式后两边一致（Safari 档 66/66、standalone 档 86/86），并加了断言防止再次漂移。
  - **实测对照（852 宽 / 8 人局，改动前 vs 改动后）**：
    - **Safari 档 852×303**：座位卡 75×100 → 75×100、手牌卡 66×94 → 66×94、`--badge` 16px → 16px、标题字号 13 → 13、底部空白 8px → 8px。**逐项不变 ✓**（唯一变化是内部计算值 `cardWidth` 60 → 66，即上面那处漂移的修正）。
    - **standalone 852×393**：座位卡 97×130（不变）、手牌卡 **66×94 → 86×122**、标题字号 13 → 17。
  - **底部空白：没有被吃掉，仍是 40px**（用户点名要顺带看的那一项）。原因用 DOM 布局树查清楚了：`.bottom-row` 的高度由其中更高的那个孩子决定，而 `#meSeat` 是 `clamp(74px, 31dvh, 146px)` = 122px，**改动前 bottom-row 就已经是 122 高、手牌卡只有 94，中间那 28px 是容器内部的留白**（正是这条规则旧注释里写的「手牌停在原来的尺寸，被更高的『我』的卡撑起来的这一行就会在手牌下方空出一截」）。所以手牌卡长大只是**填满了 bottom-row 内部**，总高度没有增加，底部那 40px 是另一回事。**要吃掉它必须单独调 dvh 系数**（把 `33dvh`/`31dvh` 往上调，或让 `.wrap` 用 flex 分配剩余空间），这一步未做、等用户决定——40px 占视口 10.2%，也可以当作合理的呼吸留白。
  - **`html.is-standalone` 标记已加**（`pwaSyncStandaloneClass`），但**当前没有任何 CSS 规则依赖它**——`dvh` 本身就跟着 layout viewport 走，两种形态自动各取所需，不需要按模式分档。保留它是因为：①诊断时一眼能看出当前形态；②将来若真需要给 standalone 单独微调，这是现成且**可测**的 seam（比 `@media (display-mode:...)` 好——Playwright 里 display-mode 模拟不生效，那个坑已经踩过）。
  - **验证**：新增 6 条断言（两档 × 手牌卡尺寸/JS-CSS 同源/与「我」的座位卡等高），**鉴别力实测**把手牌卡改回固定 `66×94` → standalone 那 3 条全部变红、Safari 那 3 条保持绿（正好证明它只影响该影响的那一档）。全量 137/137、横屏矩阵 205/0、PWA 52/0。

- **「化身」座位卡：技能名被截断 + 挤压装备区（独立于 standalone 缩放那条线的缺陷）**：
  - **先纠正主诉里的一项：卡片并没有变宽，实测宽高与普通武将完全相同。** 用户报告「自己的座位卡比其他座位卡明显更宽」，实测（852×393，8 人局）**有无化身行都是 91×122**，而且「我」的卡实际比对手卡（97×130）**窄 6%**——`.seat` 的 `aspect-ratio:3/4` 从设定的 height 反推宽度、`min-width` 在这一档算出 0，内容撑不宽它。「更宽」的观感应当来自**内容密度**：对手卡只渲染已装备的槽（这局是 2 行），而自己的卡按设计固定显示全部 4 槽（空槽显示「—」），再叠一行化身，就是 5 行内容挤在同一张卡里。
  - **真正的两个缺陷（都实测到了）**：①**化身行被截断**——`「化身：于吉·蛊惑 ?」`在 91px 宽的卡里需要约 90px，`text-overflow` 吃掉尾巴，**丢的正好是技能名**，而技能名恰恰是这行最该看的信息；②**挤压装备区**——这一行让 `.seat-bottom` 从 49px 涨到 **68px**，压在只有 122px 高的卡上。
  - **三级递降的修法（沿用装备名简称那一套，按信息量从低到高牺牲）**：①`「化身：」`前缀单独包一层 `.hs-label`，窄断点下隐藏——青色底衬本来就在表达「这是借用的技能」，前缀是四段内容里信息量最低的；②行高/内边距压到最小（`padding:0 3px;line-height:1.25`），把纵向成本从 19px 降到约 **14px**（`seat-bottom` 68→63）；③最窄的一档（`@container (max-width:80px)`，即 852×303 的 Safari 里 8 人局、「我」的卡只有 70px 宽）再省掉行尾的「?」标记（**点击入口在整行 onclick 上、不在这个标记上，隐藏它不丢功能**），并把载荷换成简称——`.hs-body[data-s]` + `font-size:0` + `::before{content:attr(data-s)}`，**武将名与技能名各取前 2 字**，固定 5 字符宽、不随名字长短漂移。用 `@container` 而不是视口断点：决定放不放得下的是**卡片自身宽度**（人数×视口共同决定），同样 852px 宽的屏，2 人局放得下、8 人局就不行。
  - **刁钻样本暴露了中间态的不足**：只做①②时，`「于吉·蛊惑」`（5 字符）在 46px 里刚好放下、通过，但换成**化身对象里名字最长的「司马懿」**（`司马懿·鬼才`，6 字符 ≈54px）立刻超出——所以才有第③级。验证用例固定包含这个最长样本。
  - **新建 `tools/verify_huashen_seat.js`（30 条断言，按用户要求独立于人数×视口矩阵）**：3 档视口（standalone 852×393 / Safari 852×303 / SE 横屏 667×375）× 2 个化身对象（于吉·蛊惑、司马懿·鬼才），每组都和**同视口下的普通武将（小乔）做对照**，钉四件事：卡片尺寸与普通武将完全一致、化身行确实渲染出来了、化身行不被截断、装备区未被挤压（行数与对照相同、截断数不多于对照、底部内容不溢出卡片）。**鉴别力实测**：恢复前缀显示 → **6 条红**（全档位）；去掉最窄档简称规则 → **1 条红**（只有最窄档 × 最长名字）——两处破坏各自精确对应其作用范围。
  - **⚠️ 顺带修掉一个让我此前所有「带化身」验证都是假绿的 setup 缺陷**：`normalize()`（`game.js:303`）会把**不在 `huashenPool` 里的 `huashenGeneral` 整体清空**。`verify_mobile_landscape.js` 的 mkSetup 此前只设了 `huashenGeneral: i===0?'guanyu':null`、没设 `huashenPool`，于是**化身行从来没有真正渲染过**——矩阵一直声称覆盖「我=左慈带化身」，实际跑的是一张没有化身行的普通卡，全绿但那一项什么都没验证到（CLAUDE.md 规则 20 的又一次现场复现，而且这次是「测试构造的状态被服务端校验静默改写」这种更隐蔽的形式）。已补齐 `huashenPool`/`huashenSkillName`，矩阵现在真的会渲染化身行，补完后仍 205/0。
  - **一并记下但未处理的既有问题**：Safari 档（852×303）「我」的卡只有 70px 宽时，**装备名有 2 行被截断，且普通武将同样如此**——与化身无关，是自己的卡在这一档本来就太窄（对手卡 75px 尚能放下）。本次断言用「不多于对照」的口径把它排除在外，没有掩盖：它是独立的一条，需要时另开。
  - 全量 137/137、化身专项 30/0、横屏矩阵 205/0、PWA 55/0。
