# 大厅/对局/1v1 BGM Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 四档 BGM（首页 lobby / 房内等待 room / 对局 game / 1v1 duel），与大厅视频音轨分离。

**Architecture:** 单 `<audio id="bgmPlayer">`。`game-bg.js` 只认模式字符串，不读 `g`。`render.js` / `enterGame` / `backToLobby` 调 `setBgmMode`。聊天 🔊 兼管 BGM。关房左侧 ⏭ 调 `skipBgm`。

**Tech Stack:** 原生 HTML/JS；测试 `testclass/run_*.js` + `vm` 沙箱；cache-bust `check_cache_bust.js`。

**Spec:** `docs/superpowers/specs/2026-08-31-bgm-design.md`

## Global Constraints

- `game-bg.js` 不读对局状态；模式由调用方传入 `'lobby'|'room'|'game'|'duel'|'off'`。
- `#bgVideo` **永远 mute**（`unmuteBgVideo` 不再给 bgVideo 取消静音；改 unmute `#bgmPlayer`）。
- 曲池硬编码 `BGM_TRACKS`；缺文件/`error` 静默跳过，池空则停。素材可后补，数组先写路径。
- 仅 lobby 每次进入播 2 首后停；room/game/duel 不限首数，`ended` 再抽（池>1 避开上一首）。
- 终局 hold：当前曲直到 `ended` 或 40s（墙钟，先到为准）→ `room`；回首页才 `lobby`。hold 中 skip = 立刻 `room`。
- 特效起播 `pauseBgmForFx()`，`hideFxVideo`/`girlFxEnd` 后 `resumeBgmAfterFx()`（从暂停点续，不换曲）。
- `toggleChatVoice` 调 `setBgmMuted(!chatVoiceEnabled)`；技能/特效音轨不管。
- 音量 0.35；切曲约 300ms 音量 fade。`skipBgm` 语义同当前档 `ended`。
- 改 JS 必须同提交 bump `index.html` 对应 `?v=`。不 push。中文注释。`game-bg.js` 用 `var`。
- 不做：新静音按钮、Web Audio、三路预加载、按角色切曲、手机禁 BGM。

---

### Task 1: BGM 引擎（game-bg.js 纯逻辑 + 单测）

**Files:**
- Modify: `game-bg.js`（`unmuteBgVideo` 之后追加引擎；改 `unlockFxAudio` 跳过 bgVideo、改 unmute bgmPlayer）
- Modify: `index.html`（`game-bg.js?v=34`→`35`）
- Test: Create `testclass/run_bgm_test.js`

**Interfaces:**
- Produces:
  - `setBgmMode(mode)` mode = `'lobby'|'room'|'game'|'duel'|'off'`
  - `skipBgm()`
  - `setBgmMuted(muted:boolean)`
  - `pauseBgmForFx()` / `resumeBgmAfterFx()`
  - `BGM_TRACKS` 对象（测试可改写）

- [ ] **Step 1: 写失败测试** `testclass/run_bgm_test.js`

沙箱加载 `game-bg.js`，mock `#bgmPlayer`：`{ muted:true, src:'', paused:true, volume:1, style:{}, play(){ this.paused=false; this._plays=(this._plays||0)+1; return {catch(){}}; }, pause(){ this.paused=true; this._pauses=(this._pauses||0)+1; }, load(){}, addEventListener(ev,fn){ (this._ls=this._ls||{})[ev]=(this._ls[ev]||[]).concat(fn); }, fire(ev){ (this._ls[ev]||[]).forEach(function(f){f();}); } }`。`document.getElementById('bgmPlayer')` 返回它。`BGM_TRACKS` 测前改成短数组：

```js
BGM_TRACKS.lobby=['a.mp3','b.mp3'];
BGM_TRACKS.room=['r.mp3'];
BGM_TRACKS.game=['g1.mp3','g2.mp3'];
BGM_TRACKS.duel=['d.mp3'];
```

断言（每条 `check`）：
1. `setBgmMode('game')` → `src` 是 `g1.mp3` 或 `g2.mp3`，`play` 被调。
2. 再 `setBgmMode('game')` → `_plays` 不增加（同模式不重载）。
3. `setBgmMode('duel')` → `src==='d.mp3'`。
4. `setBgmMode('off')` → `paused===true`。
5. game 池 2 首：记下当前 src，`fire('ended')` → 新 src 是另一首。
6. lobby：`setBgmMode('lobby')` 两次 ended 后 `paused===true`（2 首停）。再 `setBgmMode('lobby')` 重置计数能再播。
7. `skipBgm()` 在 game：src 换成另一首。
8. `setBgmMuted(true)` → paused；`setBgmMuted(false)` 且当前 mode 非 off → 再 play。
9. `pauseBgmForFx()` pause；`resumeBgmAfterFx()` play 且 src 不变。
10. 空池 `BGM_TRACKS.game=[]` + `setBgmMode('game')` 不抛。
11. `unlockFxAudio`/`unmuteBgVideo` 后 `bgVideo.muted` 仍 true，`bgmPlayer.muted===false`（需 mock bgVideo）。

- [ ] **Step 2: 跑测确认失败**

Run: `node testclass/run_bgm_test.js`  
Expected: FAIL（`setBgmMode is not defined`）

- [ ] **Step 3: 实现引擎**（`game-bg.js`，`unmuteBgVideo` 块之后）

```js
var BGM_TRACKS = {
  lobby: ['assets/audio/bgm-lobby.mp3','assets/audio/bgm-lobby01.mp3'],
  room:  ['assets/audio/bgm-room.mp3'],
  game:  ['assets/audio/bgm-game.mp3'],
  duel:  ['assets/audio/bgm-duel.mp3']
};
var BGM_VOL = 0.35;
var bgmMode = null, bgmLastSrc = null, bgmLobbyPlays = 0, bgmMuted = false, bgmFxPaused = false, bgmHoldTimer = 0, bgmHold = false;

function bgmEl(){ return (typeof document!=='undefined') ? document.getElementById('bgmPlayer') : null; }
function bgmPick(mode, avoid){
  var pool = (BGM_TRACKS[mode]||[]).slice();
  if(avoid && pool.length>1) pool = pool.filter(function(p){ return p!==avoid; });
  if(!pool.length) return null;
  return pool[Math.floor(Math.random()*pool.length)];
}
function bgmPlaySrc(src){
  var v = bgmEl(); if(!v || !src) return;
  v.src = src; bgmLastSrc = src;
  if(typeof v.load==='function') v.load();
  v.volume = BGM_VOL;
  if(bgmMuted || bgmFxPaused){ if(typeof v.pause==='function') v.pause(); return; }
  var p = v.play && v.play();
  if(p && typeof p.catch==='function') p.catch(function(){});
}
function bgmOnEnded(){
  if(bgmHold){ bgmHold=false; if(bgmHoldTimer){ clearTimeout(bgmHoldTimer); bgmHoldTimer=0; } setBgmMode('room'); return; }
  if(bgmMode==='lobby'){
    if(bgmLobbyPlays>=2){ setBgmMode('off'); return; }
  }
  var next = bgmPick(bgmMode, bgmLastSrc);
  if(!next){ setBgmMode('off'); return; }
  if(bgmMode==='lobby') bgmLobbyPlays++;
  bgmPlaySrc(next);
}
function bindBgmEnded(){
  var v=bgmEl(); if(!v || v.__bgmEnded) return;
  v.__bgmEnded=true;
  v.addEventListener('ended', bgmOnEnded);
  v.addEventListener('error', function(){ bgmOnEnded(); });
}
function setBgmMode(mode){
  if(mode!=='lobby' && mode!=='room' && mode!=='game' && mode!=='duel' && mode!=='off') return;
  bindBgmEnded();
  if(mode==='off'){
    bgmMode='off'; bgmHold=false;
    var v=bgmEl(); if(v && v.pause) v.pause();
    return;
  }
  if(mode===bgmMode && !bgmHold) return;
  if(mode==='lobby' && bgmMode!=='lobby') bgmLobbyPlays=0;
  bgmHold=false; if(bgmHoldTimer){ clearTimeout(bgmHoldTimer); bgmHoldTimer=0; }
  bgmMode=mode;
  var src=bgmPick(mode, null);
  if(!src){ setBgmMode('off'); return; }
  if(mode==='lobby') bgmLobbyPlays++;
  bgmPlaySrc(src);
}
function skipBgm(){
  if(bgmMuted || bgmMode==='off' || !bgmMode) return;
  if(bgmHold){ bgmHold=false; if(bgmHoldTimer){ clearTimeout(bgmHoldTimer); bgmHoldTimer=0; } setBgmMode('room'); return; }
  bgmOnEnded();
}
function setBgmMuted(muted){
  bgmMuted=!!muted;
  var v=bgmEl(); if(!v) return;
  if(bgmMuted){ if(v.pause) v.pause(); }
  else if(bgmMode && bgmMode!=='off' && !bgmFxPaused){ var p=v.play&&v.play(); if(p&&p.catch) p.catch(function(){}); }
}
function pauseBgmForFx(){ bgmFxPaused=true; var v=bgmEl(); if(v&&v.pause) v.pause(); }
function resumeBgmAfterFx(){
  bgmFxPaused=false;
  var v=bgmEl(); if(!v || bgmMuted || !bgmMode || bgmMode==='off') return;
  var p=v.play&&v.play(); if(p&&p.catch) p.catch(function(){});
}
function beginBgmHold(){
  if(bgmMode==='off' || bgmMuted) return;
  bgmHold=true;
  if(bgmHoldTimer) clearTimeout(bgmHoldTimer);
  bgmHoldTimer=setTimeout(function(){ if(bgmHold){ bgmHold=false; setBgmMode('room'); } }, 40000);
}
```

改 `unlockFxAudio`：循环里 `if(id==='bgVideo') return;` 跳过（用 continue）。循环后：`var b=bgmEl(); if(b) b.muted=false;`。

`setBgmMode('lobby')` 首次：`bgmLobbyPlays` 从 0 起播第 1 首时变成 1；ended 时若 `<2` 再抽并 +1。实现时 ended 判断用「已经播完的首数」：起播时 +1，ended 时若 lobby 且 plays>=2 则 off。与测试「两次 ended 后停」对齐：起播1 → ended 抽第2 起播2 → ended 停。

- [ ] **Step 4: 跑测通过** `node testclass/run_bgm_test.js` + `node check_cache_bust.js`

- [ ] **Step 5: 提交**

```bash
git add game-bg.js index.html testclass/run_bgm_test.js
git commit -m "feat(bgm): 四档曲池引擎 + skip/mute/fx暂停"
```

---

### Task 2: DOM（audio + ⏭）+ 进房/回首页接线

**Files:**
- Modify: `index.html`（`#bgVideo` 旁加 audio；`#gameToolbar` 全屏与关房之间加 ⏭；`game-bg.js?v` 再 +1 若本任务改了 js——本任务若只改 html+lifecycle 则 bump `room-lifecycle.js`）
- Modify: `room-lifecycle.js:89` `enterGame` 在 `pauseBgVideo` 后：`if(typeof setBgmMode==='function') setBgmMode('room');`
- Modify: `room-lifecycle.js:712` `backToLobby` 在 `resumeBgVideo` 后：`if(typeof setBgmMode==='function') setBgmMode('lobby');`
- Modify: `index.html` `room-lifecycle.js?v=` 递增
- Test: `testclass/run_bgm_test.js` 追加源码断言；`run_core119_lobby_portrait_gate_test.js` 应仍绿（只加一行 typeof 调用）

**Interfaces:**
- Consumes: `setBgmMode`（Task 1）

- [ ] **Step 1: 测试** — `run_bgm_test.js` 读 `index.html` 字符串：
  - 存在 `<audio id="bgmPlayer"`
  - 存在 `id="bgmSkipBtn"` 且 `onclick="skipBgm()"`
  - `#bgmSkipBtn` 出现在 `fullscreenBtn` 与 `closeRoomBtn` 之间（indexOf 顺序）
  - `enterGame` 源码含 `setBgmMode('room')`；`backToLobby` 含 `setBgmMode('lobby')`

先跑：新断言 FAIL。

- [ ] **Step 2: HTML**

`#bgVideo` 后：

```html
<audio id="bgmPlayer" preload="none" aria-hidden="true"></audio>
```

`#fullscreenBtn` 后、`#closeRoomBtn` 前：

```html
<button id="bgmSkipBtn" class="icon-btn" title="下一曲" onclick="skipBgm()">⏭</button>
```

- [ ] **Step 3: lifecycle 两行**（见上）

- [ ] **Step 4: 跑** `node testclass/run_bgm_test.js` && `node testclass/run_core119_lobby_portrait_gate_test.js` && `node check_cache_bust.js`

- [ ] **Step 5: 提交** `feat(bgm): audio 元素 + 下一曲按钮 + 进房/回首页切档`

---

### Task 3: render 切档 + 终局 hold + 特效暂停 + 🔊

**Files:**
- Modify: `render.js`（`render()` 里 `maybePlayMovieFx` 附近调 `maybeUpdateBgm(g)`；gameOver 事件时 `beginBgmHold()`）
- Modify: `game-bg.js` `hideFxVideo` 末尾 `resumeBgmAfterFx`；各 `trigger*Fx` / `triggerGirlFx` 起播处 `pauseBgmForFx`（typeof 守卫）
- Modify: `render-log.js:262` `toggleChatVoice` 末尾 `if(typeof setBgmMuted==='function') setBgmMuted(!chatVoiceEnabled);` 页面加载后按 `chatVoiceEnabled` 初值调一次
- Modify: `index.html` bump `render.js` / `render-log.js` / `game-bg.js` `?v=`
- Test: `testclass/run_bgm_test.js` 追加；`run_chat_tts_test.js` 应仍绿；`run_fx_video_audio_test.js` 改：unmute 后 bgVideo 仍 muted，bgmPlayer unmuted

**Interfaces:**
- Consumes: Task 1 全部函数
- `maybeUpdateBgm(g)`：
  ```js
  function maybeUpdateBgm(g){
    if(typeof setBgmMode!=='function') return;
    if(typeof bgmHold!=='undefined' && bgmHold) return; // 终局 hold 期间不切档
    var gameEl=document.getElementById('game');
    var inGame=gameEl && !gameEl.classList.contains('hidden');
    if(!inGame){ setBgmMode('lobby'); return; }
    if(!g || !g.started){ setBgmMode('room'); return; }
    var n=(typeof aliveCount==='function')?aliveCount(g):(g.players||[]).filter(function(p){return p&&p.alive;}).length;
    if(n===2) setBgmMode('duel');
    else setBgmMode('game');
  }
  ```
  `bgmHold` 必须是 game-bg 的全局（Task 1 已 `var bgmHold`）。

- [ ] **Step 1: 测试**
  - 源码：`render.js` 含 `maybeUpdateBgm`；`toggleChatVoice` 含 `setBgmMuted`。
  - `run_fx_video_audio_test.js`：unmute 后 `videos.bgVideo.muted===true`；若 mock 了 bgmPlayer 则 unmuted。
  - 沙箱：`setBgmMode('game')` 后 `beginBgmHold()` + `setBgmMode('duel')` 被 maybeUpdateBgm 短路（hold 时 mode 仍 game）。`fire('ended')` 后 mode 变 room。

- [ ] **Step 2: 实现接线**（见上）。`maybePlayMovieFx` 分到 `gameOver` 且即将播时 `if(typeof beginBgmHold==='function') beginBgmHold();`——在 dispatch 之后即可，hold 不依赖视频是否真播。

- [ ] **Step 3: 跑** `run_bgm_test.js`、`run_fx_video_audio_test.js`、`run_chat_tts_test.js`、`run_girl_fx_layer_test.js`、`check_cache_bust.js`

- [ ] **Step 4: 提交** `feat(bgm): render 切档 + 终局hold + 特效暂停 + 聊天静音联动`

---

### Task 4: 全量回归 + TASKS.md

- [ ] **Step 1:** `node run_all_tests.js` 期望 0 failed（偶发 `run_bot_observability_test` 可单跑确认）。
- [ ] **Step 2:** 更新 `TASKS.md` Phase 2/3 计划完成、Phase 3/3 实现完成。缺素材时注明「数组已接、文件后补则静默」。
- [ ] **Step 3: 提交** `docs: BGM 实现完成`

## Self-Review

- Spec 四档 / 2 首 lobby / 终局 40s / skip / 🔊 / 特效暂停 / bgVideo mute / ⏭ 位置 → Task 1–3 全覆盖。
- 无 TBD。函数名与 spec 一致：`setBgmMode`/`skipBgm`/`setBgmMuted`/`pauseBgmForFx`/`resumeBgmAfterFx`/`beginBgmHold`。
