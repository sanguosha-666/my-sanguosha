# 项目历史改动记录 · 第 5/7 段

> 本文件是 `CLAUDE.md` 「五、当前进度」历史内容的一部分（原文件 1157 行/约975KB，
> 2026-08-01 拆分为 7 个按时间顺序排列的分段文件，本文件是第 5 段）。
> 收录第 626 ~ 656 条改动记录（共 31 条，
> 原文件行号约 804~934）。
>
> **不需要通读**：如果要了解某个具体功能当初是怎么实现/为什么这么设计的，直接在
> `docs/progress-log-*.md` 全部分段文件里 grep 关键词（武将名/文件名/函数名）即可，
> 不需要按顺序读完。总览规则见 `CLAUDE.md` 开头的索引说明。

---

- **凌统【旋风】杀结算中失去装备的 resume 续接机制(方案A:麒麟弓/猛进,复用 `resume={type:'sha'}` 范式)**。上次修好了"独立失去装备"三入口(顺手/拆桥/借刀),但麒麟弓弃马/庞德猛进拆牌是**在杀结算中途**(命中/被闪抵消后、`finishSingleShaTarget` 收尾前)触发旋风,当时记为已知限制不修——naive"别覆盖 pending"会让杀收尾永久跳过、软锁。本次做正式续接。
  **核心:零新模式,完全复用 `resume={type:'sha'}` + `resumeAfterInterrupt`**——这正是濒死/遗计打断杀命中时用的同一套 descriptor 和分派器(`resumeAfterInterrupt` 的 `'sha'` 分支:`if(g.fangtianQueue) advanceFangtianQueue; else g.phase='play'`)。旋风在杀结算中触发,是同一条杀尾巴的又一个打断点。
  **注入点(4处,写法照抄 `consumePendingHookQueue` 的 pendingBefore 快照约定)**:每处在触发 onLoseEquip 的动作(`discardMount`/`applyTrickOnEquip`)前后快照 `g.pending`,若变了(旋风挂起了 xuanfengPick),就 `g.pending.resume={type:'sha'}` 并上报挂起(return),不再往下跑 `g.pending=null;finishSingleShaTarget` 把旋风覆盖:①`maybeStartQilin` 单马(`weapons.js`)②`qilinResolve` 双马选一(`weapons.js`)③猛进 auto-single(`game.js`)④`mengjinPick`(`game.js`)。
  **收尾分派(2处)**:`executeXuanfeng`(弃满2张)和 `cancelXuanfeng`(选择不发动)的装备触发分支,从 `g.phase=previousPhase` 改成三向:`trigger==='discard'`→举荐/据守(原有);`else if(resume)`→`resumeAfterInterrupt(g, resume, endingSeat)`(接回杀收尾);`else`→`previousPhase`(独立失去装备,原有不变)。**两条收尾路径都要走 resume**——无论旋风有没有实际弃牌,被打断的杀收尾都必须继续,否则漏 checkWin/方天队列、软锁。
  **方天画戟嵌套天然正确**:方天队列位置存全局 `g.fangtianQueue`、不在被打断的局部栈里,旋风结束 `resumeAfterInterrupt({type:'sha'})` 的 `advanceFangtianQueue(g)` 正确推进到下一目标(实测:凌统是方天多目标之一被麒麟弓触发旋风,续接后 phase 是下一目标的 respond、不是 play)。
  **【v1 已知限制,已在 v2 解决,见下方"猛进触发旋风时正确续接青龙偃月刀'再来一杀'"条目】**:`resume={type:'sha'}` 接回的是 `finishSingleShaTarget` 尾巴,**不重跑麒麟弓之后的趫猛/烈刃、或猛进之后的青龙/贯石斧**。①麒麟弓+趫猛/烈刃同触发需攻击者多 caps=左慈化身,astronomically rare,**这一半维持原样不修**(见下方 v2 条目的风险评估——趫猛/烈刃触发于杀命中,和猛进/青龙/贯石斧触发于杀被闪抵消是互斥的两条分支,结构上不可能同时发生,不属于同一类问题);②**猛进+青龙这一半已经在 v2 修复**——猛进拆凌统装备触发旋风时不再跳过庞德的青龙"再来一杀"。
  **独立失去装备三入口(顺手/拆桥/借刀)不受影响**:它们不 attach resume,`pending.resume` 为 undefined → 走 `previousPhase` 原路径,行为完全不变(实测锁定)。
  **测试**(`run_xuanfeng_resume_test.js`,25项,未落盘):麒麟弓单马/双马、猛进 auto-single/pick 各自"旋风挂起+resume.type===sha";两种收尾(弃满2张→play+目标真弃2张 / 不发动→play)不软锁;方天嵌套续接推进下一目标;独立失去装备回归(resume undefined、previousPhase=play);已知限制场景(庞德青龙+猛进→不软锁);B路径回归(弃牌阶段 trigger==='discard'、无 resume)。`git stash` 确认修复前9项机制断言变红、独立失去装备/B路径在前后都绿。既有回归(上次旋风独立入口套件已更新麒麟弓那两条过时断言 + 李典 + 本轮六套)全绿。

- **修复公孙瓒【趫猛】/祝融【烈刃】拿走目标装备时缺失 `onLoseEquip` 触发,顺带修好一个独立的既有 bug(方天画戟队列在这两个技能结算后卡死)**。上次记为待优化点,本次排查确认后按方案A实现。
  **排查结论:两者都是 mid-杀**——调用位置和麒麟弓/猛进完全同一处（`respondShan` 不闪分支,`dealDamage` 之后、`finishSingleShaTarget` 之前:`if(maybeStartQiaomeng(...)) return g; if(maybeStartLieRen(...)) return g;`）。真实 dump 确认 `onLoseEquip` 完全没触发（枭姬摸牌数为0）。
  **排查中发现的更深问题(不是 naive 补 triggerHook 就能解决的原因)**：`pickQiaomengEquip`/`respondLieRen` 的收尾此前是裸的 `g.pending=null;g.phase='play'`,从不调用 `finishSingleShaTarget`。这带来两个后果:①若直接在装备移除后插 `triggerHook`,此刻 `g.phase` 还是 `'qiaomengPickEquip'`/`'lieRenRespond'` 这种**死相**（对应 pending 已经不存在了),旋风若不发动、`previousPhase` 会捕获到这个死相并恢复回去 → 软锁(和顺手/拆桥/借刀当初的坑同构);②`checkWin`/方天画戟队列推进被完整跳过——真实 dump 证实（方天画戟+趫猛组合下,拿走目标装备后队列卡死,座位2永远不会被问）。
  **实现（方案A,套用麒麟弓/猛进模式,零新写法）**：①**先 reorder**——装备移除后立即 `g.pending=null;g.phase='play'`,再触发 `triggerHook`,让钩子捕获正确的休止相；②`pendingBefore` 快照，钩子挂起新 pending（旋风）就 `resume={type:'sha'}` + return，不覆盖；③**收尾从裸 `phase='play'` 改成调用 `finishSingleShaTarget(g)`**（麒麟弓/猛进本来就这么收尾）——不是新设计，是让这两个函数走上早就存在的正确路径，**顺带修复了方天画戟队列卡死**这个独立 bug（真实 dump 验证：非旋风场景下，趫猛/烈刃拿走方天目标1的装备后，队列正确推进到座位2）。烈刃额外处理：拼点赢拿到的牌可能来自手牌也可能来自装备区，只有 `fromEquip===true`（真的来自装备区）时才触发 `onLoseEquip`。
  **枭姬连带修复**：趫猛/烈刃拿走孙尚香装备后正确摸2张（实测锁定，属于修对而非引入副作用）。
  **v1 已知限制沿用**：`resume={type:'sha'}` 不重跑趫猛/烈刃之后可能存在的其它效果——当前项目里趫猛/烈刃之后没有类似"猛进+青龙"那种链式效果，暂无实际影响；若未来出现同类组合，按同一原则记入待优化点即可。
  **测试**（`run_qiaomeng_lieren_test.js`，25项，未落盘）：趫猛/烈刃核心触发+resume挂起、两种旋风收尾（弃满2张/不发动）不软锁、枭姬摸2张对照、方天队列非旋风场景纯推进（独立bug修复验证）、方天队列+旋风同时触发的复合场景、死相软锁回归（非相关目标场景确认落到 play 而非死相）、既有入口回归（顺手拆桥/麒麟弓/猛进不受影响）。`git stash` 确认修复前11项机制断言变红（onLoseEquip未触发、resume为null、枭姬未摸牌、方天队列卡在play且to为null），回归类断言在前后都绿。既有回归（本轮旋风套件+李典+六套）全绿。

- **修复颜良文丑【双雄】座位按钮漏 `isZhichiImmune`/`weimu` 校验，会渲染在服务端必然拒绝的座位上（已解决，移出待优化点）**：`render.js` 里"双雄:决斗"座位按钮的触发条件原来手写了一份简化版目标校验——只查 `hasCap(p,'kongcheng') && (p.hand||[]).length===0`（空城），漏了 `CARD_PLAYS['决斗'].canTarget` 真正校验的另外两项：`isZhichiImmune(g,targetSeat,card)`（陈宫【智迟】免疫）和 `hasCap(target,'weimu') && isBlackTactics(card)`（贾诩【帷幕】不能成为黑色锦囊目标）。**修复**：直接把 `blocked` 变量的手写判断整体替换成调用 `CARD_PLAYS['决斗'].canTarget(g, meP, selCard, i)`，和"武圣:杀"座位按钮（见上）完全同一写法（正向条件判断，不再用 `blocked` 取反）——外层 `i!==mySeat && p.alive` 两项校验本来就是对的（决斗没有声明 `allowSelf`，硬编码 `i!==mySeat` 和引用 `!spec.allowSelf` 语义完全等价，不需要改），只替换了目标限制这一层。**排查发现原来的 `blocked` 变量描述有误导性**：它读取的是 `g.players[i]`（目标座位），不是"双雄自己的触发前提"——双雄真正的自身触发条件是 `isShuangxiongDuelSel`（外层 `if` 已有，未改动），`blocked` 只是"目标空城"这一条 `canTarget` 内部检查的手写重复，替换后这条空城检查随 `canTarget` 整体调用自然包含，不是"额外保留的双雄专属条件"。**顺带确认了武圣批次没有同类问题**：直接读取"武圣:杀"按钮实现（render.js 约1152-1162行）及其紧邻的既有注释（约1145-1149行，本身就是当初写代码时留下的、明确指出"双雄那个按钮就是手写了简化版...新代码不要重蹈覆辙"的教训记录），确认武圣批次从一开始就正确调用了完整的 `CARD_PLAYS['杀'].canTarget`，不需要修复。**测试**（`run_shuangxiong_seat_test.js`，11项，未落盘，复用武圣座位按钮测试同一套 vm 沙箱 harness）：智迟免疫场景按钮不出现+服务端 `canTarget` 确认拒绝（核心，`git stash` 验证修复前变红：`{"btns":["双雄:决斗"]}`）、帷幕保护场景同上（核心，同样验证变红）、正常场景（目标既不智迟也不帷幕）按钮正常出现且点击后牌离手+进入决斗结算结构、目标已死亡的座位不出现按钮、决斗不允许自选目标故自己座位不出现按钮（含佐证 `CARD_PLAYS['决斗'].allowSelf` 确实是 falsy）、非双雄武将选中异色牌无按钮的对照组。既有回归套件（faction/negative_hp/qiaomeng_lieren/respond_pick/tiaoxin_guard/wusheng_popup/wusheng_seat/xuanfeng_resume/xuanfeng）全部重跑零回归。`?v=` 173→174。

- **补上马谡【散谣】`respondSanyao`/`respondSanyaoTarget` 的身份守卫（代码卫生层面加固，散谣技能本身仍是死代码，见下方说明）**：和已修复的挑衅（`respondTiaoxinChoice`）同型缺陷——两个函数原来只校验 `phase`/`pending.type`，没有校验"发起调用的客户端是不是该做这个决定的人本人"。**先重新排查确认结论未过期**：`startSanyao` 全项目仍是 0 调用点（用正则精确排除函数自己的定义行后重新 grep，不是沿用旧结论）——这两个函数依然是死代码，只有直接构造 `g.pending` 状态才能触达。**修复**：`respondSanyao`/`respondSanyaoTarget` 均在 phase/pending.type 校验之后加一句 `if(g.pending.from!==mySeat) return g;`——**字段名是重新读代码确认的，不是照抄挑衅**：`respondSanyao` 内部 `p=g.players[g.pending.from]`（弃 p 的牌）、`respondSanyaoTarget` 用 `g.pending.from` 写日志和设置新 `pending.target`，两个函数的响应者字段都是 `from`（不是挑衅的 `to`——挑衅的响应者是"被挑衅的目标"，散谣的响应者是"发动散谣、要做选择的马谡本人"，语义不同，字段名恰好也不同，不能想当然复用）。`respondSanyaoTarget` 顺带补了 `!g.pending` 判断（原来只查 `phase`，`phase==='sanyaoChooseTarget'` 但 `pending` 恰好是 `null` 这种脏数据场景下访问 `g.pending.from` 会直接抛异常，身份守卫本身需要 `g.pending` 先存在才能读 `.from`，这是让守卫自己不崩溃的必要前提，不是额外功能）。**测试**（`run_sanyao_guard_test.js`，17项，未落盘）：由于没有调用点、无法走正常游戏流程复现，直接用 vm 沙箱构造 `g.pending`/`g.phase` 为散谣中间态，模拟"非 `g.pending.from` 的座位发起调用"（核心：两个函数各自的非法调用场景，断言 `pending`/手牌/血量/`phase` 全部原封不动）+ 合法座位（`g.pending.from` 本人）调用正常生效的正对照（两个函数各一组）+ `g.pending` 为 `null` 时不抛异常的边界补测 + 前置/收尾各一次 `startSanyao` 调用点排查（确认改动前后均为 0，没有意外把散谣接成可达）。`git stash` 验证修复前 7 项断言精确变红（含"越权座位把手牌真弃了、血量真扣了、`pending` 真的被清空/篡改"这类具体状态变化，以及 `g.pending` 为 `null` 时确实抛出 `Cannot read properties of null (reading 'candidates')` 异常），恢复修复后全部转绿。既有回归套件（faction/negative_hp/qiaomeng_lieren/respond_pick/shuangxiong_seat/tiaoxin_guard/wusheng_popup/wusheng_seat/xuanfeng_resume/xuanfeng）全部重跑零回归。`?v=` 174→175。

- **马谡【散谣】第一步（服务端核心逻辑）：从零重新设计，替换掉从未真正跑通过的旧骨架，脚本可直接调用验证，UI 接入是独立的第二步（未做）**。这不是在旧骨架上打补丁——设计阶段确认旧骨架（`startSanyao`/`respondSanyao`/`respondSanyaoTarget` + `sanyao`/`sanyaoChooseTarget` 两阶段服务端 pending）从一开始就没有正确工作过：`findMaxHpSeats(g,excludeSeat)` 把发动者自己排除在候选之外（违反官方原文"全场"）、`render-controls.js` 的 `p.seat!==mySeat` UI 判断因为玩家对象根本没有 `seat` 字段而永远为真（对包括马谡本人在内的所有人都不渲染）、`respondSanyao` 弃装备时完全没有调用 `onLoseEquip` 钩子。这次整体重写，前两个问题在设计阶段已用 WebSearch/WebFetch 核实官方文本（sanguosha.cn：`出牌阶段限一次，你可以弃置一张牌并选择一名体力值最大的角色，然后你对其造成1点伤害`）确认了"全场"含发动者自己、平局时由发动者从候选中挑一个（不是全部命中/不是随机）。
  - **`findMaxHpSeats(g)`**（`skills.js`，签名去掉 `excludeSeat`）：改为对全体存活玩家（含马谡自己）取最大体力值，返回并列座位数组。全项目重新确认只有一处调用（本函数自身，即新的 `sanyao()`），改签名零连带风险。
  - **弃牌范围（手牌+装备，不含判定区）**：官方文本"弃置一张牌"不带区域限定，项目里对这类措辞的既有解释口径类推自贯石斧（`guanshifuOptionCount`，CLAUDE.md 有专门记录：官方"弃两张牌"同样不带区域限定，查证结论是手牌+装备、不含判定区）——散谣按同一套口径解释，**推翻旧骨架"含判定区"的设定**。新增 `sanyaoOptions(p)`（`skills.js`）直接复刻贯石斧 `guanshifuOptions` 的 key 格式（`'hand:'+idx` / `'equip:'+slot`），供服务端解析、也供以后 UI 步骤直接复用同一份候选列表。
  - **新原子函数 `sanyao(costKey, targetSeat)`**（`skills.js`）：这是巧变已经确立的既有规则的又一次应用——"全程只有技能拥有者一人做选择、不需要其他玩家响应"的技能，走客户端本地累积选择、最后一次性原子提交，不需要服务端两阶段 pending。服务端仍然完整重新校验：`costKey` 必须真实对应马谡当前手牌里的某一张或某个非空装备槽（不信任客户端传入的下标/槽名）；`targetSeat` 必须在服务端重新计算出的 `findMaxHpSeats(g)` 候选集内（不信任客户端传入的目标）。弃牌若是装备，先把 `phase` 设回休止相（`'play'`）再触发 `onLoseEquip`（`pendingBefore` 快照+检查是否被打断这套写法照抄 `pickResolve`/凌统旋风的既有约定），若钩子挂起了新 pending（如旋风）则把 `resume={type:'sanyaoDamage',casterSeat,target}` 挂在新 pending 上、这次 tx 到此为止；否则直接调用共用尾巴 `finishSanyaoDamage(g, casterSeat, targetSeat)` 完成伤害结算。**这条 onLoseEquip 接入目前是可以正确工作、但客观上不可达的死代码分支**（旋风/枭姬只在凌统/孙尚香身上，一人只能是一个武将，不可能同时是马谡又是他们）——按 CLAUDE.md 里"新增失去装备入口必须正确接 resume"这条强制约定补上，是面向正确性，不是修一个真能被打的漏洞。
  - **`finishSanyaoDamage(g, casterSeat, targetSeat)`**（`skills.js`）：`dealDamage(g, targetSeat, 1, casterSeat, ..., 'sanyao')`，`srcType` 按项目既有命名惯例（`kurou`/`fanjian`/`quhu`/`ganglie`/`xiaoguo`/`enyuan`/`qiangxi`/`huogong` 都用技能自己的拼音）传 `'sanyao'`，`sourceSeat` 传马谡自己（不管目标是不是他本人，这样司马懿反馈这类"看伤害来源"的钩子能正确识别）。若 `dealDamage` 自身被打断（濒死/`onDamaged` 钩子链），交由 `resumeAfterInterrupt` 接回——**这条自动生效，不需要额外代码**：设计阶段逐一读了 `dealDamage` 内部全部会产生打断的分支（`startDying`/`buquAsk`/`maybeStartZhengyi`/`maybeStartTianxiang`/`triggerZhimeng`/`consumePendingHookQueue` 触发的各个 `onDamaged` hook），确认它们无一例外都会把 `resume:{type:srcType,...}` 正确挂在各自开出的新 pending 上，`srcType='sanyao'` 天然会被这套既有机制原样传递到 `resumeAfterInterrupt`。
  - **`resumeAfterInterrupt` 新增两处**（`game.js`）：①`'sanyao'` 并入既有的 `'quhu'||'fanjian'` 分支（同一形状：发动者在自己的出牌阶段对可能是别人的目标造成伤害，没有额外尾巴要做，只需按"当前回合玩家是否还活着"正确收尾——伤害目标死的若不是回合玩家，回合不切换）；②新增 `'sanyaoDamage'` 分支，调用 `finishSanyaoDamage` 续接被 `onLoseEquip` 钩子打断的伤害结算（前述死代码分支的另一半）。
  - **马谡+制蛮的既有联动自动生效，未额外编码**：`dealDamage` 里 `triggerZhimeng(g, sourceSeat, seat, ...)` 是通用逻辑，只要 `sourceSeat` 传对（马谡自己）就会自动检查马谡是否想用【制蛮】防止这次散谣造成的伤害并改为夺牌——这正是官方玩法里"散谣和制蛮相辅相成"的组合，设计阶段查证确认了这一点，实现时不需要为这个组合写任何特殊代码。
  - **normalize 清理**：删除了旧的 `sanyaoChooseTarget`/`sanyao` 两个 pending 类型的防御性校验（这两个类型不会再被创建），`zhimengAsk` 等【制蛮】相关的校验原样保留未动。
  - **测试**（`run_sanyao_core_test.js`，45项，未落盘）：唯一候选自动结算、"全场"含自己（含旧版排除自己的对照断言）、平局候选集计算+非法/合法 targetSeat 校验、弃装备正常路径（无钩子打断）、弃装备触发 onLoseEquip 钩子的两种场景——④b 临时给 `GENERALS.masu` 挂一个测试专用钩子直接观察 `triggerHook` 确实以正确的 `seat`/`count` 被调用（不是"没报错就当作调用了"），④c 进一步模拟钩子真的打断（开新 pending）验证 `resume={type:'sanyaoDamage',...}` 被正确挂起、伤害延后到手动调用 `resumeAfterInterrupt` 续接后才真正结算——都用完后把 `GENERALS.masu.hooks` 恢复原状，不污染其它测试；`g.sanyaoUsed` 限一次；`costKey`/`targetSeat` 六种非法输入（越界手牌下标/空装备槽/判定区/null/不存在的座位，含"前面全部非法输入都不影响后面一次真正合法调用"的收尾断言）逐一验证服务端拒绝、不信任客户端；基本守卫（非当前回合/非出牌阶段/无 `sanyao` 能力）；制蛮相关函数/normalize 校验未受影响的回归确认；`findMaxHpSeats` 全项目仅一处调用点的排查（用正则时踩了一个小坑——第一版把文档注释里提到函数名的那一行也误算成调用点，改成先过滤掉整行注释再统计）；旧函数确认已删除。`git stash` 验证修复前整份测试直接在第一次调用 `sanyao(...)` 时报 `ReferenceError: sanyao is not defined`（旧代码里这个函数从未存在过），恢复后 45 项全部转绿。既有回归套件（faction/negative_hp/qiaomeng_lieren/respond_pick/shuangxiong_seat/tiaoxin_guard/wusheng_popup/wusheng_seat/xuanfeng_resume/xuanfeng）全部重跑零回归；上一次任务留下的 `run_sanyao_guard_test.js`（测试已删除的 `respondSanyao`/`respondSanyaoTarget`）因为测试对象已不存在，随这次整体重写一并从 scratchpad 移除。`?v=` 175→176。
- **马谡【散谣】第二步（UI 接入）：出牌阶段按钮+客户端本地状态机，功能全部完成（第1/2步均已完成，散谣是完整可玩的技能了）**。`sanyaoOptions(p)` 第一步已经在 `skills.js` 实现好了，这一步直接复用、没有重复实现——这次只碰 `render-controls.js`/`render.js`，没有再改任何服务端逻辑。
  - **状态机**（`render-controls.js`，紧邻 `guanshifuOptions` 之后）：`sanyaoMode`（bool）+`sanyaoCostKey`（已选弃牌 key）+`sanyaoTarget`（仅平局时需要，唯一候选时不需要玩家选、UI 自动带上）+`resetSanyao()`。完全照抄张郃【巧变】已经确立的"单人无需响应、客户端本地累积选择、一次性原子提交"规则，和第一步设计时的既定方案一致，没有中途调整。
  - **入口按钮**：紧跟在苦肉【苦肉】按钮之后，同一个 `noLocalMode` 互斥列表（新增 `!sanyaoMode` 项），触发条件 `hasCap(me,'sanyao') && !g.sanyaoUsed && sanyaoOptions(me).length>0`——和断粮"至少有一张符合条件的牌才渲染"同一原则，手牌+装备都为空时不显示一个进去也点不动的死胡同按钮。
  - **本地选择界面**：插在 `zhihengMode` 分支之后、`zhangbaMode` 分支之前（`else if(sanyaoMode){...}`），是这批"客户端多步本地流程"里第一个需要"手牌+装备混合单选"的——`zhihengMode`（纯手牌，任意张，靠 `renderHand` 的点击处理）、`duanliangMode`/`qixiMode`（纯手牌，单张，同样靠 `renderHand`）都覆盖不了装备牌，只有贯石斧的 `guanshi`（手牌+装备混合、恰好2张、toggle多选）形状最接近——**这次直接照抄贯石斧"候选项渲染成按钮列表、点击切换选中态"这套写法，改成单选**（选中新的自动替换旧的，不是 toggle 累积）。装备牌不在手牌区、没法靠点手牌选中，所以候选列表必须直接在 `render-controls.js` 里渲染成按钮，不能像 `duanliangMode` 那样依赖 `renderHand`。
  - **候选目标**：`findMaxHpSeats(g)` 在客户端本地直接调用（和服务端 `sanyao()` 内部调的是同一个函数，两边算出来的候选集合理论上永远一致，因为都是基于公开的 HP 数据），唯一候选时不渲染任何目标按钮、直接把这唯一座位当作 `readyTarget`；平局（>1个候选）才渲染目标选择按钮列表，含马谡自己（"全场"含自己这条第一步就确认过的规则，UI 侧同样不排除）。
  - **确认按钮**：`sanyaoCostKey!==null && readyTarget!==null` 才出现，点击目标座位名字（冻结成局部 `const`，不是读取可能已被后续操作改变的模块级变量，遵循 CLAUDE.md 规则14）后走 `confirmAndPlay(msg, ()=>sanyao(key,tgt))`——和 `zhihengMode`"确认制衡"同一写法，不额外手动调 `resetSanyao()`（`confirmAndPlay` 内部的 `resetSelectionState()` 已经处理，见下一条）。
  - **状态清理（三层防御，照抄 `zhihengMode` 的完整规格，不是只做一部分）**：①`resetSelectionState()`（`render.js`，`confirmAndPlay` 确认/取消都会调）的清单里加入 `resetSanyao()`；②`render()` 主循环新增单点兜底 `if(!(g.started && g.phase==='play' && g.turn===mySeat)) resetSanyao();`（判据和 `resetZhiheng` 那行逐字相同——散谣全程 `g.phase` 停在 `'play'`，没有独立服务端 pending 可判，只能靠"是否还在自己的出牌阶段"这个通用条件）；③"结束出牌"按钮的巨型 reset 链里补上 `resetSanyao()`。
  - **删除死代码**：`renderSanyaoChooseTarget`/`renderSanyao` 两个旧渲染函数（判断已作废的 `sanyaoChooseTarget`/`sanyao` 服务端 pending 类型、调用第一步就已删除的 `respondSanyaoTarget`/`respondSanyao`）连同 `renderControls` 里对它们的两行分派一并删除，全项目 grep 确认无残留死引用。
  - **测试**（`run_sanyao_ui_test.js`，33项，未落盘，复用武圣/双雄座位按钮测试同一套 vm 沙箱 harness，驱动真实 `render()`/`renderControls()`）：按钮出现条件四种（正常/非马谡/已用过/无可弃牌）；唯一候选完整流程（点入口→选牌→确认按钮直接出现不需要选目标→点击后正确调用 `sanyao`、伤害/弃牌均正确、`sanyaoUsed` 置真）；平局完整流程（选完牌正确渲染出全部候选目标按钮，含马谡自己；未选目标前无确认按钮；**明确选中数组里既不是第一个也不是马谡自己的那个候选**，验证伤害确实落在玩家选的座位而不是巧合对上的第一个）；取消/退出（点取消后手牌/血量/`sanyaoUsed` 均未变、且直接读取 `sanyaoMode`/`sanyaoCostKey`/`sanyaoTarget` 三个模块级变量确认真的清空、不只是看UI表现；外加"发起流程后 `g.turn` 变化，单点兜底自动清空不需要点取消"的独立场景）；弃装备场景（装备正确移出装备栏+进弃牌堆+伤害结算）；互斥回归（临时给马谡叠加 `zhiheng` 能力验证两个入口能同屏共存、但进入任一本地模式后另一个入口按钮消失，`noLocalMode` 机制双向验证）；死引用确认（`renderSanyao`/`renderSanyaoChooseTarget` 已删除、`sanyaoOptions` 未被重复定义）。`git stash` 验证修复前入口按钮压根不存在（点击时 `undefined.onclick` 直接抛异常），恢复后 33 项全部转绿。既有回归套件（faction/negative_hp/qiaomeng_lieren/respond_pick/shuangxiong_seat/tiaoxin_guard/wusheng_popup/wusheng_seat/xuanfeng_resume/xuanfeng）全部重跑零回归。**顺带修正了 `run_sanyao_core_test.js` 里一条被这次改动打破前提的旧断言**（CLAUDE.md 规则20"设计变更后要回头检查旧断言是否还成立"的又一次真实应用）：第一步写的"`findMaxHpSeats` 全项目只有1个调用点"这条断言，在第二步给客户端本地界面新增了对同一个函数的合法调用后自然变成2个——这是设计里明确要求的（本地渲染平局候选需要和服务端算的是同一份结果），不是回归，已把期望值从1改成2、并在注释里说明原因，不是放宽断言绕过问题。`?v=` 176→177。
  - **视觉层面的"不遮挡"验证方式说明**：这次没有另外跑一遍真实浏览器 Playwright 截图——项目里同一类"出牌阶段本地状态机按钮"的既有先例（苦肉/巧变/贯石斧/双雄/武圣）全部只用 vm 沙箱驱动真实渲染函数验证，Playwright 真实浏览器截图在这个项目里是留给真正的 CSS/布局/视觉设计类任务（座位卡、中央出牌区等）用的，不是这一类纯按钮逻辑测试的标配。"不遮挡"这个需求的真正保证机制是互斥（`noLocalMode`），已经在测试6里用双向断言验证过：两个入口按钮能同屏共存、但一旦进入任一本地模式，同一个开关会让所有其它入口一并消失——这正是防止两套按钮同时渲染进而产生视觉冲突的机制本身，比截图更能证明"为什么不会遮挡"。
  - **左慈化身借用范围（`HUASHEN_SKILL_TABLE.masu`）暂不加回散谣**：技能功能本身现在完整可用了，但这次任务范围只是"给马谡本体接 UI"，要不要让左慈也能借到这个技能是一个独立的产品决策，留给以后单独发起。

- **猛进触发旋风时正确续接青龙偃月刀"再来一杀"（v2，已解决上方"凌统旋风"条目里那条 v1 已知限制）**：v1 里麒麟弓/猛进触发的旋风统一用 `resume={type:'sha'}` 续接，只接回 `finishSingleShaTarget` 这段尾巴——庞德同时装青龙偃月刀、用猛进拆凌统装备触发旋风时，续接后会跳过猛进之后本该继续检查的青龙"再来一杀"（不是软锁，是丢一次合法效果）。
  - **排查发现的关键简化**：项目里已经有一套现成的"杀被闪抵消后效果调度系统"（`maybeStartShaOffsetEffects`/`startShaOffsetEffect`/`continueShaOffsetEffects`，猛进/青龙/贯石斧共用），`canStartQinglong(g,fromSeat)`/`canStartGuanshifu(g,fromSeat)` 这两个判断**只依赖攻击者（`from`）自己当前的 caps+手牌+装备，和被旋风拆走装备的目标（`to`）完全无关**——猛进拆的是目标的装备，不动攻击者自己的任何状态，所以这两个判断在旋风打断前后算出来的结果恒定一致。**这意味着 resume 不需要保存一份"打断前算好的候选列表"快照**（这是排查阶段一开始设想过的方案，后来确认没必要）：`continueShaOffsetEffects` 本身就会重新过滤候选，续接时直接传固定的 `['qinglong','guanshifu']`（和两个注入点未被打断时的既有写法逐字一致，`mengjin` 已处理完不会再出现在候选里）即可，多余的候选会被内部过滤掉，效果和"精确记住了还剩哪些"完全一样。
  - **resume 字段**：`{type:'shaOffset', from, to, sourceCard}`——比最初设想的更简单，去掉了 `remainingAvailable`。
  - **`resumeAfterInterrupt` 新增分支**：`else if(resume.type==='shaOffset'){ continueShaOffsetEffects(g, resume.from, resume.to, resume.sourceCard, ['qinglong','guanshifu']); }`——一行调用，`continueShaOffsetEffects` 内部候选过滤为空时天然走到 `finishSingleShaTarget`，自动向下兼容原来 `{type:'sha'}` 的收尾行为，不需要在这里分别判断"有没有青龙"。
  - **两个注入点（`startShaOffsetEffect` 的 mengjin 唯一选项分支、`mengjinPick`）从 attach `resume={type:'sha'}` 改成无条件 attach `resume={type:'shaOffset',from,to,sourceCard}`**——**不在注入点判断"有没有青龙"**，交给 `continueShaOffsetEffects` 统一判断，这样两处永远不需要各自维护一份判断条件、不会有"忘记同步判断条件"这类日后走样的风险。
  - **风险评估结论**：麒麟弓（走独立注入点，不经过 `maybeStartShaOffsetEffects`）、独立失去装备三入口（顺手/拆桥/借刀，同样不经过这套系统）都确认不受影响；趫猛/烈刃触发于杀**命中**分支，猛进/青龙/贯石斧触发于杀被**闪抵消**分支——同一张杀要么命中要么被挡，两条分支互斥，趫猛/烈刃结构上不可能和这套 offset 系统同时发生，不需要处理（对应"凌统旋风"条目里那条趫猛/烈刃的 v1 已知限制保持不变，是独立的、结构上不相关的另一半，不是本次遗漏）。
  - **"青龙再杀 vs 方天画戟队列推进谁先谁后"这个问题不需要新的判断逻辑，代码结构已经决定了答案**：`continueShaOffsetEffects` 只有在候选列表过滤为空（没有更多 mengjin/青龙/贯石斧可做）时才调用 `finishSingleShaTarget`，而 `finishSingleShaTarget` 是 `g.fangtianQueue` 推进到下一个目标的**唯一入口**——也就是说"当前目标的青龙再杀有没有全部结算完"和"方天画戟要不要换目标"从来不是两个平行判断，而是严格的先后关系：青龙杀完（可能触发若干轮新的 `resolveShaUse`→`maybeStartShaOffsetEffects` 递归）、链条收敛到没有更多可做的事，才会摸到 `finishSingleShaTarget`，方天队列才会前进。v2 只是让"被旋风打断"这条支路重新汇入这条既有链路，没有引入新的排序规则。**这条结构性认知供以后遇到类似"多个连续效果如何排序"的问题时直接参照：先找这条链路里有没有一个类似 `finishSingleShaTarget` 的唯一收敛点，多个效果的先后关系往往已经被这个收敛点的调用时机天然决定了，不需要另外设计一套仲裁逻辑。**
  - **测试**（`run_mengjin_shaoffset_test.js`，19项，未落盘）：核心1唯一弃项自动弃置路径（庞德青龙+猛进拆凌统唯一装备触发旋风，两种旋风收尾——弃满2张/不发动——各验证一次续接正确触发青龙"再来一杀"）；核心2 mengjinPick 多选项路径同样验证续接正确；核心3 贯石斧对照（`continueShaOffsetEffects` 对 qinglong/guanshifu 通用）；回归1麒麟弓确认走独立注入点、resume 仍是 `{type:'sha'}`；回归2方天画戟嵌套（明确断言续接后方天队列 `idx` 仍未推进、青龙这轮结束才推进到下一目标，验证上面那条"结构性先后关系"结论）；回归3独立失去装备三入口（顺手/拆桥/借刀）确认不受影响。**测试构造过程中的一个真实教训**：庞德自己若同时装了青龙偃月刀，在杀被闪抵消的**那一刻**（猛进触发之前）`mengjin`/`qinglong` 两个效果就已经同时可用，会先开 `shaOffsetChoice` 让庞德选先做哪个——不能想当然假设"闪抵消后直接进猛进"，必须先 `respondShaOffsetChoice('mengjin')`（或等价的 `respondMengjin()`）显式选择，这是这套调度系统的既有行为，不是这次改动引入的。另外旋风（凌统持有）不能选自己作为目标，2人局测试若只有庞德+凌统会被迫拆庞德自己的装备/手牌、污染"青龙候选还在不在"这条断言，必须加第三人当旋风目标，让庞德自己的状态在整个流程中保持不变。`git stash` 验证修复前 9 项断言精确变红（`resume.type` 仍是 `'sha'`、续接后 `phase==='play'`+`pending===null`，青龙/贯石斧被跳过），恢复后全部转绿。既有回归套件全部重跑零回归，其中两处因这次改动**合法**触发前提变化而更新了旧断言（不是放宽绕过，是按规则20"设计变更后要回头检查旧断言"更新）：`run_qiaomeng_lieren_test.js`"猛进回归"那条 `resume.type` 期望值从 `'sha'` 改成 `'shaOffset'`；`run_xuanfeng_resume_test.js` 同类两条断言（auto-single/mengjinPick）同样更新，且第9条"已知限制"整段重写为"v2 已修复"验证（保留青龙不发动的轻量收尾，完整场景组合见新的独立测试文件，不在这个文件里重复展开）。全部回归 305 项零失败。`?v=` 177→178。

- **夏侯渊【神速】断点1修复：`respondShensuSha` 不再被自己的收尾代码冲掉"视为杀"的响应阶段（真实 dump 排查发现，见排查那次任务记录；断点2——`shensuUsed` 共享总锁导致"夏侯二刀"打不出来——是独立的另一处，单独留在「四、已知的待优化点」，本次不修）**。
  - **根因不是简单的"读错字段"，比字面描述更深一层**：原代码在调用 `resolveShaUse(...)` 之后才读 `g.pending.remaining`——但 `resolveShaUse`（经 `resolveShaUseNoLiuli`→`continueShaAfterTieqi`）已经把 `g.pending` 整个替换成了杀的标准响应结构（`{from,to,noShan,shaColor}`，等待目标出闪/受伤），读到的 `remaining` 是 `undefined`。**排查中发现一个关键点**：即使把 `remaining` 的读取时机挪到调用前（字面意义上的"先快照"），单发神速1场景下算出来的值仍然是 `0`（`(1||1)-1=0`，和调用后误读 `undefined||1-1=0` 数值上恰好相同）——真正的问题不是"remaining 的数值算错了"，而是原代码的 `else`（`remaining<=0`）分支**无论 remaining 算出多少，都会在调用 `resolveShaUse` 之后立刻执行 `g.pending=null;g.phase='play'`**，这一步本身就会把刚刚建立起来的响应阶段冲掉——单纯把 `remaining` 挪到调用前读，数值虽然会变得"正确"，但这个"立刻收尾"的动作依然会原样执行，目标依然从未获得响应机会。**真正需要做的是把"这次杀处理完之后该怎么收尾"这件事，从"调用完 resolveShaUse 就立刻做"改成"推迟到杀真正彻底结算完毕（不管中途有没有被濒死/争议/天香/制蛮/毅重/仁王盾/八卦阵等任意效果打断）那一刻才做"**。
  - **修复：新增 `g.shensuResume` 全局字段（不挂在 `g.pending` 上），和 `g.fangtianQueue`/`g.luanwuResume` 同一设计**（CLAUDE.md「方天画戟嵌套天然正确」条早就确立的模式：队列/续接信息放在 `g` 上、不进被打断的局部栈，不受 `g.pending` 被谁替换的影响）。`respondShensuSha` 调用 `resolveShaUse` 之前，把 `remaining`/`fromShensu` 读出来存进 `g.shensuResume = {seat:mySeat, remaining, fromShensu}`（`seat` 必须显式快照——`resumeAfterInterrupt`/后续收尾可能发生在**完全不同的客户端**的 tx 里，那时 `mySeat` 是响应者自己的座位号，不是夏侯渊的，和散谣 `sanyaoDamage` resume 快照 `casterSeat` 同一个理由），并显式 `g.pending=null`（交给 `resolveShaUse` 自己决定这一刻该是什么 pending，和借刀杀人 `respondJiedao` 出杀分支同一写法）。
  - **`finishSingleShaTarget`（game.js）新增 `g.shensuResume` 检查，放在 `checkWin` 之后、`fangtianQueue`/`luanwuResume` 之前**：`if(g.shensuResume){ finishShensuSha(g); return; }`。选择挂在 `finishSingleShaTarget` 而不是自己发明一套新的 dispatch/resume 检查机制——**因为 `finishSingleShaTarget` 本来就是"一张杀（不管命中/被闪抵消/被毅重仁王盾无效/被八卦阵判定/等等任意路径）彻底结算完毕"的唯一收敛点**，`fangtianQueue`/`luanwuResume` 已经在复用同一个收敛点，这次是同一模式的第三次应用，不是新发明。`finishShensuSha`（新函数，`skills.js`）读取并清空 `g.shensuResume`，按 `remaining>0`/`shensuSkipJudgingAndDraw`/`shensuSkipPlay` 三种组合决定最终 `g.phase`，逻辑和原来 `else` 分支里的阶段判断完全一致，只是**执行时机从"resolveShaUse 调用完立刻执行"改成"杀真正结算完毕才执行"**。
  - **`remaining>0`（神速1+2 组合，需要连续用两张视为杀）这个分支当时仍不可达**：因为断点2（`shensuUsed` 共享总锁）还没修，`g.shensuShaRemaining` 永远不会大于1，两次神速无法在同一回合都发动。当时为了不留一段已知会走错的死代码，`finishShensuSha` 里 `remaining>0` 分支按相同思路实现了（重新挂起 `shensuSha` pending 问下一个目标），vm 沙箱验证过状态转换本身不抛异常、字段设置正确，但当时不代表真实"神速1+2"链路已经跑通——**断点2 已在下一个任务修复，这条路径现在真实可达并已有场景测试覆盖，见下方新条目**。
  - **`normalize` 新增 `g.shensuResume` 防御**：`===undefined` 回退 `null`；脏数据（`seat` 非数字/座位不存在/`remaining` 非数字）整体清空——照抄 `g.luanwuResume` 已有的防御写法（`typeof sourceSeat!=='number'||!Array.isArray(remainingSeats)` 那种"关键字段类型不对就整体清空"的模式），不是照抄 `g.fangtianQueue`（那个字段在 normalize 里其实没有防御，是既有的一个缺口，这次不趁机补，按"一次只改一件事"原则）。
  - **测试**（`run_shensu_sha_test.js`，30项，未落盘）：核心场景1（神速1，目标无闪只能受伤）——完整验证 `respondShensuSha` 后正确进入 `phase==='respond'` 的标准杀响应结构、`g.shensuResume` 正确挂起、目标随后**真实受到伤害**（hp从4变3，不是"看起来受伤但其实流程被冲掉"）、`g.shensuResume`/`shensuSkipJudgingAndDraw` 结算完毕后正确清空、最终正确落在 `play` 阶段；场景2（目标出闪抵消）——验证目标手牌真的减少（真的打出了那张闪，不是流程空转导致"看起来抵消了但其实没真的响应过"）、hp 不变、收尾同样正确；场景3判定/摸牌跳过效果的回归确认（断点1和这部分独立，日志文案确认跳过逻辑本身没被误伤）；场景4验证神速2（弃装备+跳过出牌）单独发动同样正确结算，落在 `discard` 阶段；场景5 normalize 防御（缺失字段/脏数据/合法数据三种输入）；场景6回归确认方天画戟队列推进、濒死流程完全不受 `g.shensuResume` 检查插入的影响（`g.shensuResume` 为 `null` 时新增分支不介入，原有逻辑原样执行）。`git stash` 验证修复前**精确复现报告的"毫无效果"**：目标 hp 4→4 未变化、`phase==='play'`/`pending===null`（响应阶段被冲掉）、场景2里目标手牌未减少（连"是否真的响应过"都验证不出来）——10项断言精确变红，恢复后全部转绿。既有回归套件（faction/mengjin_shaoffset/negative_hp/qiaomeng_lieren/respond_pick/sanyao_core/sanyao_ui/shuangxiong_seat/tiaoxin_guard/wusheng_popup/wusheng_seat/xuanfeng_resume/xuanfeng）全部重跑零回归，共 335 项零失败。`?v=` 178→179。
  - **同类问题的通用提醒（供以后新增"视为使用某张牌"这类委托给通用结算函数的技能时参照）**：调用 `resolveShaUse`/`playCard`/`dealDamage` 这类**会自己接管并改写 `g.pending`** 的通用结算函数之前，如果调用方后续还需要用到"调用前 `g.pending` 上的某个字段"，必须先把这个字段读出来存成局部变量（或挪到 `g` 上的独立字段，视是否需要跨 tx 存活而定），不能假设调用之后 `g.pending` 还是原来那个对象——这是本项目反复出现过的一类坑（凌统旋风、散谣弃装备、这次的神速都属于同一类），且**光是"把读取时机提前"未必够**：如果调用方紧接着还有"立刻收尾"这类动作，需要进一步确认这个收尾时机是不是真的对——通用结算函数可能会异步挂起等待其他玩家响应（如本例），调用方原地立刻收尾就会把这个待响应的状态冲掉，正确做法通常是把收尾动作也推迟到通用结算函数真正的"完成"收敛点（如 `finishSingleShaTarget`）才执行，而不是简单挪一下读取时机就当作修完了。

- **夏侯渊【神速】断点2修复：`g.shensuUsed` 从神速1/神速2 共享的一把总锁拆成两个独立标志位，"夏侯二刀"真正打得出来（已解决，移出待优化点）**。
  - **修复**：`g.shensuUsed1`（神速1专属）/`g.shensuUsed2`（神速2专属），官方规则语义是"两者各自限一次，一回合最多发动两次，不是可以重复发动同一个"——两个标志位各自独立限一次，天然满足这条规则，不需要额外的"合计上限"判断。改动4处：`continueShensu1Check`/`triggerShensu1`（`skills.js`）的守卫和置位从 `g.shensuUsed` 改成 `g.shensuUsed1`；`triggerShensu2`（`skills.js`）改成 `g.shensuUsed2`；`shensuChoose2` 的开启条件（`game.js` 的 `finishDrawPhase` 内，摸牌阶段结束后的检查点，不是 `advancePastPlay`）改成检查 `g.shensuUsed2`；`startTurn` 的回合重置和 `normalize` 的防御性初始化都从一行 `shensuUsed` 拆成两行分别处理。全项目 grep 确认（排除注释）不再有任何地方读写无后缀的 `g.shensuUsed`。
  - **和断点1（`g.shensuResume`）的交互，逐一确认过没有遗漏**：`g.shensuResume`/`finishShensuSha` 内部完全不引用 `shensuUsed`（这两套机制关注的是"这次杀什么时候真正结算完"和"这个回合还能不能再发动"，是正交的两件事），所以断点1的既有30项测试（`run_shensu_sha_test.js`）在这次改动后原样全部通过，不需要跟着改。`finishShensuSha` 里 `remaining>0` 那个"神速1+2需要连续两杀"的分支，当初实现时就已经写对了（重新挂起 `shensuSha` pending 问下一个目标），断点2修复后这条路径终于变得真实可达，这次任务补上了它的真实场景测试（不是需要重新写这段逻辑）。
  - **测试**（`run_shensu_twice_test.js`，25项，未落盘）：核心场景——神速1发动+第一刀杀结算完毕后，模拟摸牌阶段结束（真实调用 `finishDrawPhase`，不是绕过它直接摆状态），验证 `shensuChoose2` 正确弹出（不再被共享总锁挡住）；发动神速2，验证第二刀杀的 `remaining` 正确计算为1（不是被旧共享逻辑污染成2或被拒绝）、武器正确弃置、第二刀杀同样能走完整响应流程真实造成伤害、两刀都结算完毕后正确落在弃牌阶段（神速2的收尾效果）；回归场景——只发动神速1时它自己仍然只能发动一次（不会因为拆分标志位意外变成可重复发动）、只发动神速2（不先发动神速1）时能独立正常触发且 `remaining` 正确为1（不是 `shensuShaRemaining+1` 累加出2）；边界场景——两个都发动过之后，两个触发函数和 `shensuChoose2` 开启条件都正确不再允许第三次；normalize/startTurn 对两个新字段的防御与重置；全项目 grep 排除注释后确认无残留的旧字段读写。`git stash` 验证修复前精确复现"夏侯二刀打不出来"：`shensuChoose2` 不会弹出（`phase` 停留在 `play`，`pending` 为 `null`）、`triggerShensu2()` 被自己的守卫拒绝（状态原封不动），后续断言因为 `g.pending` 一直是 `null` 而在 `respondShensuSha` 内部自然崩溃——这个崩溃本身就是"修复前流程走不下去"的直接证据，不是测试脚本的问题。恢复修复后全部转绿。既有回归套件（含断点1的30项）全部重跑零回归，共 360 项零失败。`?v=` 179→180。
  - **"神速的杀无色无属性、可破仁王盾但不能破藤甲"这条规则细节——补记，此前只在排查那次任务的聊天回复里报告过，从未真正写进过 CLAUDE.md（这次一并补上，不是"确认还在"，而是"确认之前确实没有被持久化，这次让记录完整"）**：`triggerShensu1`/`triggerShensu2` 构造的杀写死为 `{name:'杀',suit:'♠',rank:2}`，是一张普通**黑色**杀，会正常触发仁王盾/毅重的黑杀无效判定；项目里没有"无色"这个通用杀属性概念（唯一出现"无色"的地方是丈八蛇矛两张异色牌合成杀的三态判定，是局部概念，不是可复用的杀属性系统）；项目也**完全没有实现"藤甲"这件装备**。这条官方细则本项目从设计上就没有涉及，现状如此，不在这次断点2的范围内处理。

- **张角【雷击/鬼道】排查（真实dump走完整链路，报告结论，不含修复）：确认不是同一根因，是三处独立缺陷，外加一处范围缺口**。①雷击自己的入口——`respondShan`（game.js:4883-4885）调用 `maybeStartLeiji` 后不检查返回值，紧接着的 `g.pending=null;finishSingleShaTarget(g);` 会在同一次 tx 里把刚挂起的 `leijiChoose` 直接冲掉，玩家连"是否发动雷击"的询问都看不到——和凌统旋风/神速那次是同一类"hook 挂起 pending 却被调用方无条件覆盖"模式。②鬼道替换成功后的收尾——`finishGuidu`（game.js:1653-1692）对 `bagua`/`delayJudge` 两种 `resume.kind` 处理不完整（`bagua` 只调 `finishBaguaColor` 不像 `finishGuicai` 那样按 `resume.type` 重新开出 `respond`/`aoeResp` 或推进 `finishSingleShaTarget`/`aoeAdvance`；`delayJudge` 漏调 `continueDelayResolution`），`beigeJudge`（悲歌）干脆没有对应分支，落到通用兜底把整个悲歌效果静默吞掉——真实 dump 确认前两者会卡死在 `phase='guiduAsk',pending=null`（没有任何按钮可点），后者不卡死但效果凭空消失。③`askNextGuidu` 的"不发动/无人可发动"分支直接 `g.pending=null;g.phase='play';`，从不像 `respondGuicai` 找不到下一个鬼才候选人时那样 `finishGuicai(g, g.pending.judgeCard)` 用原判定牌收尾——**这条影响面最大**：不需要真的换牌，张角只要被问到"是否发动鬼道"选择"不发动"，就会把这次判定（八卦阵/延时锦囊/铁骑/洛神/双雄/刚烈/悲歌/雷击自己，只要走 `maybeGuidu` 统一入口的判定类型全部覆盖）整个静默吞掉——牌从判定区消失、效果不执行、不报错不卡死，表现为"看起来正常但效果凭空消失"。④范围缺口——`aoeRespond`（南蛮入侵/万箭齐发的打闪响应）完全没有接入雷击检查，只有 `respondShan` 一条路径。**这次排查过程中意外证实了 `doLeijiJudge` 内部"先查鬼道再查鬼才"的返回值检查是对的**（task 一开始怀疑的重点，反而没问题），也证实了雷击链路本身（`triggerLeiji`→`doLeijiJudge`→伤害结算）一旦绕开①的入口就是好的。
- **张角【雷击/鬼道】修复 1/4：`askNextGuidu` 不再吞掉原判定（上条③，影响面最大的一处，优先修复）**。`game.js` 的 `askNextGuidu` 最后的 `else`（无人发动鬼道）分支从 `g.pending=null;g.phase='play';` 改成 `g.pending=null; return finishGuidu(g, pending.judgedSeat, pending.judgeCard, pending.resume);`——用从未被替换过的原判定牌 `pending.judgeCard` 接回原判定的收尾流程，和"有人真的发动了鬼道并换了牌"走的是**同一个** `finishGuidu` 收尾函数，只是传入的牌不同（换了牌传替换牌，没换牌传原判定牌），完全对齐 `respondGuicai` 找不到下一个鬼才候选人时 `finishGuicai(g, g.pending.judgeCard)` 的既有模式。**范围严格限定在这一个 `else` 分支，`askNextGuidu` 里"找到下一个候选人"和"有 `currentReplaceCard`（有人真的换了牌）"两个分支一字未动**。**这次修复独立于上条②（`finishGuidu` 自己的分派表不完整）——两处是完全独立的代码，`askNextGuidu` 调用 `finishGuidu` 是对的调用方式，但 `finishGuidu` 自己对 `bagua`/`delayJudge`/`beigeJudge` 这三种 `resume.kind` 的处理仍然不完整，属于下一个独立任务（2/4）的范围，本次不碰**（已和用户确认按"先只提交 askNextGuidu 这一处"的方案推进，不做范围合并）。**效果验证**：`leijiJudge` 是 `finishGuidu` 里唯一自完备的 `resume.kind`，绕开①的入口手动构造 `leijiChoose` 状态后，"张角不发动鬼道"这条路径现在端到端完全正确（判定黑桃→2点伤害→正确收尾回 `play`）——这是本次修复能做到的最强验证，证明 `askNextGuidu` 这一处的改法本身没有问题。`bagua`/`delayJudge`/`beigeJudge` 三类判定这次修复后**已经不再是"直接清空、连日志都没有"**（`finishGuidu` 确实被调用、`finishBaguaColor`/`finishDelayCard`/判定结果日志都正确跑了），但仍会撞上②描述的下游缺陷（卡在 `guiduAsk`/悲歌效果被吞），要等 2/4 才能端到端修好。**测试**（`run_askguidu_fallback_test.js`，18项，未落盘）：核心场景1/2/3（八卦阵/乐不思蜀/悲歌，张角不发动）断言"`askNextGuidu` 现在确实调用了 `finishGuidu`"（日志/判定区变化等可验证的部分）+ 显式标注"[已知缺陷,留给2/4]"的断言确认下游卡点仍在复现（不是这次修复的回归，是尚未触及的既有问题）；场景4（雷击自己，`leijiJudge`）断言端到端完全正确；场景5（无张角在场）回归确认完全不受影响；场景6（真的换牌，`triggerGuidu`→`askNextGuidu` 的 `currentReplaceCard` 分支，这次完全没碰）确认保持修改前就已存在的状态，不是这次引入的回归。`git stash` 验证：5 项断言精确变红（场景1/2 的"finishGuidu 被调用"断言、场景4 的伤害断言），其余 13 项修复前后一致（含场景3——修复前"直接清空"和修复后"卡在 `finishGuidu` 的 `beigeJudge` 兜底"恰好都落在 `hp=2,phase=play`，是巧合不是本次要验证的点，已在注释里说明）。既有回归套件（16 个文件，含 faction/mengjin_shaoffset/negative_hp/qiaomeng_lieren/respond_pick/sanyao_core/sanyao_ui/shensu_sha/shensu_twice/shuangxiong_seat/tiaoxin_guard/wusheng_popup/wusheng_seat/xuanfeng_resume/xuanfeng）全部重跑零回归。`?v=` 180→181。**后续排期（2/4~4/4）**：`finishGuidu` 补全 `bagua`/`delayJudge`/`beigeJudge` 三个分派分支（2/4）、雷击自己的入口修复即上条①（3/4，respondShan 检查 `maybeStartLeiji` 返回值）、`aoeRespond` 补接雷击检查（4/4，上条④的范围缺口）。
- **张角【雷击/鬼道】修复 2/4：`finishGuidu` 补全 `bagua`/`delayJudge`/`beigeJudge` 三个分派分支，对照姐妹函数 `finishGuicai`（1776行起）逐字补到同等完整深度**。`bagua` 分支：`finishBaguaColor` 判红黑之后，按 `resume.type`（`'sha'`/`'aoe'`）接回被打断的流程——红则视为出闪，推进 `maybeStartShaOffsetEffects`/`finishSingleShaTarget`（sha）或 `aoeAdvance`（aoe）；黑则原判定失败，重新开出 `respond`/`aoeResp` pending 问真正的杀/闪（含 `sourceCard`/`jiuBonus`/`need` 等字段透传）。`delayJudge` 分支：`finishDelayCard` 只处理这一张判定牌本身，还要跟一句 `continueDelayResolution(g, resume.seat)` 才能推进判定区剩余的牌/摸牌阶段，并且要处理 `finishDelayCard` 自己返回 `'pending'`（嵌套濒死/遗计）的情况——若新挂起是濒死或遗计，补上 `resume.seat`（`dealDamage`/`startDying` 不知道 seat 是谁）；若新挂起是鬼才/鬼道自己的改判，`resume` 已自带完整信息不能覆盖。`beigeJudge` 分支：此前完全没有，加上后调用 `processBeigeJudgeResult(g, replaceCard, resume.sourceSeat, resume.damagedSeat, resume.damageSource)`，字段名和 `doBeigeJudge` 里 `resume:{kind:'beigeJudge',sourceSeat,damagedSeat,damageSource}` 逐一核对无需转换。**三段都是从 `finishGuicai` 原样搬过来的既有逻辑，不是重新设计**——两个函数处理的是同一件事（判定牌亮出后被人用一张牌替换掉，该怎么接回原判定所在的流程），只是触发技能不同（鬼才 vs 鬼道），收尾逻辑理应完全一致。**刻意排除的一处**：`finishGuicai` 在 `bagua` 兜底之前还有一段独立的 `luoyiAsk`（许褚裸衣）防御性检查（校验 `g.pending.type==='luoyiAsk'` 时 `seat` 是否等于当前回合玩家且存活，不是则清空回退到 `'draw'`），这次没有一并复制进 `finishGuidu`——通读后判断这段检查和 `resume.kind` 分派逻辑本身无关，是针对鬼才历史上某次具体交互踩过的坑打的补丁，不确定其精确适用条件，贸然复制有引入未预期行为的风险，留作后续如果真的在鬼道场景发现同类问题再单独排查。
  - **铁骑/洛神/双雄/刚烈四个分支——真实 dump 验证，不是只信代码比对**：这四个分支在 `finishGuidu` 里的调用方式（函数名+参数顺序）本来就和 `finishGuicai` 逐字一致，排查阶段判断"结构上应该没问题"，这次用真实场景各自验证了一遍（张角在场、选择"不发动鬼道"），全部确认端到端正确，未发现问题，不需要修改：双雄（颜良文丑）判红后 `shuangxiongColor` 正确设为 `'red'`，正确收尾回 `play`；铁骑（马超）判红后正确进入 `respond` 阶段且 `noShan=true`（不可被闪抵消）；洛神（甄姬）判红失败后正确进入摸牌阶段、手牌不获得判定牌；刚烈（夏侯惇）判非红桃后正确触发伤害来源的弃牌/受伤询问（手牌不足2张时强制受伤）。这四个分支本来就是各自的 `finish*Judge` 函数自完备（`finishGuicai` 对它们也只做一次调用、不跟任何额外收尾），和 `bagua`/`delayJudge` 需要额外接回流程/`beigeJudge` 需要专属分支是不同的情况，这次真实验证只是坐实了排查阶段的判断，没有新发现。
  - **测试**（`run_finishguidu_dispatch_test.js`，37项，未落盘）：场景1a-1d（八卦阵，sha/aoe 两种 `resume.type` × 张角不发动/真的换牌两条路径，共4组）覆盖判红/判黑的完整分支；场景2a-2b（延时锦囊，乐不思蜀/兵粮寸断，两条路径）验证 `continueDelayResolution` 正确调用、`skipDraw` 等标志正确消费；场景3a-3b（悲歌，红桃回体力/梅花弃两张牌，两条路径）验证效果真实生效（不是日志有文字就算数，断言了 hp/手牌数量的真实变化）；场景4（铁骑/洛神/双雄/刚烈四个真实 dump）；场景5（雷击自己的 `leijiJudge`，1/4 已验证的路径）确认不受这次改动影响；场景6（无张角在场）回归。`git stash` 验证：16项精确变红（场景1a-3b 的核心断言），场景4/5/6（tieqi/luoshen/shuangxiong/ganglie/leijiJudge/无张角）在修复前后保持一致（证明这几个分支确实没被这次改动影响，之前就是对的）。
  - **回填 1/4 遗留的测试文件**：`run_askguidu_fallback_test.js` 里当初标注"[已知缺陷,留给2/4]"的6处断言（场景1/2/3/6 各自的下游卡点断言），这次2/4修完后全部改成正的回归防护断言（不再是"确认问题还在复现"，而是"确认问题已经修好、以后不会退回去"）——这是真实的状态迁移，不是随手改字符串：改动前用 `git stash` 复现过这些断言在旧代码上确实会通不过（1/4 提交时验证过），这次改完后重新用当前（2/4修复后）代码验证全部转绿，逻辑闭环完整。全部既有回归套件（17 个文件，1/4 的18项 + 2/4 新增的37项 + faction/mengjin_shaoffset/negative_hp/qiaomeng_lieren/respond_pick/sanyao_core/sanyao_ui/shensu_sha/shensu_twice/shuangxiong_seat/tiaoxin_guard/wusheng_popup/wusheng_seat/xuanfeng_resume/xuanfeng）全部重跑零回归。`?v=` 184→185。**后续排期（3/4~4/4）**：雷击自己的入口修复（3/4，`respondShan` 检查 `maybeStartLeiji` 返回值）、`aoeRespond` 补接雷击检查（4/4）。
- **张角【雷击/鬼道】修复 3/4：`respondShan` 检查 `maybeStartLeiji` 返回值——雷击自己的入口断点，四类独立缺陷里唯一直接影响张角自己技能能否触发的一处**。`respondShan`（game.js:5017-5033，出闪分支）原来调用 `maybeStartLeiji(g, mySeat, card)` 后完全不看返回值——`maybeStartLeiji` 内部一旦条件满足会把 `g.pending` 整个替换成 `{type:'leijiChoose',sourceSeat,availableTargets,shanCard}` 结构，但调用方毫不知情，继续往下执行 `if(played<needed){...}`（用的是新 pending 上不存在的 `shanCount`/`needed` 判断，通常不成立）和 `maybeStartShaOffsetEffects(g, g.pending.from, mySeat, ...)`（`g.pending.from` 此刻已经是 `undefined`，判断恒 `false`），最终落到函数尾部 `g.pending=null;finishSingleShaTarget(g);`——在**同一次 tx** 里把刚挂起的 `leijiChoose` 原地冲掉。真实 dump（排查报告场景A）证实：`respondShan(true)` 后 `phase=play,pending=null`，玩家连"是否发动雷击"的询问都从未出现过，这是雷击"完全无法触发"的唯一根因，和1/4修的 `askNextGuidu`、2/4修的 `finishGuidu` 是三个完全独立、互不重叠的缺陷（不同函数、不同触发条件、真实 dump 分别单独复现）。**修复**：`if(hasCap(me,'leiji') && card.name==='闪'){ if(maybeStartLeiji(g, mySeat, card)) return g; }`——一旦挂起立即 `return g`，不再执行后续任何以"`g.pending` 还是原来那个杀响应结构"为前提的判断。**比凌统旋风当初的修法更简单**：`maybeStartLeiji` 本身就有明确的布尔返回值（`true`=已挂起/`false`=未挂起，无候选目标时返回 `false`），不需要像凌统旋风那样引入 `pendingBefore` 快照比较——这是因为 `maybeStartLeiji` 从设计上就是"调用后立刻能确定性地知道有没有挂起"，不像 `onLoseEquip` 那样是钩子分发（触发者不知道具体哪个钩子会不会挂起 pending，才需要快照兜底）。**"使用或打出【闪】"两种场景的覆盖情况（任务明确要求确认）**：项目里【闪】从不是主动可出的 `CARD_PLAYS` 项（CLAUDE.md 规则15 早有记录），只能被动响应，所以官方文本里的"使用"这个分类在这个项目里根本不存在——真正存在的只有"打出"，而"打出"又分两条彼此独立的代码路径：`respondShan`（单体杀的响应，本次修复覆盖）和 `aoeRespond`（南蛮入侵/万箭齐发的群体响应，真实 dump 确认这次修复前后都完全没有 `hasCap(me,'leiji')` 检查，一个字都没碰）——**这次3/4只覆盖了 respondShan 这一条路径，aoeRespond 仍是排查报告里记录的④范围缺口，留给已排期的4/4**，不是本次修复的疏漏。**测试**（`run_leiji_entry_test.js`，17项，未落盘）：核心场景1（张角打出真闪，断言 `respondShan` 后 `pending.type==='leijiChoose'`，不是被冲掉的 `null`）；场景2（端到端：发动雷击→选目标→判定黑桃→目标正确受到2点雷电伤害→正确收尾回 `play`，复用1/4已验证过的下游链路，这次验证的是从入口到结算完整跑通）；场景3a/3b（选择不发动/判定非黑桃，两种"雷击最终没有造成伤害"的路径均正确收尾回 `play`，不卡死）；场景4（非张角玩家打闪，回归确认原有 `maybeStartShaOffsetEffects` 等逻辑不受影响）；场景5a（响应"别人使用的杀"——攻击者不是张角本人，雷击照样触发，证明 `respondShan` 的 leiji 检查只看响应者 `me` 自己、和攻击者是谁无关）；场景5b（`aoeRespond` 现状确认，非本次回归，明确记录留给4/4）。`git stash` 验证：7项精确变红（场景1/2/3a的核心断言、场景5a），场景3b/4/5b 在修复前后保持一致（这几个场景本来就该正常工作或本来就该确认现状，不受这次改动影响）。全部既有回归套件（18 个文件，1/4的18项+2/4的37项+3/4新增的17项+其余15个文件）全部重跑零回归；另外 origin 侧新增的 `run_cixiong_test.js`/`run_identity_mode_test.js`/`run_lidian_test.js` 一并重跑确认零影响。`?v=` 185→186。**后续排期（4/4）**：`aoeRespond` 补接雷击检查——这是四类独立缺陷里最后剩的一个，覆盖南蛮入侵/万箭齐发场景下张角打出真闪响应时的雷击触发。
- **张角【雷击/鬼道】修复 4/4（收尾）：`aoeRespond` 补接雷击检查——四类独立缺陷至此全部闭环，但实现过程中真实 dump 发现了原方案没预料到的第五个真实缺陷（AOE 续接），一并修复**。原方案是"照抄 3/4 的 `respondShan` 写法，在 `aoeRespond` 的 `card` splice 之后、`aoeAdvance` 之前插入同一段检查"，核实阶段（见上一次任务）已经确认这个插入位置和判断条件（`card.name==='闪'`，不附加 `need==='闪'` 前置条件——因为龙胆是双向转化，`canUseAs` 里 `role==='杀'&&card.name==='闪'` 同样成立，若张角能借到龙胆，用一张真闪当杀响应南蛮入侵，这张牌本身仍是"打出了一张【闪】"，按官方原文雷击依然应该触发，不该被 `need==='杀'` 挡住）都是对的、不需要返工。**但按这个方案落地后，任务自己要求的场景5（"张角是南蛮/万箭多个响应目标之一，雷击询问挂起期间其余角色不受干扰"）先用真实 dump 验证时直接报了假**：3人局万箭齐发，张角是第一个被问的目标，触发雷击后选择"不发动"（`cancelLeiji`），结果 `phase=play,pending=null`，但 `g.aoe` 仍然非空（从未清空）、第三名玩家从未被问到打闪——南蛮/万箭的响应队列被凭空放弃，状态还自相矛盾（`g.aoe` 非空但 `phase` 已经是终态 `play`）。
  - **根因**：雷击链路自己的三个收尾点——`doLeijiJudge` 的三处早退/尾部、`cancelLeiji`、`finishGuidu` 的 `leijiJudge` 分支——全部无条件写死 `g.pending=null;g.phase='play';`。这在 3/4（`respondShan` 触发）里从来没出过问题，因为单体杀响应解决完雷击之后本来就该回到 `play`，无需续接。但一旦 `aoeRespond` 也能触发雷击，这三处就需要多知道一件事——"这次雷击是不是从南蛮/万箭队列里触发的，结束后该不该转而调用 `aoeAdvance` 续接剩余目标"——而它们此前完全没有能力表达这件事，因为这个组合（雷击+群体锦囊）在这次改动之前根本不可达。
  - **修复**：新增 `g.leijiResume` 字段，和 `g.shensuResume`（神速）同一设计——不塞进 `g.pending`（会被期间可能打开的 `guiduAsk`/`guicai` 等子阶段覆盖），独立跨越 `leijiChoose`→`leijiJudge`→（可能的鬼道/鬼才改判）这几次 tx 边界存活。`maybeStartLeiji(g, sourceSeat, shanCard, resume)` 新增可选第4参数，`aoeRespond` 触发时传 `{prevSeat:mySeat}`，`respondShan`（3/4）触发时不传（`g.leijiResume` 保持 `null`，行为完全不变）。新增统一收尾出口 `finishLeijiChain(g)`——`g.leijiResume` 非空则消费掉并调用 `aoeAdvance(g, resume.prevSeat)` 续接队列，为空则维持原来的 `phase='play'`；`doLeijiJudge`/`cancelLeiji`/`finishGuidu` 的 `leijiJudge` 分支全部改成调用这一个函数，不再各自直接写 `g.pending=null;g.phase='play'`。`finishGuidu` 的 `leijiJudge` 分支顺带给了自己独立的 `return g`（不再落到给其它 `resume.kind` 兜底用的通用尾部），避免耦合。`normalize` 补一条对 `g.leijiResume` 的防御（和 `g.shensuResume` 同一写法：`typeof prevSeat!=='number'||!g.players[prevSeat]` 则清空），这一点和最初任务描述的"normalize无需改"不一致——是这次扩大范围后必须跟着补的，已经和用户确认过范围扩大再继续实现。**真实 dump 复核确认修复有效**：张角触发雷击后取消，`aoeAdvance` 正确推进到下一个目标（先开无懈可击询问窗口，这是 `aoeAdvance` 自己的既有设计，不是新问题），问完一圈无懈后正确到达第三人的 `aoeResp`，第三人选择不出闪正确受到1点伤害，最终 `g.aoe` 正确清空、`phase` 正确回到 `play`——整条队列完整走完，不再有目标被凭空丢弃。
  - **已知的、不在本次范围内的相邻问题（发现即报，不顺手修）**：`doLeijiJudge` 和 `finishGuidu` 的 `leijiJudge` 分支里，`dealDamage(g, targetSeat, 2, ...)` 的返回值从未被检查——如果这2点雷电伤害恰好让目标进入濒死（`dealDamage` 返回 `true` 挂起濒死流程），代码会继续往下执行 `finishLeijiChain(g)`，把濒死流程刚挂起的 `pending` 覆盖掉。这是一个独立的、和这次 aoe 续接无关的缺陷（在 3/4 单独触发雷击时就已经存在，不是这次新引入的），本次测试特意把目标血量设得足够高避免触发它，不在这次任务范围内处理，留给后续单独排期。
  - **测试**（`run_leiji_aoe_test.js`，21项，未落盘）：场景1/2（南蛮入侵 `need==='杀'` / 万箭齐发 `need==='闪'` 两个方向分别验证挂起 `leijiChoose` 且 `g.leijiResume.prevSeat` 正确记录）；场景3（边界，核实阶段发现的关键细节：手动构造一个同时拥有 `leiji`/`guidu`/`longdan` 三个 cap 的测试用武将，在南蛮入侵响应里明确指定 `cardIdx` 选中真闪当杀打出，断言 `need==='杀'` 但 `card.name==='闪'` 时雷击依然触发——证明判断条件没有被误绑定 `need`）；场景4（端到端，2人局，判定黑桃后目标正确受到2点伤害，`g.leijiResume` 正确消费清空，南蛮/万箭响应流程正确收尾）；场景5（多目标回归，真实复现过上面那个"续接"缺陷的同一构造，这次验证挂起雷击询问→取消→正确续接到无懈窗口→问完一圈→第三人正确被问到打闪→不出闪正确受伤→群体锦囊完整收尾，全程没有目标被丢弃）；场景6（非张角玩家打真闪的回归对照）；场景7（`normalize` 对 `g.leijiResume` 缺失字段/脏数据座位的防御）；场景8（回归确认 `respondShan`/3/4 触发雷击的路径完全不受这次改动影响，`g.leijiResume` 全程为 `null`，行为照旧）。`git stash` 验证：9项精确变红（场景1/2/3/4/7的核心断言），其余场景在修复前后表现出的差异符合预期（场景5的部分断言即使在旧代码上也会"巧合通过"，是因为更早的断言已经先行失败、后续断言在一个已经出错的前提下继续执行，不代表旧代码真的正确，已在测试注释里说明这一点，不影响整体判断）。同时回填更新了 3/4 遗留的 `run_leiji_entry_test.js` 场景5b（原先是"确认 `aoeRespond` 现状缺口"的断言，这次改成"确认 4/4 已经把缺口补上"的回归防护断言，逻辑闭环）。全部既有回归套件（20 个文件：1/4 的18项+2/4 的37项+3/4 的17项+4/4新增的21项+其余16个文件）全部重跑零回归；origin 侧的 `run_cixiong_test.js`/`run_identity_mode_test.js`/`run_lidian_test.js` 一并重跑确认零影响。`?v=` 186→187。**至此张角【雷击/鬼道】系列问题全部闭环**：①`askNextGuidu` 吞判定（1/4）、②`finishGuidu` 分派表不完整（2/4）、③雷击自己的入口断点（3/4）、④`aoeRespond` 未接雷击检查（4/4）、以及④的实现过程中额外发现并修复的⑤雷击链路收尾未感知 AOE 续接（`g.leijiResume` 机制，本条）——五个独立缺陷全部修复并有真实 dump/测试验证，当时唯一记录在案、故意留到以后单独处理的相邻问题是 `dealDamage` 濒死返回值未检查——这一条已在后续任务中修复（见下方专条），至此张角【雷击/鬼道】系列第六个、也是最后一个独立问题闭环。

- **曹冲【称象】修复①：亮出的4张牌改为全场公开可见，只有选牌/确认/取消这几个交互限本人操作——"亮出"类技能的公开展示原则，以后新增同类技能对照这条**。原来 `renderCaochong`（render-controls.js）用同一个 `g.pending.seat===mySeat` 网住 `chengxiangAsk`/`chengxiangChoose` 两个阶段的**整个**渲染，导致非曹冲玩家的客户端完全看不到任何内容——不知道曹冲发动了称象、不知道亮出了什么牌、不知道曹冲最终选了什么，这既不符合官方规则（"亮出"在三国杀里就是公开动作，只有"选哪几张"是曹冲的私有决定，牌面本身从翻开那一刻起对所有人可见），体验也很差。**核心修复原则：一个 pending 阶段的"内容可见性"和"交互操作权限"是两件独立的事，不能用同一个 `seat===mySeat` 判断同时决定，必须拆成两层**——参照项目里已有的公开判定类先例（五谷丰登的公共牌池 `poolDesc`：`picker===mySeat` 只决定"渲不渲染可点击的挑牌按钮"，牌面描述本身两个分支（`if`/`else`）都会显示，非本人客户端一样能看到公共池里的牌），不是发明新的可见性模式。**具体改法**：`renderCaochong` 对 `chengxiangChoose` 阶段改成不论 `mySeat` 是谁都调用 `renderChengxiangChoose(g)`，权限判断下沉到这个函数内部——`p.seat!==mySeat`（非本人）分支渲染一个只读展示区（牌名+点数，**不含任何 `onclick`**），`p.seat===mySeat`（本人）分支保留原有的完整交互面板（可点击选牌+"确认选择"/"选择0张"按钮）不变。`chengxiangAsk`（是否发动的意图本身）刻意维持原来"仅本人可见"的行为不变——这不是遗漏，是类比洛神"是否发动进行判定"同一处理原则：发动与否是曹冲自己的选择意图，官方规则没有要求这个决策过程本身也公开，只有"发动之后亮出的牌"才是公开信息，两者是不同性质的信息，不应该一刀切成同一种可见性。**测试**（`run_chengxiang_visibility_test.js`，11项，未落盘）：核心场景（非曹冲玩家能看到全部4张牌的牌名+点数，不再是空字符串）；权限拆分两侧对照（非本人HTML不含任何 `onclick`/"确认选择"/"选择0张"按钮文案，本人HTML完整保留这些交互元素）；`chengxiangAsk` 阶段非本人仍返回空字符串（确认"是否发动"这个意图性决策的隐藏行为没有被误改）；本人视角下 `chengxiangAsk` 的发动询问回归确认正常显示。`git stash` 验证：3项精确变红（非本人的HTML为空字符串、看不到牌名/点数，精确复现"仅曹冲可见"），其余8项修复前后一致。既有回归套件（20个既有文件）全部重跑零回归；origin 侧 cixiong/identity/lidian 一并重跑确认零影响。`?v=` 187→188。

- **曹冲【称象】排查②"不能拿牌"：真实根因是 `ReferenceError: g is not defined`，和任务最初怀疑的 Firebase/normalize 数组序列化完全无关——已用真实 dump 证伪那个方向，报告结论后确认修复（见下一条），历史排查过程原样保留**。**排查方法**：先读代码找到最可疑的两个函数 `toggleChengxiangCard(idx)`/`confirmChengxiangSelection()`（render-controls.js），发现它们都不是 `tx(g=>{...})` 回调、也不接收 `g` 作为参数，却在函数体第一行直接引用裸标识符 `g.pending`——而**全项目搜索确认整个代码库从未在任何地方声明过一个全局的 `let g`/`var g`**（`game.js:6542` 唯一出现过的 `const g = window.g;` 是法正【眩惑】那个已知损坏的函数，读的是从未被赋值过的 `window.g`，同样是空的）。渲染层真正的"最近一次收到的游戏状态"全局变量叫 `currentG`（`render.js:106` 声明，`render.js:731` 每次 `render(g)` 时更新），但 `render-controls.js` 全文件只在第1663行引用过一次 `currentG`（且那处是合法的，因为它嵌在 `renderControls(g,c)` 内部、`g` 是这个外层函数的真实参数，`currentG` 只是防御性兜底，不依赖它）——`toggleChengxiangCard`/`confirmChengxiangSelection` 完全没有引用 `currentG`，也不在任何持有 `g` 参数的闭包里，是两个孤立的顶层具名函数，通过 HTML 字符串里的 `onclick="toggleChengxiangCard(0)"`/`onclick="confirmChengxiangSelection()"` 由浏览器在全局作用域直接调用。**真实 dump 逐项验证**（用完全按 `index.html` 真实加载顺序装配的 vm 沙箱，不省略任何一个文件）：①`typeof g` 在沙箱全局作用域里确认是 `'undefined'`；②真实调用 `toggleChengxiangCard(0)` 抛出 `ReferenceError: g is not defined`；③真实调用 `confirmChengxiangSelection()` 同样抛出 `ReferenceError: g is not defined`——这精确复现了"玩家点击一张牌/点击确认选择按钮，页面毫无反应"这个报告症状（普通玩家看不到浏览器控制台，异常被静默吞掉，表现就是"点了没用"）。**任务原本怀疑的方向（Firebase/normalize 破坏 `selectable` 数组）已经证伪，不是根因**：真实调用 `confirmChengxiangAsk()` 后，`g.pending.selectable` 数组结构完整（15个组合项，含 `{indices:[],sum:0}` 兜底选项，值全部正确），额外单独跑一次 `normalize(g)` 前后用 `JSON.stringify` 逐字节比对，`chengxiangChoose` 这个 pending 完全没有被裁剪或改动（`normalize()` 里也确认了一遍——全项目搜索 `normalize` 函数体内没有任何一处提到 `chengxiang`，说明这个 pending 类型此前也从未被纳入过 CLAUDE.md「已完成」条目里"审查并补齐 normalize 里缺少防御的12个 pending 类型"那次盘点，`normalize` 对它完全不设防、也完全不裁剪，是被动放过而非主动破坏）。任务第4点怀疑的 `chengxiangSelectedIndices`（客户端本地状态）被意外清空同样查无实据——全项目搜索这个变量只在 chengxiang 自己这12行代码里被读写，没有任何外部 `reset*` 兜底逻辑碰它；而且由于 `confirmChengxiangSelection()` 在读到这个变量之前（函数体第一行 `const p=g.pending`）就已经抛出异常崩溃，这个疑虑点实际上从未被执行到过，不需要继续深究。**顺带发现两处和当前排查目标（可见性/拿牌）无直接关系、但值得记录的相邻问题，本次未处理**：①`confirmChengxiangAsk()`/`confirmChengxiang()`/`cancelChengxiang()`（game.js）三个函数**完全没有调用者身份守卫**（不检查 `g.pending.seat!==mySeat` 就直接 `return g`），和 CLAUDE.md 之前记录过的姜维【志继】`respondZhijiChoose`/马谡【散谣】旧版同型缺陷——任何在场客户端理论上都能替曹冲做发动/选牌决定；②`chooseRenxinEquip`（曹冲【仁心】，同一批代码里紧邻的另一个技能）倒是**有**身份守卫（`if(seat!==mySeat) return g;`），说明这不是"整体没做"，是称象这三个函数各自独立漏掉了，值得单独排期修。**修复方向（等待确认，本次未动手）**：`toggleChengxiangCard`/`confirmChengxiangSelection` 里的裸 `g` 改成 `currentG`（这是全文件唯一一处已验证可行的先例），或者把这两个函数改造成从 `renderChengxiangChoose(g)` 渲染时以闭包形式绑定（类似项目里其它"挂载 onclick 那一刻读取冻结值"的写法，CLAUDE.md 规则14），两种做法都能解决这个具体 bug，哪种更合适、要不要顺带把①里发现的三处身份守卫缺口一并补上，等你确认后再动手实现。

- **曹冲【称象】修复②：`toggleChengxiangCard`/`confirmChengxiangSelection` 的裸 `g` 改成 `currentG`，点击选牌/确认恢复正常**。改法采纳了上一条排查报告里的第一个方案（不是改造成闭包绑定）——两处 `const p = g.pending;` 改成 `const p = currentG.pending;`，`currentG` 是文件里唯一已验证可行的先例（`render.js:1663`，位于持有 `g` 参数的闭包内，只是防御性兜底；这次是这两个孤立顶层函数第一次真正依赖它）。**全项目搜索确认没有第三个同类函数**：写了一个跨 `render.js`/`render-controls.js`/`render-hand.js`/`render-table.js`/`render-log.js` 五个文件的静态扫描脚本（逐行追踪当前处于哪个顶层 `function` 声明、该声明的参数列表里有没有 `g`，命中"不含 `g` 参数的函数体内出现裸 `g.xxx` 属性访问"才报告）——在修复前的代码上先验证脚本本身有效（精确命中这两处已知位置，不多不少），修复后重新扫描（含放宽正则匹配任意 `g.` 属性、不局限于 `pending`/`players` 等白名单）确认零命中，`game.js`/`skills.js`/`weapons.js` 里大量的 `g.pending`/`g.players` 都是 `tx(g=>{...})` 回调内部的合法用法（不在这次排查范围内，那套模式本身没有问题）。**测试**（`run_chengxiang_pick_test.js`，17项，未落盘）：核心场景1/4（点击选牌/点击确认选择均不再抛异常）；场景2/3（连续选中/取消切换、点数和超限拒绝选中——确认修复只换了变量来源，没有动业务规则本身）；场景4（确认后 `confirmChengxiang` 服务端逻辑被正确触达：曹冲正确获得选中的牌、未选中的进入弃牌堆、`chengxiangSelectedIndices` 正确清空）；场景5（选0张的边界，`selectable` 里 `{indices:[],sum:0}` 兜底选项能被正确匹配到）；场景6（非曹冲玩家点击这两个函数仍然被 `p.seat!==mySeat` 挡住，确认修复没有连带削弱这条既有防护）。**测试环境的一个技术细节**：`toggleChengxiangCard` 末尾会调用 `render()` 刷新界面（和真实浏览器行为一致），但这次测试关心的是"点击本身会不会因为裸 `g` 引用抛异常"，不是完整 DOM 渲染管线本身的正确性（那部分项目里另有 Playwright 真实浏览器测试覆盖），所以把 `render` 换成沙箱内的 no-op，避免为了让这次点击测试跑通而去补全一堆和这个 bug 无关的 DOM stub 细节。`git stash` 验证：2项精确变红，精确复现 `ReferenceError: g is not defined`（和用户报告的"点了没反应"完全对应）。既有回归套件（22个文件，含①的11项）全部重跑零回归，确认这次改动和①的渲染权限拆分互不冲突；origin 侧 cixiong/identity/lidian 一并重跑确认零影响。`?v=` 188→189。**通用提醒（写进「三、改动原则」相关排查方法论，以后遇到"点击没反应"类报告时对照）**：这次排查一开始朝着 Firebase/normalize 数据层去查是完全合理的第一直觉（`selectable` 数组、空 `indices` 兜底项确实是历史上真实出过问题的模式，如 CLAUDE.md 记录过的 Firebase 吞空数组/序列化坑），但真实 dump 验证之后发现根因其实更靠前——**前端代码本身抛出了一个未捕获的运行时异常**，从未执行到任何数据层逻辑。以后排查"点击/操作没有任何反应"这类报告，应该把"前端是否抛出未捕获异常"作为和"数据层/Firebase同步是否损坏"**并列**的检查方向，不要默认往复杂的数据序列化方向猜——普通玩家看不到浏览器控制台，前端 `ReferenceError`/`TypeError` 造成的"静默无反应"和数据层损坏造成的"静默无反应"，从用户报告的症状描述上完全无法区分，必须靠真实 dump（或至少真实调用一遍对应函数）才能分辨，不能靠代码读起来"看起来合理"就采信最初的怀疑方向。

- **曹冲【称象】修复③：`confirmChengxiangAsk`/`cancelChengxiangAsk`/`confirmChengxiang`/`cancelChengxiang`（game.js）补齐调用者身份守卫——上一条排查②末尾发现的相邻问题，本条正式修复，任务点名3个，排查时发现同一批次的第4个函数 `cancelChengxiangAsk` 有完全相同的缺口一并修，仍在"称象"这一个技能范围内**。四处统一补 `if (seat !== mySeat) return g;`（或等价写法 `if (g.pending.seat !== mySeat) return g;`），插入位置紧跟 `pending.type` 校验之后、`const seat = g.pending.seat;` 之后，参照 `chooseRenxinEquip` 的既有写法保持风格一致。**排查结论——为什么仁心有守卫、称象没有（任务点名要求排查的问题，已找到具体原因，不是靠猜）**：查了 `chooseRenxinEquip` 的 git 历史（`git log -S"chooseRenxinEquip"`），发现在最初引入这两个技能的那次提交（`bbbf1f3`「林林总总」，作者 `paoqiyubeipan`，和这个 Claude Code 会话无关的另一位贡献者）里，`chooseRenxinEquip` **同样没有身份守卫**——两个技能是同批代码同时缺失，不是"仁心从设计之初就更严谨"。守卫是在紧接着的下一次提交（`c2fe408`「审查代码错误并改修」，同一作者）里出现的，但那次提交的主要目的是修正仁心自身一个独立的功能性 bug——`equips` 装备区实际是按槽位名（`weapon`/`armor`/`plus1`/`minus1`）索引的对象，而 `chooseRenxinEquip` 最初的实现却用 `equipIndex` 当数组下标去访问（`me.equip[equipIndex]`），这是访问方式本身就错的 bug；修正这个寻址方式（`equipIndex`→`slot`）需要重写整个函数体，作者在重写过程中顺手加上了 `seat!==mySeat` 这行安全检查——**称象的四个函数在这同一次「审查代码错误并改修」提交里完全没有被触碰，不在那次review的范围内**，守卫缺口就此原样遗留下来，一直没有被后续任何一次代码审查发现，直到这次用户报告才浮出水面。**这个结论给以后的启示**：功能性 bug 修复（如寻址方式错误）和安全性检查（身份守卫）有时会被同一次改动顺手一起补上，但这只是"顺手"，不是系统性保证——同一批引入的其它函数即使有完全相同的缺陷模式，如果没有独立触发一次对它们的review，缺口会一直存在，不能因为"同一批代码里有的函数已经修过类似问题"就假设其它函数也自动没事。**真实漏洞验证**（真实 dump，参照之前 `respondTiaoxinChoice`/`respondSanyao` 那两次的验证方式）：构造"座位2既不是曹冲(座位0)也不是攻击者"这个第三方座位，分别调用四个函数,修复前全部成功执行——能越权触发曹冲发动称象并翻开牌堆（场景A：`deck.length` 从5变1）、能越权替曹冲确认选牌把牌塞进曹冲手里（场景B：曹冲手牌从0变2）、能越权替曹冲弃置全部4张亮出的牌（场景C：`discard.length` 从0变4）、能越权强行取消曹冲还没决定的发动意图（场景D：`pending` 被清空）。**测试**（`run_chengxiang_guard_test.js`，14项，未落盘）：核心场景1-4（座位2越权调用四个函数，均应被拒绝，`pending`/`g.deck`/`g.discard`/曹冲手牌/`g.log` 全部保持不变）；正对照场景5（曹冲本人正常调用四个函数，行为完全不受影响——发动后正确翻牌进入选择阶段、确认选牌正确获得对应的牌、取消发动正确清空、选0张正确把4张全部弃入弃牌堆）。`git stash` 验证：9项精确变红（4个越权场景的核心断言全部复现漏洞，含真实数值变化如 `deck.length`/`discard.length`/曹冲手牌数），正对照的5项修复前后保持一致（本人调用从未受过影响）。既有回归套件（22个既有文件，含①②的11+17项）全部重跑零回归；origin 侧 cixiong/identity/lidian 一并重跑确认零影响。`?v=` 189→190。**全项目同类扫描（任务要求，只报告不修）**：写了一个跨 `game.js`/`skills.js`/`weapons.js` 的静态扫描脚本，找"读取 `g.pending.seat` 且是 `tx(g=>{...})` 回调、但没有 `mySeat` 比对"的函数，初筛命中10个候选；逐一人工核对代码后，其中9个是脚本的假阳性（脚本的正则只认"`seat!==mySeat` 提前 return"这种否定式写法，漏掉了项目里同样大量使用的"`if(...&&seat===mySeat){...}` 肯定式包裹"写法——`respondGuicai`/`cancelJushou`/`cancelFenxun`/`pickTianyiTarget`/`cancelTianyi`/`skipShensu1`/`skipShensu2`/`cancelShensuSha`/`cancelQiangxi` 这9个逐一确认过都有守卫，只是写法不同，脚本没识别出来）；唯一一个真实缺口是 `respondZhijiChoice`（skills.js:1693，姜维【志继】）——但这**不是新发现**，是「四、已知的待优化点」原来就记录过的姜维【志继】那条既有条目（本次排查只是重新验证了一遍这个缺口确实还在，没有变化），不在本次称象修复的范围内，继续保留在待优化点清单里等待后续单独排期，本次未处理。

- **张角【雷击/鬼道】修复⑥（收尾，系列全部闭环）：`doLeijiJudge`/`finishGuidu` 的两处 `dealDamage` 调用补上濒死返回值检查——4/4 遗留的第六个独立问题**。`dealDamage(g, targetSeat, 2, sourceSeat, ..., 'leiji')` 返回 truthy 表示目标进入濒死、已挂起濒死/求桃 pending，调用方必须立即 `return`，把控制权交给濒死流程，不能继续执行后续收尾——这是项目里已有的统一约定（`respondShan`/决斗/AOE/恩怨等至少6处在用）。雷击这两处此前完全没检查，如果2点雷电伤害恰好导致目标进入濒死，紧跟着无条件执行的 `finishLeijiChain`（4/4 新增的 AOE 续接统一收尾出口）会在同一次 tx 里把刚挂起的濒死 pending 直接覆盖——目标从未真正获得求桃机会。**修复**：两处都改成 `const dying = dealDamage(...); ...; if(dying) return g;`（判定结果日志照旧紧跟在 `dealDamage` 之后写，不受 `dying` 影响），dying 分支不做任何额外收尾。`resumeAfterInterrupt` 新增 `resume.type==='leiji'` 分支——**不是新逻辑，直接复用已有的 `finishLeijiChain(g)`**：`g.leijiResume` 是 4/4 引入的、独立于 `g.pending` 之外的字段，濒死这整段打断期间完全不会被碰到（它不挂在 `g.pending` 上，不会被濒死流程覆盖），所以直接调用 `finishLeijiChain(g)` 就能天然得到和雷击自己判完黑桃之后完全一致的收尾判断——该回 `play`（`respondShan` 触发的普通场景）还是该 `aoeAdvance` 续接（南蛮/万箭响应里触发的场景），一行代码复用，不需要为濒死这条路径单独设计一套判断。**复合场景（南蛮/万箭响应中雷击致濒死，任务点名要求验证）：不是"已知限制"，是完整支持、真实端到端验证过的**——真实 dump 逐步验证：张角在万箭齐发响应里触发雷击→选目标→判定黑桃致目标濒死→目标用桃救回→`finishLeijiChain` 正确检测到 `g.leijiResume` 非空、推进 `aoeAdvance`→AOE 队列正确开出无懈可击窗口→正确轮到获救目标自己对这次万箭齐发的响应义务（这是和雷击伤害完全独立的另一件事，只是恰好落在同一个座位号上）→该目标没有闪、再受1点伤害，血量卡在1（濒死脱离的血量恒定停在1，respondDying 每次只回1点、一旦 >0 立即结束，规则本身如此）后又立即被这1点伤害打回濒死→第二次濒死同样正确走完（`resume.type==='aoe'` 这条既有路径，本来就有正确处理，不是本次改动范围，只是顺带验证了它在"紧跟在一次 leiji 濒死之后"这个新出现的时序下依然正确）→队列正确推进到第三人→第三人正常受伤→群体锦囊正确收尾。全程没有任何一步卡死或丢失响应机会。**测试**（`run_leiji_dying_test.js`，22项，未落盘）：核心场景1（`doLeijiJudge` 挂起标准濒死 pending，`resume.type==='leiji'` 正确记录，不是被冲掉的 `play`/`null`）；场景2（无人可救，正确阵亡后接回 `finishLeijiChain`）；场景3（连续两张桃救回，脱离濒死后正确回到 `play`）；场景4（上述复合场景的完整端到端验证，含"座位号巧合导致同一个人经历两次连续濒死"这个真实边界）；场景5（血量充裕、伤害不致濒死的普通雷击回归，确认不受这次改动影响）；场景6（`finishGuidu` 的 `leijiJudge` 分支——鬼道改判后同样正确处理濒死，**这个场景在实测中曾因为测试自己的牌花色选错（用了 `♣` 梅花而不是 `♠` 黑桃）而误判失败**，排查后确认是测试脚本的问题不是代码问题——雷击官方规则精确要求黑桃 `♠`，不是任意黑色，这条规则本项目此前已经确认过，这次是测试构造数据时的疏忽，修正测试数据后正确通过）。`git stash` 验证：多项精确变红，且修复前的代码在测试脚本运行到场景6时会直接抛 `TypeError: Cannot read properties of null (reading 'resume')`（因为 `g.pending` 被冲掉成了 `null`）——这个崩溃本身就是"修复前流程走不下去"的直接证据。全部既有回归套件（23个既有文件，含张角系列1/4~4/4的全部93项）全部重跑零回归；origin 侧 cixiong/identity/lidian 一并重跑确认零影响。`?v=` 190→191。**至此张角【雷击/鬼道】系列六个独立问题全部闭环**：①`askNextGuidu` 吞判定、②`finishGuidu` 分派表不完整、③雷击自己的入口断点、④`aoeRespond` 未接雷击检查、⑤雷击链路收尾未感知 AOE 续接（`g.leijiResume` 机制）、⑥雷击伤害未检查濒死返回值（本条）——全部有真实 dump/测试验证，无遗留问题。

- **姜维【志继】`respondZhijiChoice` 补齐调用者身份守卫——原「已知的待优化点」一条，已排查确认+修复，现从该清单移除**。`respondZhijiChoice(healOrDraw)`（`skills.js`）原来的守卫只有 `if(g.phase!=='zhijiChoice'||!g.pending||g.pending.type!=='zhijiChoice') return g;`，从头到尾没有出现过 `mySeat`，任何在场客户端都能替姜维选择志继觉醒的效果（回复1点体力/摸两张牌）。**排查确认**：字段名核对无误——触发处 `startTurn` 里 `g.pending={type:'zhijiChoice',seat}`（`game.js:5573`），响应函数确实用的是 `g.pending.seat`，和挑衅的 `to`/散谣的 `from` 不同，字段名各技能各自独立，不能照搬抄错。**真实漏洞验证**（真实 dump，参照 `respondTiaoxinChoice`/`respondSanyao`/曹冲称象那几次的验证方式）：构造座位2（既不是姜维也不是任何相关方）越权调用，修复前确实能成功执行——姜维 hp 从2越权涨到3、被越权授予【观星】能力、`pending` 被越权推进到下一个阶段（`guanxingReview`）；越权摸牌分支同样确认手牌从0越权变成2张、牌堆被越权翻开。**顺手排查这条链路里是否还有第二个函数需要同样的守卫**：全项目搜索 `zhijiChoice` 只有三类出现——`normalize` 的数据形状防御（`game.js:336`，校验 `seat` 是否为存活座位，属于既有的、和身份守卫无关的另一类防御，本身没问题）、`startTurn` 里的触发点（服务端自动判定，不是客户端可调用的入口，不需要守卫）、`render-controls.js` 的 UI 渲染（`g.pending.seat===mySeat` 才渲染交互按钮，UI 本来就是对的，只是服务端没跟上）——确认 `respondZhijiChoice` 是唯一需要补的入口，不是"漏了一半"。**修复**：`phase`/`pending.type` 校验之后加一句 `if(g.pending.seat!==mySeat) return g;`，和 `respondTiaoxinChoice`/`respondSanyao` 同一范式。**测试**（`run_zhiji_guard_test.js`，14项，未落盘）：核心场景1/2（座位2越权调用回血/摸牌两个分支，均应被拒绝，hp/手牌/观星能力/`pending`/日志全部保持不变，用真实数值变化而不只是返回值做验证）；场景3（场上唯一的"另一位玩家"同样不能越权，排除"只挡住了某一种特定身份"这种狭窄修复）；正对照场景4/5（姜维本人正常调用两个分支，行为完全不受影响——回血正确+1、摸牌正确摸到2张、两种情况都正确获得【观星】能力、`pending` 正确清空并推进后续流程）。`git stash` 验证：8项精确变红（越权场景的核心断言全部复现漏洞，含真实的 hp/手牌/`pending`/日志数值变化），6项正对照修复前后保持一致（本人调用从未受过影响）。全部23个既有测试文件（含张角系列全部93项+雌雄双股剑14项）全部重跑零回归；origin 侧 cixiong/identity/lidian 一并重跑确认零影响。`?v=` 191→192。

- **凌统【旋风】第7个入口：贯石斧弃自己装备时旋风 pending 被无条件覆盖——"日志显示发动但实际没有效果"这个新症状描述的真实根因，独立于之前 6 轮修复（独立入口三处/麒麟弓猛进 resume v1+v2/趫猛烈刃）**。用户这次报告的症状形状和以往"完全没反应/pending 被覆盖成 null"不同——日志确实写入了内容，但玩家没有获得应有效果，排查时特别参照了悲歌当年"嵌套 tx 导致看起来做了但没真做"的教训，但**这次的根因不是嵌套 tx，是标准的 `pendingBefore` 检查再次被漏做**。
  - **真实 dump 定位**：`weapons.js` 的 `respondGuanshi`（贯石斧）处理 `equip:` 类型弃牌项的循环里，`triggerHook(g, from, 'onLoseEquip', {count:1})` 正确触发凌统自己的旋风钩子——钩子把 `g.pending` 换成 `{type:'xuanfengPick',...}` 并写入日志"凌统 失去装备,可以发动【旋风】,弃置其他角色的共计至多两张牌"。但循环结束后紧跟着的 `g.pending=null;`（原 weapons.js:293）**无条件执行，从未检查 `triggerHook` 是否已经改写了 `g.pending`**，在同一次 tx 里把刚挂起的旋风原地冲掉——真实 dump 精确复现：`日志里是否出现"发动【旋风】"的提示? true` / `g.pending 实际是什么: null`，和用户报告的症状逐字对应。复现条件：凌统装备贯石斧，出杀被闪抵消后发动贯石斧，弃的2张牌里包含**自己的另一件装备**（不是纯手牌）——这是贯石斧本身合法的弃牌组合，日常场景（凌统天生就带旋风，装贯石斧是完全正常的武器搭配），不需要左慈化身这类罕见组合。
  - **排查过程顺带排除的方向**：弃牌阶段 `discardCards` 路径（`run_xuanfeng_test.js` 16项全过）、已修复的7个（顺手/拆桥/借刀/麒麟弓/猛进/趫猛/烈刃）入口（`run_xuanfeng_resume_test.js` 27项+`run_qiaomeng_lieren_test.js` 25项全过）均确认**没有被夏侯渊神速/张角雷击那几轮改动波及、无新回归**；`xuanfengPick` 从选目标到 `executeXuanfeng` 真正执行弃牌的链路本身没问题（日志在真正 splice+push 进 discard 之后才写，不存在"日志早于执行"）——问题不在这条链路本身，是这条链路根本没机会被启动。
  - **修复（比单纯"检查后 return"更深一层，因为贯石斧强制命中的伤害发生在旋风打断点之后，不能像"发现即 return"那么简单，需要 resume 续接）**：`sourceCard` 必须在弃装备触发钩子**之前**先取出（钩子可能改写 `g.pending`，晚取就读不到）；处理弃牌前先 `g.phase='play'`（让旋风钩子捕获 `previousPhase` 是正确的休止相，不是死掉的 `guanshi`），再拍 `pendingBefore` 快照；等两张牌（不管手牌还是装备）**同时全部真实结算完毕**后才检查 `g.pending !== pendingBefore && g.pending`——若被打断，挂 `resume={type:'guanshiDamage',from,to,sourceCard}` 并 `return`，**不执行原来"命中即立刻 `dealDamage`"这一步，把它推迟到旋风问完之后**；抽出共用尾巴 `finishGuanshiDamage(g,from,to,sourceCard)`（和散谣的 `finishSanyaoDamage` 同一范式），两处调用：①`respondGuanshi` 未被打断的直达路径；②`resumeAfterInterrupt` 新增的 `'guanshiDamage'` 分支。这是继猛进/麒麟弓 v2 那次"不能只接回 `finishSingleShaTarget`，要真正推迟damage结算"的教训之后，第二次在这个项目里用到"resume 携带的不是恢复点，而是待执行的具体动作参数"这种模式（第一次是散谣的 `finishSanyaoDamage`）。
  - **测试**（`run_xuanfeng_guanshi_test.js`，26项，未落盘）：核心1（挂起 `xuanfengPick`+`resume` 完整字段+`previousPhase='play'`+目标此刻还不该受伤，命中被正确延后）；核心2（完整链路——选目标+弃满2张真正执行 `executeXuanfeng`，第三人真的被弃了装备，随后贯石斧的强制命中伤害正确接回结算，不是又一次"日志写了没真做"）；对照组A（不发动贯石斧，不误触发旋风）；对照组B（只弃手牌不涉及装备，直达伤害结算，行为不变）；边界（`cancelXuanfeng` 取消旋风后同样能正确接回强制命中伤害）。`git stash` 验证：修复前精确复现 `{"phase":"play","pending":null}`（和用户报告症状逐字对应），恢复后 26 项全绿。既有回归全跑：`run_xuanfeng_test.js`(16)/`run_xuanfeng_resume_test.js`(27)/`run_qiaomeng_lieren_test.js`(25)/`run_cixiong_test.js`(14)/`run_identity_mode_test.js`(35)/`run_lidian_test.js` + 本轮此前累积的全部23个测试文件，共约350+项全部重跑零回归。`?v=` 195→196。
  - **排查中发现但未逐一实测验证、结构相同的3处已知风险点（本次不处理，留待后续）**：`equipCard`（`game.js:2674`，同槽换装时旧装备离场）、大乔【流离】（`game.js:3336`，弃自己装备转移杀的目标）、曹冲【仁心】（`game.js:6925`，弃自己装备防止伤害）——这3处都是同一个模式（`triggerHook(...,'onLoseEquip',...)` 后紧跟无条件 `g.pending=null`，没有 `pendingBefore` 检查），但都需要"当事人自己也持有旋风能力"才会触发——凌统本人天然不会触发流离/仁心（不是这些技能的拥有者），要撞上必须靠**左慈【化身】借用**凑出"这个人既有流离/仁心又借了旋风"这种组合，比贯石斧（凌统装贯石斧是完全正常的日常搭配）罕见得多。标记为已知风险点，暂不处理，留待以后如果真的复现或做专项排查时再处理。
  - **这个标准范式（"触发 `triggerHook` 之后必须检查 `g.pending` 是否被改写，不能无条件覆盖"）在项目里已经出现过 7 次入口遗漏**（独立入口三处、麒麟弓、猛进、趫猛、烈刃、这次的贯石斧）——**再次强调**：任何新增会导致"失去装备"的代码路径（不管是主动弃装、被动被拆、换装、还是作为某个技能/武器的成本支付），只要涉及 `triggerHook(g, seat, 'onLoseEquip', ctx)` 这一行，都必须对照这条检查清单——先拍 `pendingBefore` 快照，触发后检查 `g.pending !== pendingBefore && g.pending`，是则挂起对应的 `resume` 信息、`return`，绝不能不加检查就紧跟着无条件重置 `g.pending`/`g.phase`。

- **凌统【旋风】第8个触发场景：`advanceXiaoguo` 的 `asker===null` 分支多余的 `finishTurn` 调用——2026-07-13 一次三方合并把两条分支各自正确的独立修改叠加在一起产生的遗留 bug，性质和前 7 个入口不同**。用户报告"日志显示可发动【旋风】但直接跳到下一回合"（`凌统 弃置了2张牌`→`凌统 可以发动【旋风】…`→`轮到 XXX`），真实 dump 精确复现，最终态 `phase='draw'`、`turn` 已推进到下一人、`pending===null`。
  - **根因溯源**（`git log -p -L` 逐版本追溯）：`advanceXiaoguo(g,endingSeat,current)` 的 `asker===null` 分支，改动前是 `g.pending=null; continueEndPhaseAfterXiaoguo(g, endingSeat);finishTurn(g, endingSeat); return;`——`continueEndPhaseAfterXiaoguo` 自己内部（旋风→举荐→据守链条）在挂起新 pending 时会 `return`，没有更多可发动的技能时**自己就会调用 `finishTurn`**（函数体内多处 `finishTurn(g,endingSeat);return;`），是一条自足、自终止的链条——但紧跟着这行又无条件补了一次 `finishTurn(g,endingSeat)`，若 `continueEndPhaseAfterXiaoguo` 中途挂起了旋风（或举荐/据守）的新 pending 并 `return`，这个多余调用会在**同一次 tx** 里把刚挂起的 pending 冲掉、直接推进到下一回合。追出处：`c2fe4083`/`2df93b2`（paoqiyubeipan 分支）先后把这一行改成"只调用一次 `continueEndPhaseAfterXiaoguo` 后 `return`"（正确，无 bug）；同一天 `050d965`（zjc-taikutu，main 分支，"修复循环型响应函数结束分支未置空pending"那次）在**尚未合并对方分支改动、`advanceXiaoguo` 还是最原始版本**的基础上，把 `if(asker===null){finishTurn(...);return;}` 改成 `if(asker===null){g.pending=null;finishTurn(...);return;}`——在当时那个分支自己的上下文里同样正确；两条分支各自独立、各自正确，`f0c2b4890935`（"Merge branch 'main' into wenwen_dev"）三方合并这两条分支时，git 把两边对同一行的修改都保留了下来，拼成了现在这个"`continueEndPhaseAfterXiaoguo` 和 `finishTurn` 都调用"的错误版本——**这不是任何一次单独提交写错了代码，是合并冲突没有被人工正确解决**。此前「已知的待优化点」曾经记录过"`finishTurn` 被调用两次"这个现象（左慈【化身】开发时顺带发现），但当时的评估结论是错的——判断"这条链路每一环都是无消耗型的读判断、两次调用巧合收敛到同一个终态"，**漏算了旋风的 B 路径检测本身就是消耗型判断**（`g.xuanfengDiscardUsed` 一次性翻转 + 开新 pending 后 `return`）——这条错误评估已随本次修复从「已知的待优化点」移除、更正。
  - **修复**：直接删掉这行里多余的 `finishTurn(g, endingSeat);` 调用（不是新增检查逻辑，是恢复到 `continueEndPhaseAfterXiaoguo` 自己已经自足自终止这条既有设计）——改成 `g.pending=null; continueEndPhaseAfterXiaoguo(g, endingSeat); return;`，和 `endTurn()` 自己那处正确的调用方式（`game.js:5229`，`continueEndPhaseAfterXiaoguo(g, mySeat); return g;`，同样没有多余的 `finishTurn` 尾巴）完全一致，两处调用同一条链的方式统一。**复现条件**：至少要有一名骁果候选人先被问过并拒绝（`respondXiaoguo(false)`→`advanceXiaoguo` 找下一候选人→无更多候选→`asker===null`），纯粹"没有骁果候选人"的场景（`endTurn()` 自己的内联检查，`game.js:5221-5229`）从来没有这个 bug，一直是既有 `run_xuanfeng_test.js` 第7项 B 路径测试的既有盲区（凌统+张飞，张飞没有骁果，天然走不到 `advanceXiaoguo` 的这个分支）。
  - **影响面比"旋风专属"更广**：这个多余的 `finishTurn` 调用出现在骁果问完之后的整条结束阶段技能链条入口，**举荐（徐庶）/据守（曹仁）同样会被这个 bug 波及**——只要骁果先问过至少一人并被拒绝，紧接着该轮到的举荐/据守询问同样会被这次多余的 `finishTurn` 调用冲掉，不是旋风独有的问题。
  - **顺带排查了全项目还有没有其它同类"合并冲突叠加"遗留（只报告，未处理）**：用 `git log --oneline --all --merges` 找了 2026-07-13 前后其它三方合并提交，未发现第二处类似"两条分支各自正确的修改被合并成错误组合"的模式——这次 `advanceXiaoguo` 这处目前是唯一确认的实例，后续如果再发现类似症状（"看起来两次独立修复都对，合起来却错了"），可以按这次的排查方法（`git log -p -L` 逐版本追溯到具体哪次合并）复用。
  - **测试**（`run_xiaoguo_finishturn_test.js`，18项，未落盘）：核心1（凌统弃2张+乐进骁果拒绝→旋风pending正确挂起+回合未提前推进，完整链路续跑选目标+弃牌执行+旋风结束后正确推进到下一回合）；核心2（`cancelXuanfeng` 取消旋风后同样正确推进）；回归1（既有B路径对照组——凌统+张飞无骁果候选人，走 `endTurn()` 自己的内联分支，从未受这次改动影响，确认无回归）；回归2/3（徐庶举荐、曹仁据守——骁果问完一圈之后同一条链的另外两环，同样验证正确挂起、不被冲掉，证明这次修复对整条结束阶段技能链都生效，不只是旋风）。`git stash` 验证：8项精确变红，且回归2/3的失败（`phase:'draw',pending:null`）和用户报告的旋风症状完全同构——证实这确实是"一条链、一个 bug 点、影响三个技能"，不是三个独立问题。既有全套回归（`run_xuanfeng_test.js`/`run_xuanfeng_resume_test.js`/`run_qiaomeng_lieren_test.js`/`run_xuanfeng_guanshi_test.js`/`run_cixiong_test.js`/`run_identity_mode_test.js`/`run_lidian_test.js` + 本轮累积全部28个测试文件）全部重跑零回归。`?v=` 196→197。

- **修复 `normalize()` 里 11 处 `d.xxx !== mySeat` 判断——跨客户端污染共享状态的系统性 bug，影响面覆盖 6 个技能家族、不止旋风。这是凌统旋风排查系列的收尾，但根因和前 8 个触发场景完全不同类**。用户报告"三人局，日志已写'可以发动【旋风】'，但操作区仍是弃牌阶段横幅+'结束回合'按钮"，此前三轮沙箱 dump（2人局无骁果/2人局骁果拒绝/3人局马谡先手回合）全部"正常"，直到用户提供截图逼出关键线索——**日志活下来了、`phase`/`pending` 没活下来**，这不可能是"服务端从未挂起过"（那样连日志都不会写），必然是挂起之后又被后续某一步清空了，而且是**只在非本人视角下**才清空。
  - **根因**：`normalize(g)` 是跨客户端共用的数据整形函数，会在两条路径上被无条件调用——**读路径**（`render.js:741`，任何客户端收到 Firebase 推送的新快照都会调，用的是**这台客户端自己的** `mySeat`）和**写路径**（`game.js:2243`，`tx(fn)` 内部在业务回调执行前先调，用的是**发起这次调用的客户端自己的** `mySeat`）。但 `normalize` 函数体内有 11 处 pending 防御写成了 `if(...结构性检查... || d.xxx !== mySeat){ 清空pending }`——把"这个 pending 是不是给我看的"这种**视角相关**的判断，混进了本该只做数据结构合法性校验的共享整形函数里。结果：这个 pending **只在归属者本人的客户端上存活**，任何其他玩家的客户端只要 `normalize` 一次（哪怕只是被动接收一次 Firebase 推送，不需要主动做任何操作），本地看到的画面就会被清空回上一个阶段——真实三人局 dump 精确复现：凌统（`mySeat=1`）触发旋风后，凌统自己视角 `pending` 保留、`phase='xuanfengPick'`；马谡（`mySeat=0`）和刘备（`mySeat=2`）视角 `pending=null`、`phase='discard'`，和用户截图逐字对应。**更严重的一层，真实 dump 验证过**：这不只是"别人本地画面不对"这种纯展示问题——**任何非归属者客户端只要触发了任意一次 `tx()` 调用（哪怕这次调用本身会被那个函数自己的业务 guard 拒绝、什么都没做成），`tx()` 内部无条件先跑的 `normalize(g)` 就会用那个客户端自己的 `mySeat` 把 pending 真的从 **Firebase 共享数据本身**里清空——这时连归属者本人重新刷新页面看到的都已经是被污染后的状态了，不是"我这边显示不对"，是"数据真的没了"。这就是用户报告"点结束回合看起来没反应，再点一次才推进到下一回合"的完整成因：第一次点击（凌统）服务端正确写入了 `xuanfengPick`，但在凌统来得及选目标之前，只要另一名玩家的客户端因为任何原因触发了一次 `tx`（哪怕是完全不相关的操作），pending 就被悄悄冲掉，凌统这边画面变回弃牌阶段，再点一次"结束回合"才真的推进到下一人。
  - **为什么日志能活下来**：`g.log` 的整形完全不依赖 `mySeat`（只涉及 `{seq,text}` 结构投影），任何客户端 `normalize` 之后都原样保留；`d.xxx!==mySeat` 是这 11 处防御里**唯一**依赖"当前是谁在看/谁在写"的条件，两者被同一次 `normalize` 调用处理、判断依据却完全不同，因此呈现"日志活着、pending 死了"这个分裂现象。
  - **影响面：11 处，覆盖 6 个技能家族，不止旋风**。全项目搜索 `normalize` 函数体内的 `!== mySeat` 模式，逐字比对写法几乎一致（大概率是同一个开发习惯被复制了11次），完整清单：袁绍【乱击】`luanjiChoose`/`luanjiConfirm`、张角【雷击】`leijiChoose`、曹仁【据守】`jushouChoose`、蔡文姬【悲歌】`beigeChoose`/`beigeDiscard`/`beigeJudge`、丁奉【短兵】`duanbingChoose`、凌统【旋风】`xuanfengPick`、丁奉【奋迅】`fenxunDiscard`/`fenxunTarget`——这些技能理论上都可能复现和这次旋风一模一样的"响应窗口被队友的客户端悄悄清空"现象，只是复现率不同（取决于窗口期间有没有其他玩家的客户端凑巧触发过 tx）。
  - **修复**：把这 11 处的 `d.xxx !== mySeat` 条件**全部移除**，只保留原有的结构性校验（字段类型/存活性/数组形状等，这些本来就是对的，没有一并动）。**"是不是我该操作"这件事，本来就有两层独立完整的防线，不需要 normalize 掺和第三层**——渲染层（`render-controls.js`，逐一核对确认全部 11 处 pending 类型都有对应的 `xxx===mySeat`/`xxx!==mySeat` 判断，决定该给这个座位渲染操作按钮还是等待提示）和服务端响应函数（各自的 `respond*` 函数开头，逐一核对确认全部 11 处都有 `pending.xxx!==mySeat` 身份守卫，和这个项目已经修过多轮的姜维志继/挑衅/散谣等同一套机制）——**排查确认这两层对全部 11 处都完整覆盖，没有发现任何一处因为移除 normalize 这条防线而出现新的越权缺口**，不需要额外补任何守卫。
  - **方法论教训（重点记录，防止以后重蹈）**：**沙箱单 `mySeat` 测试天然无法发现这类"多客户端视角污染共享状态"的问题**——这次凌统旋风的排查连续三轮（2人局无骁果、2人局骁果拒绝、3人局马谡先手）都用"单个 vm 沙箱、单个 `mySeat`、直接检查内存里同一个 `g` 对象"的方式验证，全部得出"正常"的结论，恰恰是因为这种测试方式里从头到尾只有一个 `mySeat`、一份 `g`，天然模拟不出"服务端一份共享数据、每个客户端各自带着不同 `mySeat` 独立调用 `normalize`"这个真实联机场景的核心特征——**以后任何涉及 `g.pending`/共享状态的排查，只要怀疑"服务端正确但客户端表现不一致"这类症状，必须补一步"同一份 `g`，分别以不同 `mySeat` 独立跑 `normalize`，对比结果是否一致"**，不能只用一个 `mySeat` 跑一遍就下结论"正常"。
  - **测试**（`run_normalize_myseat_test.js`，43项，未落盘）：核心1-8——对全部 11 处 pending 类型（`xuanfengPick`/`beigeChoose`/`beigeDiscard`/`beigeJudge`/`leijiChoose`/`luanjiChoose`/`luanjiConfirm`/`jushouChoose`/`duanbingChoose`/`fenxunDiscard`/`fenxunTarget`）分别用同一份 `g` 模板、三个不同 `mySeat`（`JSON.parse(JSON.stringify(...))` 深拷贝出独立份，模拟每个客户端各自拿到的 Firebase 快照反序列化结果）跑 `normalize`，断言全部三个视角 `pending` 均保留；回归1——8个代表性脏数据场景（`from`/`sourceSeat`/`seat` 指向已死玩家/不存在座位/非数字类型、数组字段为空或非法），即使用归属者本人的 `mySeat`（排除掉已修复的 mySeat 不匹配这个因素干扰），仍应被正确清空，确认移除 `!==mySeat` 条件没有连带削弱其余结构性防御；回归2——真实调用 `renderControls(g)`，归属者视角能看到操作按钮、非归属者视角看不到，确认渲染层这道独立防线不受这次改动影响。`git stash` 验证：22项精确变红（全部11种pending类型的"非归属者视角"场景），且失败模式和用户截图描述完全一致；脏数据回归和渲染层回归在修复前后保持一致（本来就没受这条 mySeat 判断保护，移除前后行为不变）。既有全套回归（本轮累积全部29个 scratchpad 测试文件 + `run_cixiong_test.js`/`run_identity_mode_test.js`/`run_lidian_test.js`）全部重跑零回归。`?v=` 197→198。

- **新增周泰【不屈】牌在座位卡上的展示——数据层此前一直完整（`p.buquCards` 放置/移除/防死判定都正确），但从未有任何渲染代码，这次纯新增视觉展示，不碰任何数据/逻辑层**。排查确认 `render.js`/`render-controls.js` 全项目从未引用过 `buquCards`，唯一相关渲染是"是否发动不屈"那个一次性询问弹窗——这个功能是彻底的空白，不是"渲染坏了"。
  - **必须做的理由（不只是好看）**：周泰的存活判定规则是"所有不屈牌点数唯一才能不死"，这意味着其他玩家需要看到周泰有哪几张不屈牌、点数分别是多少，才能判断他还能不能被杀死——这是一条**本该公开、且规则判定依赖**的博弈信息，此前完全不可见，等于隐藏了规则相关的关键信息。所以这次新增的展示**对所有玩家可见，不做任何视角限制**。
  - **位置与布局**：`.seat-bottom`（座位卡底部锚定的 flex column，判定区+装备条既有的复用容器）内，顺序为 化身行 → **不屈牌行** → 判定区 → 装备条（不屈牌代表"当前是否命悬一线"，优先级高于普通判定区，摆得更靠上更显眼）。直接复用 `.seat-bottom` 现成的"flex column 自然顶起"机制（判定区/装备条早已验证过能安全处理0~N行可变内容，不需要为不屈牌新起一套布局方案）和 `flex-wrap:wrap`（多张时自动换行，同一套判定区chip已验证过的机制）。
  - **展示内容**：每个 chip **只显示花色+点数**（如"♠5"），**不显示牌名**——判定区chip显示牌名是因为延时锦囊只有3种固定名字、长度可控，不屈牌可能是任意基本/普通牌、名字长度不可控，数量还可能多张，塞进小chip容易在窄屏挤爆；规则判定唯一需要的是点数，牌名放进 `title` 属性（hover可见），信息不丢失。
  - **视觉方案（红色系 + 不透明底衬，走"势力标识/装备条"确立的标准，不抄 `.dchip` 现成的半透明+text-shadow旧写法）**：新增 `.seat-buqu-row`（容器，`flex-wrap:wrap`）+ `.bchip`（每张牌，`background:#4a1414` 不透明深红、`border:1px solid var(--cinnabar-bright)`），文字直接复用 `seatEquipFace(card)`（装备条已经校准过的红/黑花色配色：红花色 `#ff6a4d`、黑花色 `var(--paper)`）——**这两个颜色原本是为装备条那层半透明深色渐变背景调的，这次用真实 WCAG 对比度公式重新计算过，在这个新的不透明 `#4a1414` 底上同样达标**（红花色 5.30、黑花色/白字 11.54，均远超 AA 的 4.5），所以直接复用，不新造一套颜色——保持全项目"红花色统一用这一个色值"的既有约定。字号走判定区 `.dchip` 同款的两档固定尺寸（base 9px / `.seat.me` 11px），不是装备条/化身行那套 `clamp(cqw)` 连续缩放，判定区这套简单两档尺寸本来就已经过验证能应对全部断点，不需要过度设计。
  - **门控条件**：和判定区同一原则，`g.started && p.buquCards.length>0` 才渲染，空数组/字段缺失（旧存档兼容）时整个容器完全不渲染，不留空占位，不需要额外的 `hasCap` 判断（`buquCards` 只有真的放置过不屈牌才会非空，天然只有周泰才可能命中）。
  - **测试**（`pw_buqu_test.js`，Playwright 真实浏览器驱动 `renderSeatCard`，37项，未落盘）：0/1/多张（3张）渲染正确性+多张时`flex-wrap`在最窄断点（80px容器）下不溢出；**三视角一致性**——三人局分别以周泰本人、另外两名玩家的 `mySeat` 渲染，断言三者都能看到完整的不屈牌chip（这条是最近刚修完 `normalize` 跨客户端视角污染那个系统性 bug 之后新加的检查，主动确认这次新增展示没有引入同类问题——不屈牌行的门控条件本来就完全不依赖 `mySeat`，所以这条从设计上就不会中招，测试只是验证这一点）；非周泰角色（`buquCards=[]`及字段缺失两种情况）座位卡渲染完全不受影响、不出现多余空容器；响应式——160/120/96/85px（覆盖全部既有 `@container` 断点档位）下不屈牌行都在座位卡边界内、且不与判定区/装备行/血量重叠；**WCAG对比度实测**（不是靠代码走查/公式推算，是真实渲染后用 `getComputedStyle` 读取实际生效的 `background-color`/`color`，逐像素级公式计算对比度）——黑花色11.54、红花色5.30，和实现前用 Node.js 脚本预先计算验证过的数值完全一致；**不透明性验证**——同一 chip 背景色在最亮（machao）/最暗（simayi）立绘背景下用 `getComputedStyle` 读取到的 `background-color` 完全相同（`rgb(74,20,20)`，恒定不变，证明真的不透明、不受立绘影响，和势力标识那次同一手法）；回归——判定区/装备条/血量等既有元素渲染不受影响、DOM顺序正确（化身行→不屈牌行→判定区→装备行）。`git stash` 验证：修复前（未新增功能的原始代码）在场景2（1张不屈牌应渲染出1个chip）精确复现 `count:0`，后续依赖该元素存在的断言直接 timeout 找不到元素——证实测试确实在测这次新加的功能，不是空转。既有全套回归（本轮累积全部29个 scratchpad 测试文件 + `run_cixiong_test.js`/`run_identity_mode_test.js`/`run_lidian_test.js`）全部重跑零回归。`?v=` 198→199。
- **修复贾诩【乱武】链条推进到最后一人时被 `normalize` 误判成脏数据、原地清空——只问了下家一个人，其余角色（尤其链条最后一人）从未真正被问到**。真实dump排查确认**这不是**此前"跨客户端视角污染"那批系统性 bug 的第12处——`normalize` 里 `luanwuChoose` 这条校验从头到尾没有任何 `d.xxx!==mySeat` 判断，三个不同 `mySeat` 视角结果完全一致，`chooseLuanwuOption`/`proceedToNextLuanwu` 内部推进逻辑本身也完全正确。**真正的根因是另一类、此前从未出现过的问题**：这条校验里 `d.remainingSeats.length===0` 被当作"脏数据"的判据之一，但 `remainingSeats` 变成空数组恰恰是 `proceedToNextLuanwu` 推进到"正在询问链条最后一个人"时的**合法中间态**（不是"数据损坏"，是"候选队列已经排到底"）——链条每问完一人都会经过一次 `tx()` 写路径（内部无条件先跑 `normalize`）和后续读路径（Firebase 推送触发的 `render(g)` 同样会跑 `normalize`），只要链条推进到最后一人，下一次任意客户端的 `normalize` 调用就会把这个合法状态当场清空，**必然复现"最后一人永远问不到"这个现象**，3人局（只剩1个"其他角色"可问）下最容易被注意到，因为这就是唯一一次推进、清空即等于"只问了下家"。修复：从这条校验里**只**移除 `d.remainingSeats.length===0` 这一个判据，`currentSeat`/`sourceSeat` 的类型与存活性校验、`Array.isArray(d.remainingSeats)` 校验原样保留——这些才是真正能标出脏数据的项。**和之前那批"跨客户端视角污染"bug 是两类不同性质的问题，不要混淆**：前者是"把依赖谁在看的判断混进了共享整形函数"，后者是"把某个字段的合法边界/中间态错当成了非法值"。**通用提醒（以后写 `normalize` 防御时对照）**：校验某个数组字段时，"长度为0"不能想当然等同于"数据损坏"——必须先想清楚这个字段在业务逻辑里是否存在"合法的空状态"（这里是"队列已经问到最后一人"），有的话就不能拿它当脏数据判据，需要用更精确的信号区分真损坏和合法边界（这里靠 `currentSeat`/`sourceSeat` 各自的类型与存活性就已经足够精确，删掉这条误判条件后不留任何防御缺口）。**顺带发现但当次未处理，已在后续单独任务中修复（见下一条）**：`chooseLuanwuOption`（响应函数本体）完全没有调用者身份守卫（不检查 `pending.currentSeat!==mySeat`），理论上任何客户端都能替该被问的人做选择。测试（`run_luanwu_test.js`，21项，未落盘）：核心场景（`remainingSeats:[]` 这个合法状态经三个不同 `mySeat` 独立跑 `normalize` 后应保留而非被清空）+ 端到端真实链路（3人局/4人局，模拟每一步响应后的真实 `tx`/Firebase 推送往返，验证最后一人真正被问到且能正常响应、链条正确收尾扣血）+ 回归（`currentSeat`/`sourceSeat` 指向死亡角色或类型错误、`remainingSeats` 非数组等真正的脏数据场景，确认仍被正确清空；`remainingSeats` 非空的正常中间态确认未受这次改动影响）。`git stash` 验证：修复前精确复现——`remainingSeats:[]` 状态经 `normalize` 后 `pending` 变为 `null`，且端到端场景在响应第二人时因为 `pending` 已被冲掉而直接抛出 `TypeError: Cannot read properties of null (reading 'type')`（这个崩溃本身就是"链条走不下去"的直接证据，和用户报告的"只问了下家一个人"完全对应）；修复后全部21项转绿。既有全套回归（本轮累积全部30个 scratchpad 测试文件 + `run_cixiong_test.js`/`run_identity_mode_test.js`/`run_lidian_test.js`）全部重跑零回归。`?v=` 199→200。
- **修复 `chooseLuanwuOption`（贾诩【乱武】）缺调用者身份守卫的真实漏洞——上一条乱武链条修复顺带发现、留到本条单独处理，和 `respondTiaoxinChoice`/`respondSanyao`/曹冲称象四个函数/`respondZhijiChoice` 是同一类缺陷，用同一套已验证过的修复方式**。`chooseLuanwuOption(option)` 原来只校验 `g.pending.type!=='luanwuChoose'`，从头到尾没有出现过 `mySeat`，任何在场客户端都能替 `g.pending.currentSeat` 对应的、当前该被问的那个人做选择（"使用杀"或"失去1点体力"）。**字段名核对无误**：读代码确认响应者是 `g.pending.currentSeat`（不是 `sourceSeat`——那是乱武发起者贾诩自己的座位，`sourceSeat` 本人同样不能代替被问的人做选择，两者是不同的角色）。**真实漏洞验证（真实 dump，越权座位=既不是 `currentSeat` 也不是 `sourceSeat` 的第三方）**：修复前 `hp` 分支——越权调用后被问角色的体力真的从4扣到3、`g.pending` 真的被推进到下一人；`sha` 分支——越权调用后被问角色手中的【杀】真的被打出（手牌数1→0）、`g.phase` 真的从 `luanwuChoose` 推进到 `respond`（进入完整的杀响应链）；连**发起者本人**（`sourceSeat`，同样不是 `currentSeat`）越权调用同样成功执行、体力照样被扣。**修复**：`phase`/`pending.type` 校验之后加一句 `if(g.pending.currentSeat!==mySeat) return g;`（`skills.js` 一处，一行）。**测试**（`run_luanwu_guard_test.js`，18项，未落盘）：核心1-3（第三方越权调用 hp/sha 两个分支、发起者本人越权调用，均应被拒绝，被问角色的体力/手牌/`g.pending`/`g.phase` 全部原封不动）+ 正对照1-2（`currentSeat` 本人正常调用两个分支，行为完全不受影响，含链条正确推进到最后一人、`normalize` 往返后保留、最终正确收尾扣血）。`git stash` 验证：修复前7项精确变红，且记录了具体被篡改的真实数值（体力4→3、手牌1→0、`phase` 变为 `respond`、`pending.currentSeat` 被越权推进到2）；正对照在修复前后保持一致（本人调用从未受过影响）。既有全套回归（本轮累积全部31个 scratchpad 测试文件，含上一条乱武链条修复的21项 + `run_normalize_myseat_test.js` 的43项 + `run_cixiong_test.js`/`run_identity_mode_test.js`/`run_lidian_test.js`）全部重跑零回归（`run_identity_mode_test.js` 复现过一次已知的、和这次改动无关的既有随机性 flaky 失败——见「四、已知的待优化点」`run_startgame_wiring_test.js` 场景4那条同类问题的记录，连续重跑8次确认8/8通过，非本次改动引入）。`normalize` 无需改。`?v=` 200→201。
- **修复翻面状态提示挡住需要响应的界面——上一次"翻面锁死自己当前回合"修复留下的遗留缺口**。上次修复把短路条件从"只要 `faceup===false` 就显示"改成"`faceup===false && !myTurn` 才显示"，但 `!myTurn` 覆盖了"当前不是我的回合"的绝大多数时间，**包括正好需要响应别人操作（比如对我出的杀，该不该出闪）的这些时刻**——这些响应界面全都定义在 `renderControls` 后面几千行的 `g.pending` 分派链条里，`renderFaceupStatus` 排在这条分派链条最前面提前短路，会把它们全部盖住。**官方规则澄清**：翻面只影响"轮到自己回合开始时被跳过"这一件事，完全不影响响应别人操作的正常询问，两者是不同的规则。**真实dump排查确认假设成立**：构造"翻面玩家A、非A回合、B对A使用杀"场景，真实调用 `renderControls(g)`（A视角），确认渲染出的是"你处于翻面状态…"提示而不是出闪/不闪的响应界面。**修复位置——不是函数最开头，也不是函数最末尾（两处都试过，都不对）**：①最初设想"挪到函数最末尾、和袁术【同疾】同一个 `c.innerHTML===''` 兜底位置"，但真实测试发现这是死代码——`renderControls` 内部有一个独立的 `if(!myTurn){ setBanner('等待…'); return; }` 早退分支（排在几千行 `g.pending.xxx===mySeat` 判断的最后面），任何真正空闲的非本回合玩家都会在这里直接 `return`，函数根本走不到末尾，我加在末尾的检查永远不会被执行到。②**正确的修复位置就是这个 `if(!myTurn)` 早退分支本身**——它排的位置天然就是这条分派链条确立的"真正空闲"边界：能走到这里，说明前面所有 `g.pending.xxx===mySeat` 判断都没命中过（真的没有任何 pending 需要这个玩家响应），不需要在这里逐一枚举 pending 类型/字段名去重新判断，直接复用分派链条本身已经做过的判断即可。改成：`if(!myTurn){ const faceupHtml=renderFaceupStatus(g); if(faceupHtml){ c.innerHTML=faceupHtml; return; } setBanner('等待…'); return; }`。是我自己当前回合（`myTurn===true`）时，这个分支根本不会进入，不受影响；有真实响应类 pending 时，更早的分支早已 `return`，同样走不到这里。**只改了 `render-controls.js` 两处（顶部移除早期短路+改写这一个早退分支），未涉及 `game.js` 状态机，未新增字段，`normalize` 无需改动**。**测试**（`run_faceup_pending_test.js`，8项，未落盘）：核心（翻面玩家A响应别人对A出的杀，应显示正常出闪/不闪界面，不是翻面提示）+ 补充（AOE/南蛮入侵响应场景同样不受翻面提示遮挡）+ 回归（真正空闲时依然正确显示翻面提示；正好轮到A自己回合开始，`startTurn` 按规则正确跳过并翻回正面，和这次UI改动无关未受影响；`myTurn===true` 时回合中途被翻面，上次修复的场景依然不受这次改动影响）。**测试过程中的一个方法论细节**：这批 vm 沙箱假 DOM 的 `appendChild` 不会同步更新 `.innerHTML` getter（只有直接字符串赋值 `el.innerHTML=xxx` 才会），而正常的响应按钮都是走 `c.appendChild(button)`——第一轮测试断言直接查 `c.innerHTML` 会因为这个 stub 限制而误报，改成参照 `run_wusheng_seat_test.js` 的既有做法，递归遍历 `_children` 收集内容（翻面提示走的是直接字符串赋值，两种检查方式都命中；响应按钮走的是 `appendChild`，只有递归遍历才能验证到）。`git stash` 验证：修复前3项精确变红（核心场景+AOE场景均显示翻面提示、遮挡了真实的响应按钮，和用户报告的"无法任何按钮显示"完全对应），回归场景在修复前后保持一致（未受影响）；恢复修复后全部8项转绿。既有全套回归（本轮累积全部32个 scratchpad 测试文件 + `run_cixiong_test.js`/`run_identity_mode_test.js`/`run_lidian_test.js`）全部重跑零回归。`?v=` 201→202。
- **排查【无懈可击】响应被翻面提示挡住的报告——真实dump未能复现，确认是已部署的通用修复、没有代码缺陷；顺带用真实dump证明修复对多种响应类型都通用，并补齐了这方面的正式回归测试**。用户提供截图（马超使用火攻,曹仁翻面+被问无懈可击）质疑上一条修复"是不是只覆盖了杀/闪这一种响应类型"。**先如实核实部署状态（未假设）**：`git log`+`git fetch` 确认修复提交 `e211f6e` 已经 commit 且已 push（`origin/main` 与本地 `HEAD` 完全一致，双向 `git log A..B --oneline` 均为空）；`gh api repos/.../pages` + `.../pages/builds` 确认该提交对应的 GitHub Pages 构建 `status:"built"`、`error.message:null`，部署成功，不是 CLAUDE.md 记录过的"构建失败、线上停留在旧版本"那种坑。**用真实dump精确复现截图场景（马超对某目标使用火攻,曹仁翻面且被问是否打出无懈可击）**：真实调用 `renderControls(g)`（曹仁视角），渲染结果是正确的"打出【无懈可击】/不出"响应界面，不是翻面提示——**未能复现**。追加5个变体（旁观者被问/自己是目标但无无懈可击/反制轮depth>0/曹仁兼具据守能力/曹仁是旁观者而非被问者）全部真实dump，同样全部正确，无一复现。**结构性核查证明修复本身确实通用，不是"只覆盖respond这一种类型"**：`renderFaceupStatus` 的唯一调用点在 `renderControls` 内部一个共用的 `if(!myTurn){...}` 早退分支（分派链条天然确立的"真正空闲"边界），逐一核对项目里全部约40种响应类 `g.pending` 类型（`respond`/`aoeResp`/`duel`/`wuxie`/`guicai`/`beigeChoose`/`beigeDiscard`/`beigeJudge`/`leijiChoose`/`jushouChoose`/`xuanfengPick` 等）各自的判断代码在 `render-controls.js` 里的行号位置，**全部**排在这个 `if(!myTurn)` 早退分支**之前**——这不是巧合，是这条分派链条本来的书写顺序决定的：真正走到这个早退分支，意味着前面所有响应类型的判断都没有命中，天然是通用的，不需要为每种类型单独打补丁。**顺带发现一个独立的、和这次翻面修复无关的既有小问题（未修复，如实记录）**：袁绍【乱击】(`luanjiChoose`/`luanjiConfirm`)、丁奉【短兵】(`duanbingChoose`)、丁奉【奋迅】(`fenxunDiscard`/`fenxunTarget`) 各自的"旁观者提示 banner"分支（`sourceSeat!==mySeat`/`seat!==mySeat`，注释明写"观察者界面（其他玩家发动XX时）"）被嵌套在 `else if(g.phase==='play')` 内部，而这个大分支本身只有 `myTurn===true` 才能到达——但这几个技能是"技能拥有者在自己回合内发动"的场景，旁观者（非当前回合玩家）理应看到"XX 正在选择【乱击】的牌…"这类具体提示，实际却因为位置排在 `if(!myTurn)` 早退分支之后，永远走不到，只能看到笼统的"等待 XX 行动…"。**这是纯文案精细度问题，不是本次任务范围**：这几个技能本身不需要旁观者点击任何按钮（只是观战提示不够具体），且这个位置关系在这次翻面修复之前就已经是这样——旧版本的翻面短路条件在这类场景下同样落到同一个 `if(!myTurn)` 分支、表现完全一致，不是被这次改动引入或暴露的新问题，未处理。**新增覆盖6种响应类型+反制轮的正式回归测试**（`run_faceup_multi_response_test.js`，13项，未落盘）：核心（用户截图场景本身）+ 回归（杀/闪，上次已测过的场景）+ 补充（南蛮/万箭 `aoeResp`、蔡文姬悲歌 `beigeChoose`、张角雷击 `leijiChoose`、曹仁据守 `jushouChoose`、无懈可击反制轮 `depth>0`）+ 回归（真正空闲显示翻面提示、正好轮到自己回合正确跳过并翻回正面）。**这次没有代码要改，但测试仍按"能变红转绿"的要求验证过有效性**：由于本次没有发现需要修复的代码，没有可 `git stash` 的改动——改用等价方法，临时（未提交）把 `render-controls.js` 改回历史上的错误模式（在 `myTurn` 算出后立即无条件短路：`faceup&&!myTurn` 就显示翻面提示，不检查任何 pending），重跑这份新测试，9/11 个"响应场景不该被挡住"的断言精确变红（另外2个不变红是因为测试数据本身用的是"曹仁自己回合内发动据守"这个场景——据守官方规则就是在技能拥有者自己回合结束时发动，`myTurn` 恒为真，这个场景从设计上就不会被"仅在 `!myTurn` 时短路"的旧错误模式命中，不是测试或修复的缺陷）；空闲/自己回合两个回归场景在错误模式下依然保持通过（这两个场景确实不该受影响）。验证完立即用备份恢复原文件，`git status`/`git diff` 确认零残留改动。既有全套回归（本轮累积全部33个 scratchpad 测试文件）全部重跑零回归。**本次未发现代码缺陷、未做任何代码改动，`?v=` 保持 202 不变**——最可能的解释是用户浏览器有未刷新的旧标签页（该提交部署前就已打开、之后未重新加载），已如实告知用户结论。
- **审查外部贡献者（`Nami929`，`bobo_dev`分支 PR#31）的8个commit（`919798a`）——结论:质量好、无功能回归、可以信任，额外独立发现并修复了一处真实的既有bug**。逐个commit真实diff审查+独立真实dump复核（不是只看commit message），覆盖四块：①**翻面响应**——只改了 `renderFaceupStatus` 的提示文案（"翻面状态"→"背面状态"，补充"回合外仍可正常响应"这句，印证理解并认可了此前的修复），`if(!myTurn)` 早退分支的结构、调用位置完全未动；另外补上了 `庞统【涅槃】`/`finishGeneralAssign`/`newGame` 三处此前遗漏的 `p.faceup=true` 显式重置（独立的初始化bug，和分派逻辑无关）。②**法正眩惑**——`startHuanhuo`等六个函数从 `window.mySeat`/`window.g` 正确改成了 `tx(g=>{...})`+顶层 `mySeat` 写法（正是本项目「四、已知的待优化点」记录过的"法正【眩惑】本体损坏"那条的修复方式），补齐了调用者身份守卫（`sourceSeat!==mySeat`同款判断），且不再信任发起时的旧手牌快照、改成实时校验。**额外独立发现并修复一处和这批修复主线无关的真实bug**：华佗【青囊】给持有【恩怨】能力的目标（法正）回血时，`const targetSeat=g.players.findIndex(...)` 在同一个箭头函数作用域内和外层参数 `targetSeat` 重名——这不是语法错误（是合法的闭包变量遮蔽），但会导致*更早一行*对 `targetSeat` 的引用落入这个局部声明的暂时性死区（TDZ），产生 `ReferenceError: Cannot access 'targetSeat' before initialization`。用真实dump确认：**这个bug在这批提交之前（`4b951b6`）就已经存在**，和这条对话线任何工作无关；旧代码上青囊治疗法正必崩，新代码（重命名成 `resolvedTargetSeat` 且提到两处引用之前）正常工作。③**凌统旋风**——把交互模型从"选目标+输入弃牌数量、随机弃"整体重写成"选目标+逐张指定弃哪张牌（手牌随机/装备判定区可指定）"，`render-controls.js` 同步重写；新增的一处 `normalize` 防御（`xuanfengPick` 刚触发时 `targets`/`discardedCounts` 是空数组，Firebase 不保留空数组导致同步后字段缺失被误判成脏数据）和贾诩乱武那次 `remainingSeats.length===0` 是同一类"空数组≠脏数据"问题，独立发现独立修复；`respondXiaoguoChoice`（骁果自己的弃装备选项）新增了这条对话线已经反复用过7次的 `pendingBefore`/`resume` 续接范式的**第8个独立触发点**，用真实dump确认正确工作。旧的 `chooseXuanfengDiscardCount` 函数因为交互模型重写变成死代码（`pending.stage!=='chooseCount'` 守卫永远拒绝，真实UI已经不会再让 `stage` 变成这个值），保留在代码里无害但属于技术债，未清理（不在本次审查范围）。④用当前真实API（`pickXuanfengTarget`→`pickXuanfengCard`→`finishXuanfengSelection`）独立重新驱动了"庞德青龙偃月刀+猛进拆凌统装备→旋风→续接询问是否再来一杀"这条完整链路，确认续接机制在新交互模型下完全正确，不是被这批提交破坏的。**新增的 `test_fazheng.js`/`test_xuanfeng.js` 是真实dump风格**（直接调用真实函数、断言真实状态变化，其中两处显式用 `_g=JSON.parse(JSON.stringify(_g))` 模拟真实Firebase序列化往返再调用 `normalize`——和本项目"多客户端normalize测试法"同一纪律），但提交时**没有配套的可执行harness**（这个仓库当时没有 `package.json`/`.mocharc`，直接 `node test_fazheng.js` 报 `describe is not defined`）——本次收尾时已补齐（见下）。整体回归：**这条对话线积累的全部33个scratchpad测试文件+3个既有仓库测试文件（`run_cixiong_test.js`/`run_identity_mode_test.js`/`run_lidian_test.js`）在这批提交上完整重跑，6个scratchpad文件因为调用了新交互模型下的死代码 `chooseXuanfengDiscardCount` 而失败**（详见下一条清理记录），**其余全部零回归**。
- **清理审查中发现的两处遗留问题：6个scratchpad测试文件改用旋风新API重新驱动，2个新增测试文件补齐可执行harness**。①`run_mengjin_shaoffset_test.js`/`run_qiaomeng_lieren_test.js`/`run_xiaoguo_finishturn_test.js`/`run_xuanfeng_guanshi_test.js`/`run_xuanfeng_resume_test.js`/`run_xuanfeng_test.js` 里所有 `chooseXuanfengDiscardCount(N)` 调用点，逐一核对每个场景里旋风目标的真实持牌构成（纯手牌2张/纯手牌1张/仅1件装备无手牌），换成对应的真实API序列——**手牌2张**：`pickXuanfengTarget(seat); pickXuanfengCard('hand'); pickXuanfengTarget(seat); pickXuanfengCard('hand');`（每次 `pickXuanfengCard` 后 `stage` 变回 `'selecting'`，`pending.targets` 现在恒为空数组不再拦第二次选择，必须为每一张牌重新 `pickXuanfengTarget`；`maxRemaining` 减到0时 `executeXuanfeng` 自动触发，不需要额外调用）；**只有1件可弃项（手牌或装备）**：`pickXuanfengTarget(seat); pickXuanfengCard('hand'|'equip',...); finishXuanfengSelection();`（`maxRemaining` 减到1不会自动触发，必须显式调用 `finishXuanfengSelection`——这正是这批提交里"至多两张,允许只弃一张后主动结束"那个UX修复要解决的场景）。**不是简单把断言改绿，是用当前真实API重新驱动了每个场景要验证的完整链路**（贯石斧强制命中续接/骁果续接/青龙偃月刀再来一杀续接/趫猛烈刃onLoseEquip等），逐个真实dump验证行为和替换前预期完全一致后才定稿。全部6个文件+其余27个scratchpad文件+3个仓库测试文件+新增的2个harness共计**38个测试文件全部重跑零失败**。②给 `test_fazheng.js`/`test_xuanfeng.js` 各配一个仓库根目录下的 `run_fazheng_test.js`/`run_xuanfeng_test.js`（**复用本仓库已有的 `run_lidian_test.js` 既定约定**——`describe`/`it`/`assert` 注入+加载 `config/data/weapons/room-lifecycle/game/skills.js`+在同一个vm上下文里执行目标测试文件，不是发明新的harness风格）：唯一的行为差异是 `it()` 失败后不再 `throw` 中断整份文件、而是记录下来继续跑完剩余用例，最后统一报告PASS/FAIL计数（比 `run_lidian_test.js` 那种"失败即中断"更贴近真实mocha的完整报告，且不影响现有 `run_lidian_test.js` 本身）。两个新harness各自用真实mutation验证过鉴别力（分别临时重新引入①里那个真实TDZ bug、和临时打断 `normalize` 的空数组防御，确认能精确捕获）后复原、`git status`确认零残留改动。`run_fazheng_test.js` 6/6全绿，`run_xuanfeng_test.js` 5/5全绿——注意这两个仓库级harness用的是这个repo既有的、更朴素的沙箱构造方式（不像本次审查阶段那个一次性诊断脚本那样把Node主realm的 `Array` 注入vm上下文），所以之前诊断阶段发现的那个"跨realm `Array.from` 和字面量数组比较不相等"的现象在这里天然不出现，不需要额外处理。

- **favicon 从印刷体升级成真小篆体**（上一条记录的 SVG 印刷体版本已被本条完全取代并删除）：小篆本身没有"杀"（简体）这个字——小篆是秦代规范字体，早于近代简化字，真正有小篆写法的是"殺"（繁体，杀的本字），二者含义完全相同、只是繁简对应，常见于印章/复古设计场景。这条是用户明确选定的方案（另一选项是退回印刷体"杀"，已展示双方真实渲染预览后由用户选择）。**字体来源**：中研院资讯科学研究所"崇羲篆體"项目（`https://xiaoxue.iis.sinica.edu.tw/chongxi/`，以《说文解字》为依据，覆盖11596字），**CC-BY-ND-3.0-TW 授权**——商业使用允许、需署名、禁止改作字体本身；本项目只用它**渲染出图片**（不修改、不重新分发字体文件本身，字体文件不进仓库，只有渲染结果 PNG 提交），符合"禁止改作"的限制。**技术核实,不是凭印象**：字体是纯二进制文件，为确认它是否真的覆盖"殺"/"杀"两个字，手写了一个不依赖 Pillow/fonttools（环境没有 pip）的最小 OTF cmap 解析器（直接按 sfnt/cmap 二进制格式读 format 4/12 子表），核实结果：**有"殺"(U+6BBA)的字形，没有"杀"(U+6740)的字形**——用 Chromium 真实渲染验证过这个结论（"殺"渲染出笔画繁复的真小篆，"杀"静默回退成普通无衬线字体，不是破损方框）。**实现**：`@font-face` 引入本地字体文件（只在生成图片这一步临时使用，不随项目分发）+ CSS 还原 `.seal` 徽章视觉（红底圆角方块+浅色描边环），用 Chromium 无头浏览器渲染成图。**踩过一个坑**：CSS `font-size:62%` 在这类容器里是相对**默认16px根字号**、不是相对视口尺寸，导致字符渲染得极小且位置诡异；改用 `vmin` 单位（相对视口宽高较小值）后才正确按目标图标尺寸缩放。**已知的清晰度取舍（如实记录,不回避）**：小篆本身笔画纤细繁复，在32px以上能清楚看出书法感，但缩到16px（多数浏览器标签页实际显示尺寸）会糊成看不清具体笔画的一个色块轮廓——这是小篆这种字体本身的特性，不是渲染出了问题；为了让浏览器缩放时尽量清晰，主图标用256×256渲染（96px视口+2倍设备像素比），比直接原生渲染16/32px更平滑。**最终文件**：`favicon.png`（256×256，主用）+`favicon-180.png`（`apple-touch-icon`）；`<head>` 精简为两行 `<link>`（上一版还带的 `favicon.svg` 已删除——SVG 版本用的是印刷体"杀"，字符和现在的小篆"殺"不一致，且没有实际能力把这个特殊字体转成矢量路径嵌入SVG，保留会造成"两种图标显示不同字"的混乱，索性只留PNG）。`?v=` 209→210。
  - **后续两轮加粗加大调整**：这款字体只有 Regular 一个字重（读 `name` 表核实过，没有 Bold/Black 变体），用 CSS `-webkit-text-stroke` 给描边加粗+调大 `font-size` 占比来模拟"更粗更大"，不是换字体。**第一轮**（字号从约64%画布占比调到约82%，描边加到1.8vmin）：16×16 下从"稀疏浅色斑点"变成"清晰的一整块实心浅色标记"，但仍不到能读出具体字的程度。**第二轮（用户明确要求"推到极限"后确认，曾短暂上线）**：字号推到约90%画布占比，描边加到3.2vmin——试过从64%/0描边到96%/5.5vmin描边的完整梯度并逐档截图对比过（含"放大12倍看16×16实际像素"这种验证方式，不只是看原始大图），96%+5.5vmin 那一档会让笔画之间彻底糊连、整个字变成一坨看不出内部结构的实心块，讨论后回退到 90%/3.2vmin 这一档——32×32 下能清楚看出"X"交叉、右侧偏旁、两条竖笔的字形结构；16×16 下是"上方一团、下方两条腿"的清晰饱满标记，比第一轮更醒目，但仍然如实认："16×16 下认不出具体是哪个字"是任何笔画数这么多的汉字在256像素预算内的物理极限，不是调参没调到位，继续加粗只会让16×16更接近纯色块而不会让"认字"变得可能。`?v=` 211→212。**第三轮（当前最终版，用户反馈第二轮"糊连得有点过头"，要求回调到第一轮和第二轮之间）**：在"字号82%/描边1.8vmin"（第一轮展示过但从未上线的候选）和"字号90%/描边3.2vmin"（第二轮，当时已上线）这两个参照点之间取算术中点——字号86%、描边2.5vmin。**验证方式**：把两个参照点的256px渲染裁剪放大、对齐显示右侧偏旁这个笔画最密集的区域逐档对比，确认82/1.8和86/2.5两档的笔画之间都还留有清晰间隙，88/2.8开始在顶部出现轻微粘连迹象，90/3.2（第二轮）则有多处明显糊连——最终选定的86/2.5这一档，32×32下能看清笔画间隙（不再是实心块），16×16下依然是饱满清晰的整体标记，比第一轮更醒目、又不像第二轮那样糊连。`?v=` 212→214（其中213是拉取 Nami929 两次法正眩惑修复提交时各自产生的中间版本号，非本次改动引入）。**第四轮（当前最终版，第三轮基础上再小幅回调一步）**：字号86%→85%、描边2.5vmin→2.2vmin——比第三轮再细一点点，幅度刻意控制得很小（不是重新在"太细/太粗"两个参照点之间取一次新的中点，就是在第三轮基础上单向微调），32×32下笔画间隙比第三轮略清楚，16×16依然是饱满完整的整体标记。`?v=` 214→215。

