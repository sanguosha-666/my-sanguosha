- **CORE-115（issue #115）新增身份标记功能：玩家可自行标注座位卡中心的忠/反/内猜测标记（2026-08-16, issue-fixes-9 分支）**：issue 正文里已有四点确认过的设计决定（用户明确要求"按已确认方案实现,不重新讨论"）——①数据存 `localStorage`，按房间号+座位号做 key，不写入 `g`；②座位卡新增独立小图标触发菜单，不复用/不干扰整卡 `onclick` 的出牌选目标行为；③仅 `gameMode==='identity'` 时显示；④随 `newGame()` 清空。实现全程未偏离这四点，也没有遇到"技术障碍无法执行"的情况需要中途同步。**存储层（`render.js`，renderSeatCard 之前）**：`identityMarkKey(seat)` 返回 `identityMark:<roomId>:<seat>`；`getIdentityMark(seat)`/`setIdentityMark(seat,mark)` 读写，`mark` 不在 `IDENTITY_MARK_OPTIONS`（`['zhong','fan','nei']`，故意不含 `'zhu'`——主公身份从不隐藏，不需要猜）时一律视为"清除"（覆盖手改 localStorage 塞进非法值的脏数据场景）；`clearAllIdentityMarks()` 按 `identityMark:<roomId>:` 前缀扫描 `localStorage` 全部 key 逐个删除，不依赖固定座位数上限。全部三个函数都套 `try/catch`——隐私模式/localStorage 被禁用时静默放弃，不影响主流程（和 `game.js:17` 的 `sgsClientId` 现有客户端本地持久化写法同一套防御纪律）。**交互层**：`openIdentityMarkMenu(seat)` 复用 `#confirmModal` 容器但不走 `showConfirm` 固定的两按钮语义，自渲染"忠/反/内/清除标记/取消"五个按钮——和 `confirmOwnOrSha`（关羽武圣二选一弹窗）同一套"自渲染多按钮复用同一容器"写法，不新造弹窗组件。点击选项后 `setIdentityMark` + `render(currentG)` 立即重绘。**渲染层（`renderSeatCard`）**：新增 `identityMarkEntry`——`g.gameMode==='identity' && g.started` 时才生成（大厅/选将阶段还没真的在玩，不渲染；乱斗/组队模式没有"忠反内"概念，不渲染），未标记时显示 🔖 图标（`opacity:.55`，降低视觉权重、不抢镜，但仍可发现可点），已标记时显示"忠/反/内"单字（`opacity:1`）；`onclick="event.stopPropagation();openIdentityMarkMenu(seat)"`——和武将说明"?"角标/装备条/判定区角标同一套 `stopPropagation` 写法，不需要在 `buildSeatDOM`（render.js，出牌选目标那段逻辑）额外做任何特殊处理，两套点击天然互不干扰。**放置位置（"座位卡中心"，issue 明确要求实测确认不冲突）**：`top:58%;left:50%;transform:translate(-50%,-50%)`——不是严格的 50%,50% 正中心，因为那个精确坐标已经被 `.flipped::after`（"翻面"文字标签）占用，58% 这个位置和它、以及罕见的 `.seat-autopilot-badge`（`top:30%` 附近，托管中才出现）都留出了间隙；这三者理论上仍可能同时出现（翻面+托管+已标记），但都是各自独立的低频/瞬时状态，不追求把全部组合情况都错开——和 `.flipped::after` 当初的取舍原则一致，不是遗漏。**样式**：不透明背景 `#1a1410`（和 `.info-badge` 同一色值，已在此前多轮微调中验证过"不透明底衬、对比度不随立绘漂移"）+ `2px dashed var(--gold)` 边框（虚线，用来和右上角"真实身份"角标的实心色块视觉区分——这是玩家自己的猜测，不是确认过的身份，边框风格上要一眼看出"这不是权威信息"）+ `var(--paper)` 文字；尺寸走 `clamp(18px,16cqw,30px)` 自适应卡片宽度（装备条同款做法，不逐个 `@container`/`@media` 断点手写）。**`newGame()` 挂钩（`room-lifecycle.js`）**：在函数最开头、`tx(...)` 之前无条件调用 `clearAllIdentityMarks()`——和同一函数里已有的 `resetBotTwoStep()`（CORE-113）完全同一个写法（`typeof` 守卫 + 函数定义在加载顺序更靠后的文件里，但 `newGame()` 只在用户点击"再来一局"、即全部脚本早已加载完成后才被调用，`typeof` 守卫只是防御性写法，不是真的会遇到未定义）。issue 只要求 `newGame()` 清空，未涉及 `backToLobby()`，本次严格按此范围，未额外扩展。**可信度补充说明——视觉验证方法（issue 明确要求程序化验证，不能只靠肉眼看几张截图）**：本次开发环境（node vm 沙箱测试）没有可用的浏览器渲染环境——`headless chromium` 依赖的系统共享库（`libnspr4.so` 等）缺失，`npx playwright install-deps` 需要 sudo 密码但当次会话不可获得，尝试后确认真的不可用，不是图省事跳过。但这次的元素在设计上从一开始就规避了"必须截图才能验证"的必要性：CLAUDE.md"视觉验证必须挑最刁钻样本"这条教训，历史上两次真实踩坑（血量胶囊/装备条）的根因都是**半透明**底衬，导致有效对比度随立绘明暗漂移（最亮 machao/最暗 simayi 给出不同结果）——这次的 `.seat-identity-mark` 从设计上就没有这个自由度可踩：`background:#1a1410` 是不透明纯色（`alpha=1`，直接复用 `.info-badge` 已验证过的同一色值），不透明意味着背后立绘像素被 100% 遮盖，对比度在数学上纯粹是前景色/背景色两个常量的函数、与立绘完全无关——截图验证在这种情况下不会比直接计算更可信，只是更慢更间接。所以测试改用**程序化 WCAG 2.1 相对亮度公式计算**：从 `index.html` 源码里正则提取 `.seat-identity-mark` 规则的真实 `background` 声明（同时断言它确实是不透明六位十六进制、不是 `rgba(...,<1)`——防止以后有人悄悄改成半透明却没人发现）、从 `:root` 提取 `--paper`/`--gold` 的真实定义值（不手抄字面量，源码改颜色这条测试会跟着改，不会变成假绿），跑标准 WCAG 相对亮度公式算出对比度——文字 14.04、边框 5.88，均远超 AA 门槛 4.5；另外用一组真实会挂科的低对比度组合（两个相近的暗色）反向验证 `contrastRatio` 这个函数本身确实有鉴别力，不是无论如何都返回大数字的摆设（CLAUDE.md 第 20 条"断言必须真的会红"同一条原则，这次用在验证公式本身而不是业务断言上）。**测试**：新增 `testclass/run_identity_mark_test.js`——功能测试 17 项（localStorage 增删改查 7 项、点击交互独立性 3 项、仅 identity+已开局才渲染 5 项含"标记本身也不因模式切换泄露"、`newGame()` 清空 2 项含"只清当前房间不影响别的房间残留"）+ 对比度验证 5 项，共 **22 项**，修复前逐条验证会红（`getIdentityMark is not defined`/`setIdentityMark is not defined`/`openIdentityMarkMenu is not defined`/`未渲染标记入口`/`未找到.seat-identity-mark规则` 等 14 项功能测试 + 2 项对比度测试确认会红），修复后 22/22 全过。全量回归 `run_all_tests.js` **91/91 全过**（90+1）。`index.html`：`room-lifecycle.js?v=403→404`、`render.js?v=403→404`。
- **CORE-79（issue #124）制定测试范围判断规则：全量测试套件应改为按改动风险分级判断，追加 CLAUDE.md 第 29 条（2026-08-16, issue-fixes-10 分支）**：此前处理 issue 时默认无差别每次都跑 `run_all_tests.js` 全量套件，即使像 CORE-115（身份标记，纯 UI、`localStorage` 存储、零修改既有函数）这种影响面明显很小的改动也照跑，用户要求制定一套具体可执行的判断规则取代"无差别全量"。这条纯粹是文档变更（CLAUDE.md「三、改动原则」新增第 29 条），不涉及代码、不需要跑测试，也不需要 `?v=` cache-bust（不影响任何前端加载文件）。**规则核心结构**：两条独立触发轴，任一命中即必须跑全量——**轴 A**（改动是否修改了"被多处共用"的既有函数体，用 grep 数调用点/共享表引用判断，不含纯新增函数）、**轴 B**（是否触碰"高连带风险子系统"清单：机器人决策核心分发机制 `BOT_DECISIONS`/`BOT_SEAT_PICKS`/`BOT_PHASE_ACTOR`/`scheduleBotTurn` 等、`pending` 状态流转与 `normalize` 校验逻辑、soak.js 压测覆盖范围内的核心公共入口如 `dealDamage`/`canTarget`/`playCard`、`normalize`/`tx`/`check_cache_bust.js`/`run_all_tests.js` 本身、`index.html` 脚本加载顺序，不看调用点数量、碰到即命中）。两轴都不命中且同时满足"只新增不修改既有函数体"/"不碰轴B清单"/"不写入g核心结算路径"三个条件时，才允许只跑最小相关测试集。**规则里每一类判据都配了本项目真实发生过的 CORE 编号案例作为锚点**（不是凭空制定）：轴 A 用 CORE-97/98/99/102（顺手牵羊/过河拆桥/借刀杀人/火攻的目标校验缺陷，看似独立实际都触碰了共享的 `canTarget`）；轴 B 的机器人分发子系统用 CORE-113（`seatPickLocalFallback` 一处缺陷覆盖 `BOT_SEAT_PICKS` 表全部技能）和 CORE-116（`botTwoStepA` 自我触发缺口覆盖借刀/离间/丈八/仁德四个技能）；轴 B 的 pending/normalize 子系统用 CORE-25 类与 CORE-112/113/114 那一串"链结束忘记显式清空 `g.pending`"；"可以只跑最小集"这一档用 CORE-115（本身的开发过程）作为示例锚点。**最小测试集的组成有明确定义，避免被简化成"跑一下新写的测试文件就算了"**：必须是「改动新增的专属测试文件」∪「对改动触碰到的每一个已存在的函数名，在 `testclass/*.js` 里 grep 出的全部既有测试文件」的并集——这条直接来自处理 CORE-115 时的真实操作（当时先 grep 了全部 `testclass/*.js` 里引用 `newGame(` 的既有测试文件，逐一确认无回归，而不是只跑新写的 `run_identity_mark_test.js`），这次把这个隐性操作步骤显式钉成规则条款，防止以后有人跳过这一步、想当然地假设"新写的测试就够了"。**默认原则**：拿不准时跑全量，理由不是空泛的"保守稳妥"，而是明确写了成本对比——全量套件是分钟级开销，漏测一次连带影响（尤其是 soak.js 那几轮压测反复暴露过的、跨技能/跨座位的间歇性卡死类问题）的排查成本历史上远高于此。**编号说明**：本次创建 issue 前，用户特意要求重新完整拉取全部 91 条 issue（open+closed）、逐条扫描正文里的 `编号：CORE-NN` 字段确认实际最大值——发现 CORE 编号序列并不连续（CORE-1~71 → CORE-108~116 → CORE-72~78，其中后两段是不同时间段创建、编号交错），扫描确认的最大值是 CORE-116，中间 CORE-79~107 有一段历史缺口从未使用；用户最终选择用 CORE-79 填补这段缺口（而不是严格取"最大值+1"的 CORE-117，也不是简单套用即将自动生成的 GitHub issue 编号 #124），这是用户的主动编号选择，如实记录，不代表"最大值+1"这条取号惯例本身被推翻——以后仍应先扫描确认最大值，是否填补历史缺口由用户决定。
- **CORE-77（issue #122）对局确定性重放第一期：开局记录seed + tx()处动作命令采集（2026-08-16, issue-fixes-11 分支）**：issue 本身明确分三期，本次严格限定在第一期（"开局记录seed + 动作命令采集"），未提前做第二期（随机数种子化，替换 `Math.random()`）或第三期（重放器/整局压测接入）。用户明确要求：这次改动了全项目每次状态变更的必经路径 `tx(fn,onCommitted)` 本体，按刚制定的 CORE-124 测试范围判断规则（CLAUDE.md 第 29 条），轴 A（改了被多处共用的既有函数体，`tx` 调用点数以百计）和轴 B（`tx(fn)` 的封装逻辑本身就在轴 B 高连带风险清单里）都命中，必须跑全量 `run_all_tests.js`，不适用最小测试集——已按此执行。**实现**：①`g.seed`——新增 `generateSeed()`（game.js），在唯一开局收敛点 `finishGeneralAssign(g)`（room-lifecycle.js，random/pick/身份局三种模式都经过这里）生成并写入 `g.seed`。②`commandLog`——新增模块级纯本地内存数组（game.js，刻意不挂在 `g` 上、不写 Firebase，和 `aiSuspicionEvents`/`aiDecisionRecords` 同一类"本地诊断数据不进共享状态"的既有写法），每次 `tx()` 调用对应一条记录（`seq`/`ts`/`actingSeat`/`commandName`/`phaseAtStart`/`pendingTypeAtStart`），封顶 `COMMAND_LOG_MAX=500` 防止长局无限增长（和 `aiSuspicionEvents` 上限20/`recentLog` 上限15 同一类惯例）。`commandName` 用 `captureCommandName()` 从调用栈自动解析"谁调用了 tx()"（`new Error().stack` 取第4行、正则提取函数名），不需要在全项目上百个 `tx()` 调用点逐一手工传参——项目"多文件无构建流程、从不做代码压缩/混淆"这一约定（CLAUDE.md 一直如此）使得函数名在生产环境同样保留，栈解析可靠。**已知的、本期刻意不解决的局限（如实记录，不假装已完成）**：`commandName` 只是"哪个函数发起了这次 tx"，不含具体参数（打了第几张牌/指定了哪个目标）——真正做到"seed+命令序列可精确重演"需要参数级别的命令记录，是第二期"重放器"要解决的问题。**"零行为变化"声明的关键取舍**：`generateSeed()` 最初实现用了 `Math.random()`，实测发现这会让开局后全部既有随机调用点（`shuffleSeats`/`buildDeck` 等）在真实未播种的 `Math.random()` 流水线里整体错位一位——即使规则逻辑没变，给定同一串真实操作，具体谁摸到哪张牌也会和没有这次改动时不同，这在严格意义上违反了 issue 自己声明的"行为零变化"。改为 `Date.now() ^ Math.imul(单调自增计数器, 0x9E3779B1)` 的异或组合，完全不触碰 `Math.random()`，用"零行为变化"回归测试实测确认了这一点确实成立（且用反向实验故意改回 `Math.random()` 版本验证了该测试会正确报红，不是摆设）。**"零行为变化"验证方法**：动手改代码前先用 `git show HEAD` 快照了改动前的 `game.js`/`room-lifecycle.js`（`testclass/fixtures/core77_pre_change_*.js.snapshot`），新增 `testclass/run_core77_replay_infra_test.js`，同时加载"改动前快照+改动后当前文件（其余文件不变，两边共用同一份 render.js/bot.js/skills.js 等）"两份完整引擎到两个独立 vm 沙箱，在两边注入**同一个确定性 seeded Math.random（mulberry32，测试专用、不是 shipped 代码）**，用 soak.js 同款驱动循环（`botSeatForState`→`runBotDecision`→逃生舱兜底）跑同一个 seed+人数+步数上限的真实随机对局，对比两边最终 `g` 状态（剔除本次新增的 `g.seed` 字段）是否逐字节一致。两个场景（3人局400步/5人局600步）均逐字节一致；另有一条"比对方法本身有鉴别力"的反向验证（不同 seed 驱动出的两局，最终状态确实被判定为不同），以及前述"故意让 `generateSeed` 消耗 `Math.random()`"的破坏性实验（确认会被正确抓出：两个场景步数分别从40/190分叉到83/196，断言正确报红）——这套验证方法本身也经过了"真的会红"的检验（CLAUDE.md 第20条），不是自证式的摆设。**测试**：`run_core77_replay_infra_test.js` 共12项（单元断言9项：`generateSeed` 类型/不重复/不消耗Math.random、`finishGeneralAssign` 写入 `g.seed`、`commandLog` 增删改查含"同一次外部tx()调用内多次内部重试只覆写同一条记录、不重复push"、封顶截断；零行为变化2项+鉴别力反向验证1项），12/12全过，修复前（对照改动前快照）确认 `generateSeed`/`commandLog` 等标识符均不存在（会抛 `ReferenceError`）。全量回归时发现一个既有测试的连带影响：`run_timeout_universal_test.js` 用正则从 `game.js` 里单独提取 `tx()` 函数体、加载进一个不含完整 game.js 的最小 context 单独测试超时逻辑，我新增的 `commandLog`/`commandLogSeq`/`COMMAND_LOG_MAX`/`captureCommandName` 在这个最小 context 里未定义，导致 `ReferenceError`——补上和该文件已有的 `normalize`/`pruneExchangeCards`/`stripUndefined` 等同一类的最简单桩（`commandLog:[]`/`commandLogSeq:0`/`COMMAND_LOG_MAX:500`/`captureCommandName:function(){return null;}`）修复，这个测试关心的是超时相关逻辑本身，不需要真实的命令日志行为。全量 `run_all_tests.js` 连续跑 3 次均 **92/92 全过**（91+1，含 cache-bust 检查）。`index.html`：`room-lifecycle.js?v=404→405`、`game.js?v=425→426`。**#123（CORE-78，技能系统数据驱动化）本次刻意不做**，留给下次单独会话专门处理——已和用户确认，两条改动都触碰同一批中枢函数（`tx()`/`normalize()`），一次会话内同时推进违反"一次只改一件事"，且 #123 要做到验收标准要求的"覆盖现有全部技能"本身是一次不小的全仓库审计工作，值得单独一次会话完整做。
- **CORE-78（issue #123）技能系统数据驱动化第一期：建立技能注册表骨架，108 条技能全量登记 + 9 条交叉验证（2026-08-16, issue-fixes-12 分支）**：issue 明确分三期，本次严格限定第一期（"建立技能注册表骨架，只登记不改行为"），未提前做第二期（`normalize()` 的 pending 校验分支改查表）/第三期（`BOT_DECISIONS`/超时保守表按注册表声明）。**动手前先做了全仓库审计并与用户确认了三件事**：①注册表用**单文件 `skills/skill-registry.js` + 中文字段名**，不按 issue 原文拆 107 个独立文件——本项目是无构建流程、全靠 `index.html` 里 `<script src>` 顺序加载的架构，107 个独立文件意味着同样数量的 `<script>` 标签，加载顺序维护成本和首屏请求数都不可接受；这是对 issue 原文的偏离，特意提出确认而非擅自更改。②`CARD_PLAYS`(36)/`EQUIPS`(19) 不纳入本期（issue 讲的是武将技能）。③字段设计与 9 条交叉验证规则。**规模**：66 武将 / 107 个 `GENERALS.skill` 技能名 / 99 种 caps（101 条声明）/ 2 种 hooks（8 条声明）/ 120 个 `STAGE_TABLE` 阶段 / 33 个 `BOT_DECISIONS` / 12 个 `BOT_SEAT_PICKS`。**最终登记 108 条**（107 + 补登记 `simayi/鬼才` 一条，见下）。**审计中发现的两处 `GENERALS` 数据缺口（本表按真实实现登记，未改 data.js）**：①`simayi`（司马懿）的 `GENERALS.skill` 只写了"反馈"、漏了"鬼才"，但 `desc` 原文和 `HUASHEN_SKILL_TABLE` 都有"鬼才"、`caps` 里也确实声明了 `guicai`——注册表补登记为独立一条，测试里以显式豁免集合 `SKILL_NAME_EXEMPT` 记录这个偏差，不是静默放过；②`yuji`（于吉）的"缠怨"既无 cap 也无 hook，靠 `player.chanyuan` 状态字段实现，登记为 `'状态字段'` 类。**注册表字段**：`武将`/`技能名`/`实现方式`（5 类：`cap-被动查询` 44 条、`cap-主动阶段` 55 条、`hook` 7 条、`cap+hook` 1 条、`状态字段` 1 条）/`能力标识`/`钩子`/`触发阶段`/`机器人接入{决策,座位选择}`/`效果函数`/`查询点`/`主公技`。**`效果函数` 设计为可选字段**——审计中实测发现大量被动技能（武圣/空城/龙胆/倾国/马术等）根本没有独立效果函数，是内联 `hasCap` 判断（`canUseAs`/`canTarget`/`distance` 里），最初把它设成必填字段时 108 条里有 52 条填不出来；如实改成可选，并新增可机器校验的 `查询点` 字段（该 cap 被 `hasCap` 类函数查询到的源文件列表）替代其定位作用。**8 个边界案例的具体登记方式**（这些是最容易摘录错的，用户特意要求优先覆盖）：郭嘉（天妒=`cap-被动查询`/`tiandu`；遗计=`hook`/`onDamaged`+阶段 `yijiAsk`,`yijiAssign`）、孙尚香（枭姬=`hook`/`onLoseEquip`，无 cap 无阶段）、夏侯惇（刚烈=`hook`/`onDamaged`+阶段 `ganglieAsk`,`ganglieChoice`）、荀彧（驱虎=`cap-主动阶段`/`quhu`；节命=`hook`/`onDamaged`+阶段 `jiemingAsk`）、鲁肃（好施**独占两个 cap** `haoshi`+`extraDrawPhase`；缔盟=`dimeng`）、于吉（蛊惑=`guhuo`；缠怨=`状态字段`）、左慈（化身=`huashen`+5 个阶段；新生=`hook`/`onDamaged`+`xinshengAsk`）、司马懿（反馈=`hook`；鬼才=补登记 `guicai`）。另外还处理了 4 类自动匹配匹配不上的情况：拼音与 cap 名不一致（制蛮 `zhiman`→cap `zhimeng`、鬼道 `guidao`→cap `guidu`、眩惑 `xuanhuo`→cap `huanhuo`、烈刃 `lieren`→cap 驼峰 `lieRen`）、技能名与 cap 名完全不同（咆哮→`unlimitedSha`、马术→`extraMinus1`、英姿→`extraDrawPhase`）、运行时授予的 cap（孙策魂姿觉醒后 `p.caps.yinghun=true`，`yinghunTarget`/`yinghunChoice`/`yinghunDiscard` 三个阶段登记在魂姿名下、能力标识留空）、重名技能（`马术` 同属 machao/pangde，所以注册表主键必须是 `武将id/技能名` 而不能用技能名单独做 key）。**交叉验证（`testclass/run_skill_registry_test.js`，16 项断言覆盖 9 条规则）**：正向 7 条——武将存在于 `GENERALS`／技能名存在于 `GENERALS[id].skill`（两处缺口显式豁免）／能力标识合集与 `GENERALS[id].caps` **完全一致（不多不少）**／钩子合集同理／每个触发阶段在 `STAGE_TABLE` 真实存在／每个机器人接入点在 `BOT_DECISIONS`·`BOT_SEAT_PICKS` 真实存在／每个效果函数在沙箱全局 `typeof==='function'`；反向 2 条（防漏登记，最关键）——`GENERALS` **全部 66 名武将**每一条 caps/hooks 声明都被覆盖（实测 66/66，一个不漏）、`STAGE_TABLE`/`BOT_DECISIONS`/`BOT_SEAT_PICKS` 每一项要么被注册表引用要么在**显式白名单**里（阶段 120 项 = 技能认领 101 + 白名单 19；决策 33 项 = 认领 20 + 白名单 13；座位选择 12 项全部被认领，无白名单），白名单本身还有一条"白名单里每一项都必须是真实存在的阶段/决策"的反向校验防止堆废弃名字。**分批登记 + 每批验证**（用户明确要求，避免最后才发现系统性摘录错误）：批次1（22 武将/37 技能）→ 正向规则 1-7 全绿；批次2（22 武将/31 技能，累计 68）→ 全绿；批次3（22 武将/40 技能，累计 108，含白名单）→ 完整 9 条规则 16 项断言全绿。**断言鉴别力验证（4 组破坏性实验，CLAUDE.md 第 20 条）**：①抄错一个 cap 名（`tieqi`→`tieqiX`）→ 规则3、规则8 各报红；②整条漏登记一个技能（删掉 `xiahoudun/刚烈`）→ **同时触发 6 条断言报红**（规则8 武将无条目、规则8附加 武将缺失、规则8附加 技能缺失、规则9 `ganglieAsk`/`ganglieChoice` 变孤儿阶段、规则9 `ganglieChoice` 变孤儿决策、条目总数不符）——反向完备性校验的强度在这里得到实证；③写一个不存在的效果函数名 → 规则7 报红；④从白名单删掉一项（`dying`）→ 规则9 报红。四组全部正确抓出，恢复后 16/16 全绿。**测试范围判断（CORE-124 / CLAUDE.md 第 29 条）**：动手前预判"纯新增→最小测试集"，动手后按规则重新评估——`git diff --stat` 证实确实**零既有函数体改动**（只有 `index.html` +4 行和两个新文件），轴 A 不命中；但新增 `<script src="skills/skill-registry.js?v=1">` 标签**改变了脚本加载顺序**，这正是轴 B 清单里逐字点名的一项，**轴 B 命中 → 必须跑全量**，已按此执行（`run_all_tests.js` **93/93 全过**，92+1）。这次是 CORE-124 规则落地后第一次出现"最初判断最小集、实际评估后改判全量"的真实案例。**本期的诚实边界**：这份注册表是**当前映射关系的锁定快照**，价值在于"回归检测（谁改了 `GENERALS` 的 caps 却没同步注册表，测试立刻红）+ 单一入口读到某技能触碰哪些阶段/决策"，**不是独立重新推导出的第二实现**——第一期本来就只要求"登记"，本文件目前不被任何既有代码读取（第二、三期才按表驱动）。`index.html`：新增 `skills/skill-registry.js?v=1`。
- **CORE-117（issue #125）修复 GENERALS.simayi 的 skill 字段缺失"鬼才"，补全数据一致性回归测试（2026-08-16, issue-fixes-13 分支）**：CORE-78（#123）技能注册表审计过程中发现的既有数据缺陷，本次单独立项跟进修复。**修复**：`data.js` 里 `GENERALS.simayi.skill` 从 `'反馈'` 改为 `'反馈/鬼才'`（三处交叉印证：`desc` 原文自带"(鬼才)"字样、`HUASHEN_SKILL_TABLE.simayi` 早已正确登记两条、`caps` 里也确实声明了 `guicai:true`）。字段全部消费点均为直接插值展示文案（`showGeneralInfo`/`showHelp`（render.js）、`render-hand.js:73`、`render-controls.js` 选将界面多处、`bot.js` AI 候选文案），没有硬编码字符串，改完 `data.js` 自动生效，不需要额外改动这些消费点本身——已逐一读过源码确认。**`skills/skill-registry.js`（CORE-78）联动清理**：移除文件头"已知的两处 GENERALS 数据缺口"注释里 simayi 那一条（改成"CORE-117 已修复"的历史说明），保留 `yuji/缠怨` 那一条（仍是技能本身的真实实现方式，不是数据遗漏）。`testclass/run_skill_registry_test.js` 同步移除 `SKILL_NAME_EXEMPT` 里的 `simayi/鬼才` 豁免项，规则2/条目总数断言的措辞与逻辑相应简化（不再需要"+豁免"这个特例分支）——移除豁免后 CORE-78 的全部既有断言依然全绿。**新增回归测试**（`run_skill_registry_test.js`）：①`hasSkillName(司马懿玩家,'鬼才')` 应返回 `true`（钉住这条此前休眠的逻辑路径——CORE-117 issue 正文调查阶段已确认全仓库没有任何现存调用点以 `'鬼才'` 为参数调用 `hasSkillName`，这条断言把它变成一条主动验证过的、不再是"理论上受影响"的路径），另加两条对照断言（司马懿的"反馈"依旧返回 true、一个不存在的技能名返回 false，确认没有把函数改成恒真）；②按用户要求做了**全部 66 个武将的通用一致性校验**，检查 `GENERALS.skill` 与 `HUASHEN_SKILL_TABLE` 是否同步。**开发过程中的一次真实自我纠正（如实记录，不隐藏）**：第一版把这条通用校验写成"两个技能名集合完全相等"，第一次运行就报出 **10 个"不一致"**（马谡/孙权/孙策/刘备/曹操/袁绍/袁术/张角/于吉/左慈）——但逐一核查后发现这些全部是`data.js`（约528-543行）自己注释里明确记录的**有意设计排除**：`HUASHEN_SKILL_TABLE` 按官方"化身"规则的精神排除了主公技（激将/护驾/黄天/妄尊/血裔/救援/制霸，项目当前无按 cap 声明这些技能所以天然不在表里）、马谡"散谣"（一段已知的死代码，caps 从未被任何 `hasCap` 查询过，故意只保留"制蛮"一条）、以及左慈自身的化身/新生（借用会递归借用自己）——这些技能只出现在 `GENERALS.skill`、不出现在 `HUASHEN_SKILL_TABLE` 里，是正常状态，不是数据问题；第一版断言把这个方向也当成错误，是检验逻辑本身的缺陷，不是真的发现了 10 个新 bug。**修正为单向子集校验**：`HUASHEN_SKILL_TABLE` 里出现的每一个技能名都必须是 `GENERALS.skill` 的子集——这个方向精确对应 simayi 这次的真实缺陷模式（HUASHEN 表有"鬼才"、`GENERALS.skill` 没有），且不会对"主公技只在 skill 里、不在 HUASHEN 表里"这种合法情形误报。修正后覆盖 `HUASHEN_SKILL_TABLE` 涉及的全部 65 个武将（`zuoci` 自身不在该表中，65/66 符合预期），**零发现**——本次调查没有找到其它同类数据不一致，如实报告。**断言鉴别力验证**（CLAUDE.md 第20条）：临时把 `simayi.skill` 改回 `'反馈'` 重新测试，4 条相关断言（规则2、条目总数、`hasSkillName` 回归、单向子集校验）全部正确报红，证明检验没有失效；恢复后全部转绿。**全量回归发现的一处连带影响**：`testclass/run_ai_bus_l3_test.js` 里一条断言硬编码了旧的选将候选 label 字符串 `'司马懿(反馈)'`（`buildCandidates` 直接透传 `gen.skill` 拼接文案），`GENERALS.simayi.skill` 改动后候选 label 自然变成 `'司马懿(反馈/鬼才)'`，导致这条硬编码断言报红——更新期望值为新字符串并加注释说明这是"数据修复导致的预期变化，不是行为改坏"，不是掩盖问题。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：改动的 `GENERALS.simayi.skill` 是被多处共用的核心数据表字段（武将信息展示、选将界面、AI候选文案、CORE-78 技能注册表交叉验证等多处消费），轴 A 明确命中，跑全量 `run_all_tests.js`，连续两次 **93/93 全过**（92+1）。`index.html`：`data.js?v=399→400`、`skills/skill-registry.js?v=1→2`（内容有实质改动，注释更新也按惯例递增版本号）。
- **CORE-84（issue #131）修复身份标记重开后仅房主被清空、其他玩家旧标记残留（2026-08-16, issue-fixes-14 分支）**：CORE-115（#115，身份标记功能）的多人场景后续 bug。**根因**：标记存 `localStorage`（`identityMark:<roomId>:<seat>`，纯客户端本地状态），清空逻辑 `clearAllIdentityMarks()` 只在 `newGame()`（room-lifecycle.js）里执行；而"再来一局"按钮仅房主可见可点（`render-controls.js` 的 `isRoomOwner(g,mySeat)` 守卫），房主点击只能清**自己浏览器**的 `localStorage`，清不到其他玩家设备上的——`localStorage` 本身是设备本地的，"房主帮别人清掉"在架构上就不成立。issue 正文精准指出这是 CORE-115"随 `newGame()` 清空"这条设计决定建立在"每个玩家自己点重开"的单机视角上、与实际多人场景（重开是房主单点操作）的错配。**关联性判断**：与 CORE-116（botTwoStepA 超时兜底）、CORE-113/114（候选生成与真实 execute 不一致）均无关联，是身份标记这个独立功能自身的多人协作设计缺陷，明确排除牵强关联。**修复（issue 推荐方案A，最小侵入）**：标记 key 加入**局标识**——`identityMark:<roomId>:<seed>:<seat>`。局标识直接复用 **CORE-77（#122）已经实现的 `g.seed`**（`finishGeneralAssign` 时生成的每局唯一无符号32位整数，两个前置工作在此自然衔接，未新增任何全局状态或概念）。新一局的 `seed` 必然不同于上一局，任何客户端（不管是不是房主）读旧 key 天然读不到值——**不需要跨设备清除**，从根源上规避了"本地存储无法被他人清除"这个矛盾。`identityMarkKey`/`getIdentityMark`/`setIdentityMark` 三个函数签名从 `(seat)`/`(seat,mark)` 改为 `(g,seat)`/`(g,seat,mark)`，两个调用点（`renderSeatCard` 内的 `identityMarkEntry`、`openIdentityMarkMenu`）同步改传 `g`/`currentG`。`seed` 缺失时（理论上不该发生——标记UI仅在 `g.started` 时渲染，而 `g.started` 与 `g.seed` 在同一次 `finishGeneralAssign` 里一起写入）回退空字符串，不产生字面量 `"undefined"` 污染 key。`clearAllIdentityMarks()`（仍按 `roomId` 前缀扫描，不含 seed）继续保留在 `newGame()` 里调用，语义降级为"房主本机的历史陈旧 key 清理"这个次要用途（纯粹是 `localStorage` 卫生，不再是正确性的必要条件）——即使它完全不执行，新局的 seed 隔离本身已经足够保证不显示旧标记，这正是方案A"不依赖跨设备清除"的核心意义。**测试**：`testclass/run_identity_mark_test.js` 既有 CORE-115 断言（除模式泄露/点击写入两处因涉及具体标记值比对，补了显式 `seed:1`/`G1.seed` 对齐，避免局隔离这个新引入的维度干扰"模式"维度的测试意图）全部适配新签名（定义共享 `var G1={seed:1}` 代表"同一局"场景，绝大多数既有断言测的是存储本身/房间隔离，不关心局隔离），22 项全绿；新增 CORE-84 专属 6 项：同房间不同 seed 互不可见（且旧局自己仍能读到，证明是"隔离"不是"删除"）、**精确还原 issue 描述的核心场景**——非房主客户端从未调用过 `newGame()`/`clearAllIdentityMarks()`，仅凭新局 `g.seed` 不同即天然看不到旧标记、`identityMarkKey` 对 `seed` 缺失/`g` 为 `null` 的防御性回退、`renderSeatCard` 端到端验证局A局B标记显示隔离、`clearAllIdentityMarks` 降级后仍能正常清理房间内任意局残留 key。**红绿验证（两轮，CLAUDE.md 第20条）**：①用改动前的 `render.js`（`git show HEAD`）加新测试文件跑，因函数签名不匹配（1参 vs 2/3参）导致 10 项断言全线报红——证明测试文件确实依赖这次改动才能通过；②更精细的破坏性实验——只保留新函数签名、但让 `identityMarkKey` 故意忽略 `seed` 参数（退回旧行为的等价逻辑），结果**精确只有 3 条 CORE-84 专属断言报红**（局隔离3项），其余19项（含全部CORE-115断言）依然绿——证明新增断言的鉴别力精准命中"局隔离"这一个维度，不是靠签名不匹配这种粗糙信号蒙混过关。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：`getIdentityMark`/`setIdentityMark`/`identityMarkKey` 都是**修改已有函数体**（签名变更）且**各自被 ≥2 处既有调用点共用**（`renderSeatCard`、`openIdentityMarkMenu`），轴 A 明确命中——跑全量 `run_all_tests.js`，连续两次 **93/93 全过**（含 cache-bust 检查）。`index.html`：`render.js?v=404→405`。
- **CORE-110（issue #110）渲染层 XSS 转义审计：全量扫描 155 处 setBanner + 55 处 innerHTML/insertAdjacentHTML，修复 17 处未转义玩家名裸拼接（2026-08-16, issue-fixes-15 分支）**：issue 是一次安全审计任务（P1/严重程度高），要求先审计后修复。**审计方法**：用脚本（不是人工逐条读）对 `render-controls.js` 提取全部 setBanner(...) 调用的完整参数文本（用括号平衡解析，正确处理跨行/嵌套括号，共提取到 155 处，与 issue 正文估算一致），以及对 `render-controls.js`/`render.js`/`render-table.js`/`render-hand.js`/`render-log.js`/`render-discard.js` 提取全部 `.innerHTML=`/`.insertAdjacentHTML(...)` 语句（55 处），用统一规则分类：是否存在未被 `escapeHtml(...)` 包裹的 `<标识符>.name` 引用——`.name` 是玩家名/武将名/牌名的共同后缀，逐一核实变量声明确认其真实来源（`g.players[...]`=玩家名=用户输入=真实风险；`getGeneral(...)`=武将对象=固定数据；`card`/`.claimedCard`=牌对象=固定数据）。**审计结果**：155 处 setBanner 里 19 处命中"未转义 `.name`"规则，逐一核实后 **17 处是玩家名（真实风险，已修复）、2 处是固定数据**（`genForPick.name` 武将名/`d.claimedCard.name` 牌名，issue 正文自己也提到"变量来源待确认"的这条最终确认为无风险）；55 处 innerHTML/insertAdjacentHTML 里仅 2 处命中，均为固定的 `card.name`，与 issue 正文"风险低"的初步判断一致，无需修复。**17 处修复列表**（render-controls.js，均补 `escapeHtml()` 包裹）：猛进弃牌提示（`g.players[d.from].name`+`target.name`，2处一行）、神速1/神速2跳过提示（各1处 `p.name`）、乱武发动提示（`source.name`）、恩怨触发/选择/选牌三处提示（各1处 `source.name`）、眩惑交牌/获得牌两处提示（各1处 `target.name`）、神速选杀目标提示（`p.name`）、神速多次选目标提示（`p?p.name:'夏侯渊'`三元表达式内）、濒死求桃提示（`dyingP.name` 两个分支，第三个"你没有可用的桃"分支此前已经转义、只是前两个分支漏了，属于同一行三元表达式里的不一致遗漏）、蛊惑声明/质疑决定/借刀声明/选目标四处提示（`source.name`/`asker.name`）、同疾提示（`yuanshuTongji.name`）。**输入侧策略确认**：`room-lifecycle.js` 的 `joinRoom()` 里玩家名输入只做 `trim()`，不做任何字符过滤（可以是任意字符，含 `<>"'`）——这是刻意的策略选择，不是遗漏：采用"渲染侧统一转义"而不是"输入侧过滤"作为唯一防线，过滤本身存在被绕过风险（HTML实体编码/大小写混用/Unicode变体）且会误伤玩家想用的正常特殊字符（emoji等），已在该函数顶部补代码注释记录这个策略决定（issue 验收标准要求的"过滤策略已确认并在代码注释中记录"）。`setBanner`（render.js）本身也补充了约束性注释：函数自身不转义（不能转义，因为需要拼合法的 `<span>` 倒计时标签），调用方必须自行保证用户输入部分已转义，这是强制约束不是建议。**测试**：新增 `testclass/run_xss_audit_test.js`，两层验证——①**穷尽式静态扫描**（规则A）：把审计脚本本身封装成可执行断言，扫描 `render-controls.js` 全部 setBanner 调用，断言零处存在未被 `escapeHtml` 包裹的玩家名引用；配一条白名单校验（两个已确认安全的例外必须仍能在源码里找到，防止有人删掉对应代码后白名单变成"允许任意未来同名字段不检查"的空洞）——这条断言是**回归防护性质**的，以后任何人新增一处 `setBanner(xxx.name+...)` 忘了转义会被自动抓到，不需要重新人工审计一遍全部调用点。②**端到端真实渲染验证**（规则B）：挑 3 个真实修复过的渲染函数（`renderPendingEnyuanChoose`/`renderPendingShensuChoose1`/`renderPendingLuanwuChoose`，覆盖 source/p 两类变量命名），用真实构造的恶意玩家名（`<img src=x onerror=alert(1)>"'<script>alert(2)</script>`）真正调用渲染函数、捕获 `setBanner` 写入 banner 元素的 `innerHTML`，断言不含可执行标签、能看到转义后的实体编码——证明 `escapeHtml` 真的在运行时生效，不只是"代码里存在转义函数调用"这种表面正确。**红绿验证**：用 `git stash` 切回改动前的源码跑这套新测试，7 项里 4 项正确报红（含 3 个端到端测试**真实打印出了完整的未转义 payload**，如 `<div class="banner">你对 <img src=x onerror=alert(1)>"'<script>alert(2)</script> 造成了伤害...`，直观证明修复前的真实漏洞），恢复后全部转绿；另有一条断言专门验证"如果不转义，恶意 payload 确实会原样出现"，用来反向确认整套端到端检测机制本身没有因为 sandbox/mock 问题而失去鉴别力。**范围内确认无问题的项**：`waitAskBanner(name, skill)` 的 `skill` 参数（issue 正文标记为"来源待确认"）——核实后确认全项目只有一处调用点、传入的是 `STAGE_TABLE` 注册表里的固定字符串常量（如 `'化身'`），不是变量，无风险。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：`escapeHtml` 是被 `render.js`/`render-controls.js`/`render-log.js` 等几乎全部渲染文件共用的核心函数，本次改动虽未修改 `escapeHtml` 自身实现，但改动的 `render-controls.js`/`render.js` 是被极大量既有测试文件加载共享的文件（超过 20 个 testclass 文件里加载了 render.js/render-controls.js），轴 A 明确命中——跑全量 `run_all_tests.js`，连续两次 **94/94 全过**（93+1）。`index.html`：`room-lifecycle.js?v=405→406`、`render.js?v=405→406`、`render-controls.js?v=418→419`。**本次未做（如实标注，issue"非目标"章节已明确排除）**：未重构渲染层、未引入模板引擎；未处理服务端/Firebase 安全规则（#107 及审计治理路线范畴）；未评估/修复 prompt 注入（#109 范畴）。
- **CORE-116（issue #116）修复机器人刘备【仁德】给出第1张牌后永久卡死：移除localFallback无条件stopAfter，并修复continue链判断依赖的g/me引用过期问题（2026-08-16, issue-fixes-16 分支）**：这条 issue 是本仓库更早一次会话调查后创建的（当时未实现修复，只完成了复现+建档），本次实际实现修复。**主根因（issue 原文已诊断）**：`bot.js` `BOT_DECISIONS.rendeTwoStep.localFallback` 无条件对候选套用 `stopAfter:true`，导致 `execute()` 给完第1张牌后立即 `resetBotTwoStep()`，跳过了紧邻的"`renDeCount<2` 则续问"既定 continue 链设计；`botTwoStepA` 提前清空又使得 `scheduleBotTurn` 的自我触发条件（`else if(botTwoStepA)`）天生不成立，机器人给完1张后再没有任何机制主动继续，安静房间下永久卡死。**修复**：`localFallback` 不再强加 `stopAfter`，原样返回候选，是否继续完全交给 `execute()` 既有的 `renDeCount<2` 判断，让有/无AI Key在终止条件上收敛成同一套逻辑，不再有"无Key单独提前退出"的特例分支；`execute()` 里判断本身也同步移除了 `choice.stopAfter` 分支。**开发过程中发现并修复的第二个、更隐蔽的独立缺陷**：修完第一个bug后，用真实驱动 `scheduleBotTurn` 生产路径的 vm 沙箱探针复现，发现给出的牌数会超过预期上限（观测到给了3张而不是2张），且最终会在手牌耗尽后陷入第二次卡死（`botTwoStepA` 悬空不清空）。逐步排查确认：`execute()` 在 `botInvoke(seat, ()=>renDe(...))` 调用**之后**紧接着读取 `g.renDeCount`/`me.hand.length` 做继续判断，但 `renDe()` 内部通过**独立的** `tx()` 提交（对服务端状态的修改落在一次单独克隆的事务快照里），不会同步反映回这次 `execute()` 调用手上持有的 `g`/`me` 引用——这次调用读到的永远是"给牌前"的旧值，导致继续判断永远慢一拍：真实 `renDeCount` 已经到2了，判断却还在用给牌前的1去比较，多给了一张；手牌真正耗尽后同理错判为"还有牌"，导致下一轮继续要求给不存在的牌，二次卡死。**这个第二缺陷完全独立于第一个 bug**——第一个bug（stopAfter提前终止）本身会掩盖第二个bug（因为流程从不会真正走到第2次继续判断），只有在第一个bug修复后，第二个bug才会暴露出来，本次算是"揭出了原本被更严重的bug盖住的一层"。**第二个缺陷的诊断过程本身有一次值得记录的自我纠正**：最初用一个手写的、不模拟真实 Firebase `onValue`→`currentG` 刷新链路的 ad-hoc 探针复现，观察到"给3张后彻底卡死"的现象，一度怀疑是 `scheduleBotTurn` 自我触发机制本身失效；后来意识到该探针从未在轮询间隙同步刷新沙箱里的 `currentG`（真实生产中，每次 `tx()` 提交后 Firebase 的 `onValue` 监听器会触发 `render(g)`，其顶部就是 `currentG=g`，持续保证新鲜）——这是探针基础设施的局限，不是生产环境会出现的现象；补上这层刷新后，探针行为更贴近真实生产，才准确定位出"过期是发生在单次 `execute()` 调用内部（renDe调用前后），而不是跨调度之间"这个更精确的根因。**修复方式**：不依赖 `botInvoke` 调用后 `g`/`me` 的（滞后）值，改用"这次调用自己知道的信息"在本地推算给牌后的状态——`buildCandidates` 每次只挂一张牌的候选（不会一次给多张），所以给牌后 `renDeCount` = 给牌前 `renDeCount`+1、给牌后手牌数 = 给牌前手牌数-1，在 `botInvoke` 调用**之前**就地计算好 `renDeCountAfter`/`handLenAfter` 两个局部变量，continue 判断只依赖这两个本地推算值，不再依赖 `botInvoke` 之后可能过期的 `g`/`me` 引用。**验证方式**：①单元测试——重写并新增 `testclass/run_ai_bus_l3_test.js` 里的仁德相关断言：原"仁德A6-5"曾直接把 bug 症状（"一张即停不设continue"）钉成"预期行为"，本次按验收标准重写为"给完第1张应continue"；新增一条明确要求"恰好给2张不多不少"的完整两阶段+续给流程测试（用能真正改变 `g` 状态的自定义 mock，而不是通用 `spyService` 那种只记参数不改状态的 mock）；新增一条手牌耗尽收尾测试；专门新增一条**精确模拟"renDe通过独立tx提交、不同步反映到execute()当次调用手持的g/me引用"**这一真实生产语义的测试（同一次调用内 g 冻结在给牌前状态、跨调度之间才会有一份反映了上一次给牌结果的新快照）——这条测试在只有第一个bug修复、没有第二个off-by-one修复时会正确报红（"给完第2张后应正确收尾，实际仍是continue:true"），验证了修复的必要性不是臆测。②生产路径探针——用同一套"仅调用一次 `scheduleBotTurn`、此后不再手动干预"的复现手法重新验证：第一个bug单独修复后，流程不再在给1张后永久卡死，能继续推进；第二个bug修复后（探针补上 `currentG` 刷新以贴近真实生产），流程精确停在 `renDeCount=2`（不多给不少给），不再出现二次卡死。③soak.js 回归压测——两次批次（80局2-6人、150局2-7人）零卡死零崩溃，另在两处修复全部就位后单独再跑一批（100局2-6人）验证零回归。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：改动的 `BOT_DECISIONS.rendeTwoStep` 属于 `bot.js` 机器人决策核心分发机制，CORE-124 轴 B 逐字点名的高连带风险清单项，无需评估调用点数量即命中——跑全量 `run_all_tests.js`，连续两次 **94/94 全过**（93+1）。`index.html`：`bot.js?v=407→408`。
- **CORE-83（issue #130）修复AI决策面板/导出在游戏结束后无记录：清空时机从"游戏结束"改为"确认进入下一局"（2026-08-16, issue-fixes-17 分支）**：CORE-73（#118 决策面板）与 CORE-75（#120 下载导出）的核心用途就是"结束后复盘/导出调查"，但旧代码 `syncAiTestGamePhase(phase)` 里 `phase==='over'` 时会把 `aiDecisionRecords` 整体清空——这是托管信息窗时代的旧语义（"这局结束了，信息窗记录没意义"），CORE-73/75 把决策记录升级成"本局全部 AI 决策统一存储 + 导出数据源"之后没有同步调整这个清空时机，直接摧毁了这两个功能的核心价值（用户实测：结束后面板空白、导出 dump 的 `aiDecisions:[]`）。**修复**：清空判据从"observed phase 变成 over"改为"确认 `g.seed` 变化"——`g.seed` 是 CORE-77（#122）每局开始时生成的唯一值，属于**共享房间状态**，所有客户端（不分是不是房主）通过 Firebase 同步都会观察到同一次变化，不依赖谁点了"再来一局"按钮（和 CORE-84 那次"不能用 client-action 级 hook、要用共享状态级观察"是同一条教训的复用，这次直接把该教训用在了另一个功能上）。`syncAiTestGamePhase` 函数签名从 `(phase)` 扩展为 `(phase, seed)`，新增 `let aiTestLastObservedSeed = null` 跟踪；首次观察到某个 seed 时不清空（本来就是空的，清了也没意义），只有确认换到了下一局（seed 变成一个新值）才清空——`if(aiTestLastObservedSeed!==null) clearAiTestRecords();` 这条守卫正是为了区分"这是本次会话第一次看到某个 seed"和"从一个已知 seed 换到另一个新 seed"两种情况。`render.js` 里唯一的调用点同步改为 `syncAiTestGamePhase(g.phase, g.seed)`。`stopAiTestAutopilot()`（游戏结束自动停止托管）继续保留在 `phase==='over'` 分支触发，和记录清空**解耦**——游戏结束仍应该自动停止托管，只是不该顺带清空刚打完这局的记录，这是 issue 验收标准明确要求的行为拆分。**测试**：`testclass/run_ai_test_button_test.js` 里原有一条断言直接把 bug 症状（"游戏结束应清空记录"）钉成了预期行为，本次按修复后的正确行为重写为"phase 变 over 不应清空"，并拆分成 4 条更精细的断言：①结束不清空记录、②结束仍自动停止托管且这个动作本身不清空记录（验证解耦）、③只有 seed 真正变化才清空上一局记录（含"首次观察某 seed 不清空"这条边界）、④seed 缺失（大厅阶段尚未生成）时不误清空/不报错。**红绿验证**：临时把 `syncAiTestGamePhase` 还原成旧的"phase===over 即清空"写法，3 条新断言正确报红，恢复后全部转绿。全量 `run_all_tests.js` 连续两次 **94/94 全过**（93+1，`run_ai_decision_panel_test.js` 26 项不受影响，无回归）。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：`syncAiTestGamePhase` 是修改已有函数体（签名扩展），生产代码里只有 `render.js` 一处调用点，严格按轴A"超过一处调用"的字面标准不算命中；但改动的 `render.js`/`ai-bot.js` 是被超过20个既有测试文件共享加载的文件（和 CORE-110 那次同一类"文件本身被广泛共享"的从宽判断），出于一致性和谨慎跑了全量。`index.html`：`ai-bot.js?v=399→400`、`render.js?v=406→407`。
- **CORE-89（issue #136）身份局出牌候选未硬过滤友方目标，忠臣AI可主动攻击主公（2026-08-16, issue-fixes-18 分支）**：AI 模块 P1，源码走读确认+用户实测吻合，未要求单独复现即可直接实现。**根因**：`bot.js` 的敌我策略边界（忠臣不能杀主公/已知忠臣、反贼不能杀已知反贼队友、主公不能杀已知忠臣）此前只体现为 `botTargetScore()` 里的 `-Infinity`，而 `-Infinity` 只影响候选排序，不会把候选从列表里删掉——`bot-ai-bus.js` 明确告诉 LLM 分数"只是排序参考，不代表最优解"，LLM 能无视分数直接选中这些候选，`executePlayWindowChoiceAwait()→playCard()` 真实生效（游戏规则层对"忠臣杀主公"完全合法，不会拦，这本身没错，真人玩家理应能自由选择）。**排查确认的两个候选生成入口都命中同一缺陷类**：①`enumerateAllLegalOneStepActions`（喂给 LLM 的"强C"出牌候选枚举，覆盖【杀】+决斗/火攻/顺手牵羊/过河拆桥等所有 `CARD_PLAYS` 里 `spec.target` 为真的牌，以及铁索连环单/双目标两条分支）；②`BOT_SEAT_PICKS` 的 11 个座位技能候选构建器（`duanliang`/`guose`/`qixi`/`xuanfeng`/`wusheng`/`longdan`/`shuangxiong`/`tiaoxin`/`fanjian`/`quhuDamage`/`guhuoTarget`，覆盖断粮/国色/奇袭/旋风/武圣/龙胆/双雄/挑衅/反间/驱虎伤害/蛊惑），逐一确认它们的 `buildSeatCandidates` 同样只做游戏规则合法性过滤（距离/`canTarget`），没有任何敌我策略过滤。**排查确认为安全、不需要改动的路径**：`botBestTarget(g,seat,card,actionId)`——单目标选择 helper，`if(score>bestScore){best=i;}` 严格大于比较从 `bestScore=-Infinity` 起步，若全部候选评分为 `-Infinity`（全部策略禁止），比较恒为假，`best` 保持初始值 `-1`（=无合法目标），这条 helper 本身已经是"安全"的，不属于本次修复范围；本次修复不涉及它。**修复**：新增独立布尔谓词 `botTargetRelationAllowed(g,seat,targetSeat,kind)`（`bot.js`，紧邻 `botTargetScore` 之前），把原先散落在 `botTargetScore` 里的 -Infinity 判定条件原样抽取成纯关系判断，**只在 `g.gameMode==='identity'` 时生效**（组队模式复用既有 `sameTeam` 排除同队；非身份局/非组队一律放行，不套任何硬边界）——身份局分支逐字对应原判定：忠臣排除已知主公/已知忠臣，未知目标 `suspicion<35` 排除；反贼排除已知反贼；主公排除已知忠臣，未知目标 `suspicion<30` 排除；**内奸完全不在此函数设限**（恒返回 `true`），继续 100% 依赖 `botTargetScore` 自身的动态评分分支决定，精确对应 issue"内奸继续使用现有动态策略，不能粗暴套固定敌我过滤"这条非目标要求。在两处候选生成入口——`enumerateAllLegalOneStepActions` 的铁索连环单目标循环、铁索连环双目标组合循环（先过滤单目标列表再做组合，组合天然不含被过滤座位）、通用 `spec.target` 单目标循环，以及 `seatPickBuildCandidates` 聚合出口（对全部 `BOT_SEAT_PICKS` 候选做统一过滤，覆盖当前和未来新增的座位技能，不用逐个技能改）——分别接入 `botTargetRelationAllowed`，命中即从候选列表里整体剔除，不是打低分。**唯一例外**：`qingnang`（青囊，唯一的正面/治疗类座位技能）在聚合出口显式加入 `HARMLESS_SEAT_PICK_KEYS` 排除名单——治疗类技能的候选目标本就该优先选自己人/已知队友，若不排除会被"禁止选自己人"的敌我过滤误伤到把合法治疗对象全过滤光；以后新增正面效果的座位技能需要同步加入这个排除名单。**`botTargetScore` 本身逐字未改**——原计划把 `botTargetScore` 重构成调用 `botTargetRelationAllowed` 的等价形式，但全量测试暴露这个重构并非真正零行为变化：多个既有非身份局测试用 `gameMode:'ffa'` + 遗留的 `role:'zhu'` 填充值（纯粹作为 mock 占位符，不代表真的进入身份局逻辑）构造场景，`botTargetScore` 历史上从不检查 `gameMode` 就直接按 `role` 分支走 `-Infinity` 判定，这是这些测试隐式依赖的既有行为；引入 `gameMode==='identity'` 网关后，若让 `botTargetScore` 也共用这同一个网关，会让这些 mock 在非身份局下意外跳过原本的 `-Infinity` 分支，产生和历史评分不一致的候选排序（真实复现：铁索连环目标座位从"因-Infinity并列、按插入顺序稳定排序命中座位1"变成"评分不再是-Infinity、改成命中座位2"，3 个既有测试文件因此报红）。最终方案是保持 `botTargetScore` 内部逻辑完全不动（包括它历史上就不按 `gameMode` 区分这一点），`botTargetRelationAllowed` 作为纯粹独立的第二重过滤，只用在候选生成层，不参与也不改变 `botTargetScore` 内部的打分路径——这是本次任务里唯一一次方案调整，如实记录。**测试**：新增 `testclass/run_core89_bot_faction_filter_test.js`（vm 沙箱加载真实全链路），11 项覆盖 issue 全部验收标准：忠臣对主公/已知忠臣（用决斗牌交叉验证不止杀受影响）的杀候选/决斗候选均不存在；`suspicion<35` 未知目标不生成候选、`suspicion>=35` 时可以生成（4人环形局默认杀射程1，两条断言分别选距离1的座位，避免距离限制这条独立规则和策略过滤断言互相干扰，注释已说明）；反贼候选排除已知反贼队友、保留主公为合法目标；主公候选排除已知忠臣、保留已知反贼；内奸零回归两项（`botTargetRelationAllowed` 对内奸恒真、`botTargetScore` 对内奸的评分逐字不变，仍可能因"前中期不让主公暴毙"这条内奸自己的既有动态策略返回 `-Infinity`）；非身份局（`ffa`）零回归；座位技能候选（断粮为例）同样硬过滤忠臣→主公，且显式断言"过滤前的原始候选确实包含主公"（证明过滤发生在策略层而非规则层，游戏规则本身仍允许真人自由选择，符合非目标要求）；**破坏性验证**——临时把 `botTargetRelationAllowed` 替换成恒真函数，重新枚举后杀→主公候选应该重新出现，证明"候选列表中不存在杀→主公"这条核心断言真的在检测这个函数、不是巧合通过。全部 11 项一次写完即绿（过程中两项因未考虑4人环形局默认杀射程距离限制而误报，调整测试座位布局后转绿，非实现问题）。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：改动的 `enumerateAllLegalOneStepActions`/`botTargetScore` 均是 `bot.js` 里被广泛共享的核心机器人决策函数（`botTargetScore` 有 20+ 处调用点），且新增的候选生成硬过滤直接影响 `BOT_SEAT_PICKS` 全部 12 个座位技能注册项，轴 A/轴 B 均命中——跑全量 `run_all_tests.js`，连续两次 **95/95 全过**（94+1新增）。`index.html`：`bot.js?v=408→409`。
- **CORE-88（issue #135）修复酒杀被闪抵消后发动贯石斧只造成1点伤害（酒加成jiuBonus全链路丢失）（2026-08-16, issue-fixes-19 分支）**：GAME 模块 P2，源码逻辑确认。**issue 原文根因**只点出 `finishGuanshiDamage`/`maybeStartGuanshifu` 两处没有透传 `jiuBonus`，但实际排查发现问题比 issue 描述的范围更大：`g.pending.jiuBonus`（酒 +1 伤害标志，在 `resolveShaUseNoLiuli` 创建响应 pending 时由 `consumeJiuShaBonus` 写入）从「杀被闪抵消后的效果调度链」（`maybeStartShaOffsetEffects`→`startShaOffsetEffect`→猛进/青龙偃月刀/贯石斧→`continueShaOffsetEffects`）的**最顶端入口**就已经开始丢失——这条链条本身在设计时就只透传了 `sourceCard` 一个参数，从未考虑过 `jiuBonus`，不只是贯石斧一个环节漏了。**排查方法**：逐一读完 `maybeStartShaOffsetEffects` 的全部调用点（`respondShan` 闪响应分支、`continueShaAfterTieqi` 八卦阵判红分支、`finishGuidu`/`finishGuicai` 的鬼道/鬼才改判后 bagua-red 分支、`completeLordAsk` 护驾替出闪分支），确认全部 5 处都没有把当时可读到的 `jiuBonus`（`g.pending.jiuBonus` 或 `resume.shaInfo.jiuBonus`）传给 `maybeStartShaOffsetEffects`；继续读完链条内部（`startShaOffsetEffect`/`continueShaOffsetEffects`/`respondShaOffsetChoice`/`mengjinPick`/`respondMengjin`(game.js 里一个绕开 `respondShaOffsetChoice`、直接从 shaOffsetChoice 切到 mengjin 的独立函数，容易漏查)/`maybeStartQinglong`/`respondQinglong` 的"不发动"分支/`maybeStartGuanshifu`/`respondGuanshi` 的"不发动"分支/`finishGuanshiDamage`/`resumeAfterInterrupt` 的 `shaOffset`/`guanshiDamage` 两个 resume 分支），确认这条链条的**每一站**都只透传了 `sourceCard`，`jiuBonus` 全程缺席。**规则层面的一个关键判断**（决定了修复范围的边界，如实记录排查过程中的推理）：青龙偃月刀触发的是"重新使用一张全新的杀"（`respondQinglong` 内部走 `resolveShaUse` 完整流程），按官方规则酒的 +1 只在"本回合第一张杀"结算时生效一次（已经在 `CARD_PLAYS['杀'].effect` 里通过 `consumeJiuShaBonus` 在使用时消耗/清零），青龙的新杀**不应该**重新吃到这份加成——`me.jiuShaBonus` 已经在第一次出杀时被消耗为 `false`，青龙再次调用 `consumeJiuShaBonus` 天然返回 `undefined`，这一点本来就是对的，不需要改；但猛进（弃牌，无伤害）和贯石斧（强制**这同一张**被抵消的杀继续命中）都不是"新的杀"，理应保留原杀的酒加成。**修复**：给 `maybeStartShaOffsetEffects`/`startShaOffsetEffect`/`continueShaOffsetEffects`/`respondShaOffsetChoice`/`mengjinPick`/`respondMengjin`/`maybeStartQinglong`/`maybeStartGuanshifu`/`finishGuanshiDamage` 全部加一个可选的 `jiuBonus` 参数，和既有的 `sourceCard` 参数用完全相同的写法逐层透传（`if(jiuBonus) g.pending.jiuBonus=true;`、新开的每个中间 pending 都存一份供 continue 链取用）；`maybeStartQinglong` 存这份值**只为了给"不发动"分支 continue 到下一个效果时找回**，不用于青龙自己新杀的伤害计算（函数顶部补了详细注释说明这个不对称）；`finishGuanshiDamage` 最终把它传给 `damageAmount(g, from, 1, 'sha', {jiuBonus:!!jiuBonus})`，和 `sha/sha-resolution.js:541` 正常"不闪"路径的既有写法完全对齐。5 处入口调用点分别改为读取当时能拿到的 jiuBonus 来源：`respondShan`（`g.pending.jiuBonus`，读取时机在原 pending 被 null 之前）、`continueShaAfterTieqi`（函数已有的 `shaInfo` 参数）、`finishGuidu`/`finishGuicai`（`resume.shaInfo.jiuBonus`）、`completeLordAsk`（`g.pending.jiuBonus`）。**测试**：新增 `testclass/run_guanshifu_jiubonus_test.js`（6项，简化版 vm 沙箱，仿 `run_qinglong_quota_test.js` 同款轻量 harness）——① respondShan 直接触发唯一效果自动结算路径（2点伤害）；②未用酒对照组（仍1点，无回归）；③ shaOffsetChoice 多选一路径（青龙+贯石斧同时可选，选贯石斧仍2点）；④青龙"不发动"退回贯石斧（酒加成经过一次可选环节仍不丢失）；⑤八卦阵判红（视为出闪，绕开 respondShan 的独立路径，issue 验收标准明确要求覆盖）；⑥破坏性验证（临时把 `finishGuanshiDamage` 替换成不传 `jiuBonus` 的旧版本，重新验证伤害确实退回1点，证明测试①的断言有真实鉴别力，不是巧合通过）。调试过程中一次真实的测试脚本坑：前4项最初全部卡在 `phase` 停留在 `'respond'` 不推进——排查后发现是测试脚本自己忘记在调用 `respondShan` 前把 `mySeat` 切到目标座位（`respondShan` 内部校验 `g.pending.to!==mySeat` 会静默拒绝），修正后全部转绿，属于测试脚本本身的疏漏，不是实现问题。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：改动的 `maybeStartShaOffsetEffects`/`startShaOffsetEffect`/`continueShaOffsetEffects` 等函数是「杀被抵消后效果调度」这条被 `respondShan`/`continueShaAfterTieqi`/`finishGuidu`/`finishGuicai`/`completeLordAsk`/`mengjinPick`/`respondMengjin`/`respondQinglong`/`respondGuanshi`/`resumeAfterInterrupt` 共10+处共用的核心链路，轴A（修改已有函数体+多处调用点）与轴B（触碰 `dealDamage`/`damageAmount` 等 soak.js 覆盖的核心结算入口）均明确命中——跑全量 `run_all_tests.js`，连续两次 **96/96 全过**（95+1新增）。`index.html`：`game.js?v=426→427`、`sha/sha-resolution.js?v=404→405`、`weapons.js?v=398→399`。
- **CORE-82（issue #129）修复机器人（尤其身份局主公）装备诸葛连弩+用酒后当回合无杀可用仍照样喝酒的资源浪费（2026-08-16, issue-fixes-20 分支）**：AI 模块 P2，issue 原文可信度标注"实测现象（完整对局dump），根因待AI决策日志验证"，本次任务落地根因确认与修复。**根因**：`enumerateAllLegalOneStepActions`（`bot.js`）早先已有一段"酒→杀顺序修复"逻辑（找出候选里最高分的杀，把酒的分数拉到比它更高，保证本地兜底/AI参考分都优先喝酒再出杀）——但那段逻辑只处理"酒和杀同时是合法候选时谁先出"的顺序问题，完全没处理"这一次枚举里压根没有一张杀是合法候选"这种情况：`botCardPriority('酒')` 是一张不带上下文的静态分（40），没有杀候选陪跑时这个分数原样保留，仍然高于本地兜底出牌阈值（25），会被 `localFallbackPlayWindow`（无密钥兜底，取最高分候选）当成一次"值得打出"的候选选中——而酒的效果只对"本回合下一张杀"生效，没有后续杀可用时喝下去纯粹是资源浪费，和"装备一把永远用不上的武器"是同一类无意义操作。**确认与该局面吻合的触发条件**：issue 附带 dump 是 8 人身份局，机器人4 是主公（马谡），`botTargetScore` 对 `me.role==='zhu'` 有 `else if(suspicion<30) return -Infinity;` 这条既有的保守分支（主公不主动出杀打嫌疑值不够高的未知身份）——回合数较早（issue 描述的是第4回合）时全场对主公来说大概率都是低嫌疑未知目标，叠加**同一天刚合并的 CORE-89（#136）候选硬过滤**（策略禁止目标此前只打 `-Infinity` 低分、现在直接从候选列表里删除），杀会**彻底没有合法候选**（不是有候选但分低，是候选列表里根本不存在杀这一项），这正是"酒→杀顺序修复"那段逻辑的 `maxShaScore` 归约结果恒为初始值 `-Infinity` 的精确场景——`if(maxShaScore > -Infinity)` 判断为假，酒的分数完全不受这段既有逻辑影响，原样保留在40分，被无密钥兜底选中。dump 本身 `aiDecisions` 为空（印证当局未配置 AI Key，走的是纯本地启发式兜底路径，不是 LLM 决策质量问题）与本次代码走读确认的根因完全吻合——**根因归属为决策执行/本地启发式链路的静态分数缺陷，不是 AI 模型判断失误**，issue 验收标准第一条"复现该回合并确认根因归属"已用源码交叉验证的方式完成，不依赖尚未拿到本地的原始 dump 附件。**修复**：在同一段"酒→杀顺序修复"逻辑紧邻处补一个互补分支——`if(maxShaScore === -Infinity) jiuCandidate.localHeuristicScore = Math.min(jiuCandidate.localHeuristicScore, 20);`（压到本地兜底阈值25以下，但不是硬编码成固定值20——用 `Math.min` 是为了防御性地兼容"酒的基础分未来被调低于20"这类情况，届时压制不应该反而把分数抬高）。不是彻底禁止喝酒——如果同一次枚举里确实存在至少一张合法杀候选（哪怕分数不高），既有的顺序修复分支仍然正常生效，只压制"确定没有下文"这一种场景。**测试**：更新 `testclass/run_ai_bus_l3_test.js` 里原有的一条断言——"酒→杀顺序修复回归：酒/杀单独出现时分数不受影响"曾把"只有酒时分数应保持原值40"钉成预期行为，这正是 issue 描述的 bug 现场本身（酒单独出现=没有任何杀候选陪跑），本次按 CORE-82 修复后的正确行为重写为"分数应被压到25以下"，并保留同一条断言里"只有杀时分数不受影响"这部分不变（zero-regression，杀单独出现的场景本来就不该被这段逻辑触碰，改动前后逻辑一致）；新增3项：①本地兜底在"只有酒、没有杀候选"场景下确实不再选择喝酒；②破坏性验证——手动把候选的分数还原成修复前的40分，重新验证本地兜底会（错误地）选中酒，证明前两条断言不是巧合通过；③**精确复现 issue 附带 dump 的真实触发条件**——8人身份局、主公、全员嫌疑值默认0（<30门槛）、手持杀+酒，先断言 CORE-89 硬过滤后杀确实完全没有合法候选（这条断言本身也在交叉验证 CORE-89 那次修复仍然生效），再断言酒的分数被压低、本地兜底不再选中酒。全部6项（含既有的2项零回归对照）一次写完即绿。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：改动的 `enumerateAllLegalOneStepActions` 是被 LLM 出牌候选枚举与无密钥兜底共用的核心函数（`bot.js` 里被 `runBotActionWindow`/多个既有测试文件共同依赖），轴A（修改已有函数体+被多处依赖）与轴B（`bot.js` 决策核心分发链路）均命中——跑全量 `run_all_tests.js`，连续两次 **96/96 全过**（95+新增3项并入既有测试文件，测试文件总数不变）。`index.html`：`bot.js?v=409→410`。**与 CORE-89（#136）的关系**：非同一 bug，但存在真实的触发依赖——CORE-89 修复"策略禁止目标从打低分变成硬删除候选"之后，"某个动作类型完全没有合法候选"这种边界情况从理论上的低概率变成了更容易达成的常见场景（尤其身份局早期回合，主公/忠臣面对多个未知身份时），本次修复可以看作 CORE-89 上线后暴露/放大出的一个既有静态分数缺陷，两次修复共同完善了同一条"杀候选硬过滤→连带调整依赖杀候选是否存在的其它分数逻辑"的完整链条。
- **确定正收益技能对真人玩家也自动发动，取消每次都弹确认框（2026-08-16, feature-auto-trigger-zero-risk-skills 分支，用户直接口头要求，非issue追踪）**：背景是 issue #134（CORE-87）要求把机器人侧"确定正收益"的技能决策从 AI 判断改成固定发动以省 token——审计后确认这类binary"是否发动"技能（铁骑/烈弓/连营/闭月/礼让回收/酒诗翻正面/落英拾取/洛神判定等）在机器人侧早已全部固定发动、零 AI 调用（`bot.js` 逐条核实：`tieqi`/`liegong` 在 `CONTROLS_CHOICE_EXCLUDE` 里且有无条件 `botInvoke(seat,()=>respondTieqi(true))` 硬编码分支；`lianyingAsk`/`biyue`/`jiushiFlipAsk` 同理；均无新候选可转），issue #134 本体因此确认"已审计、无新增候选"、按用户指示跳过不做代码改动。但用户指出这些技能对**真人玩家**仍然每次都要点一次"发动/不发动"确认框——这是一个不同维度的问题（游戏本身的交互摩擦，不是 AI token 消耗），用户明确要求修复。**范围确认**（用户从两个选项里选择了较保守的一个）：只对"纯binary发动/不发动，且不发动完全没有意义（纯损失或和发动等价的空转）"的技能做自动化，不碰"多选项但都是正收益"的技能（称象的选牌组合、耀武的回体力/摸牌二选一——这些仍是需要真人判断"哪个更划算"的真实选择，只是每个选项都不亏，不属于这次范围）和装备特效类（寒冰剑/贯石斧/雌雄双股剑——虽然零风险但涉及"是否要付出成本"的战术判断，且已有既定 UI 流程）。最终范围锁定 8 个技能：**铁骑**（马超）、**烈弓**（黄忠）、**连营**（陆逊）、**闭月**（貂蝉）、**礼让回收**（孔融）、**酒诗翻正面**（曹植）、**落英拾取**（甄姬）、**洛神判定**（甄姬）——逐一核对官方效果确认"不发动"分支相对"发动"分支只有纯粹的损失（少判一次红/少摸一张牌/少免一次伤害），不存在任何场景下"不发动更优"的可能，符合issue #134原始描述的"确定正收益"定义，只是这次是应用在真人交互层而不是 AI 决策层。**实现方式（8个技能按各自代码结构分3种模式处理，核心思路一致：跳过"挂起pending等待响应"这一步，直接原地执行"发动=true"分支的逻辑，其余判定链路/续接逻辑完全不变）**：①**铁骑/烈弓**（`sha/sha-resolution.js` 的 `resolveShaUseNoLiuli`）——原本 `hasCap(me,'tieqi')`/`hasCap(me,'liegong')` 判断通过后创建 `g.pending={type:'tieqi'/'liegong',...}` 并 return，现在直接内联执行原 `respondTieqi(true)`/`respondLiegong(true)` 的核心逻辑（铁骑：`judge(g)`+`maybeGuicai`+`finishTieqiJudge`，仍然正确接入鬼才/鬼道改判——这是**另一名玩家**的独立决策，不属于这次要跳过的"发动者自己是否发动"这一层，遇到改判进行中(`'pending'`)照常等待；烈弓：直接令 `noShan=true` 走 `afterShaTargetSkills`）。②**连营/闭月/礼让回收/酒诗翻正面/落英拾取**——原本创建对应 pending 并 return 的位置，直接内联执行原 `respond*(true)` 的核心逻辑（摸牌/翻正面/拿回弃置牌等），再调用原来 `respond*` 函数末尾的续接逻辑（`startTurn`/`advanceXiaoguo`/`continueDelayResolution`等）。其中连营（`tryFlushLianying`）额外做了一个小优化：原来一次只开一个人的询问（队列里排了多人也只处理队首），现在既然是确定性生效，直接把整条队列在同一次调用里全部处理完，不用等下一次 tx 收尾再触发下一个人。③**洛神判定**（`game.js` 的 `continueTurnStart`/`finishLuoshenJudge`）——这是唯一一个"可能循环多轮"的技能（黑色判定牌拿到后可以继续判，直到判红或牌堆耗尽），把原来"设置pending、等respondLuoshen响应、respondLuoshen内部再递归设置下一轮pending"的写法，改成一个新的 `autoLuoshenRound(g,seat)` 函数——`judge`+`maybeGuicai`+`finishLuoshenJudge`，`finishLuoshenJudge` 判黑的分支不再重新挂起pending，而是直接递归调用 `autoLuoshenRound` 进入下一轮，直到判红（`enterDrawPhase`）或无牌可判为止；原 `respondLuoshen` 函数因此彻底不再需要，已删除（不是留作死代码，是真删除——见下方"代码清理"）。**代码清理的范围拿捏（如实记录一个不完全对称的决定）**：8个技能里，除了洛神（`respondLuoshen` 被真正删除），其余7个的原 `respond*`/`maybeStart*` 函数体本身**没有删除**（`respondTieqi`/`respondLiegong`/`respondLianying`/`respondBiyue`/`respondLiRangRecover`/`respondJiushiFlip`/`respondLuoying` 仍然存在，只是这次改动后不再有任何代码路径能触发到它们所依赖的 `g.phase==='xxx'` 守卫，因为对应的 `g.pending`/`g.phase` 赋值已经被替换成直接内联执行，成了各自的死代码）——这是任务范围内的一个刻意取舍，不是遗漏：这7个函数仍被 `render-controls.js` 的按钮 `onclick`/`bot.js` 的机器人硬编码分支引用，但由于函数本身依然存在（只是永远不会被调用到），这些引用不会抛错，属于"安全的死代码"，删除它们需要同步清理 `render-controls.js` 的7个渲染函数+`bot.js` 的7条硬编码分支+`stages/stage-table.js` 的7条 `registerStageTimeoutAction`，工作量和这次改动本体相当，权衡后决定只清理"真正会导致悬空引用"（`respondLuoshen` 被删除后 `bot.js`/`render-controls.js`/`stages/stage-table.js` 里对它的引用会指向不存在的函数，这个必须清理）的部分，其余7个留作后续独立任务清理（不是"以后必须做"，只是"目前刻意搁置，如实标注"）。洛神清理内容：`render-controls.js` 的 `renderPendingLuoshen` 函数+`PENDING_RENDERERS` 表里的 `luoshen` 一行、`bot.js` 的硬编码分支+`CONTROLS_CHOICE_ALLOWLIST` 里的 `'luoshen'`（连带同一次顺手把已经确认不再触发的 `luoyingAsk`/`luoshen` 机器人分支合并成一条说明注释，保留 `luoyingAsk` 分支代码本身——它对应的 `respondLuoying` 函数还在，不是同一类需要清理的悬空引用）、`stages/stage-table.js` 的 `luoshen` 行动者字段+`registerStageNormalizer`+`registerStageTimeoutAction` 三处、`skills/skill-registry.js` 洛神条目的 `实现方式`（从 `cap-主动阶段` 改为新增枚举值 `cap-自动生效`，同步在 `testclass/run_skill_registry_test.js` 的合法值清单里补了这个新枚举）与 `效果函数`（从 `['respondLuoshen']` 改为 `['autoLuoshenRound','finishLuoshenJudge']`）与 `触发阶段`（清空为 `[]`，因为 `g.phase` 不会再出现 `'luoshen'` 这个值）。**测试**：新增 `testclass/run_zero_risk_auto_trigger_test.js`（8项，每个技能一条，验证"触发条件满足后不再出现对应的询问 phase、直接完成该技能的核心效果"），采用和 `run_qinglong_quota_test.js` 同款轻量 vm 沙箱 harness。**红绿验证**：`git stash` 还原改动前的 `game.js`/`skills.js`/`sha/sha-resolution.js`/`skills/late-generals.js` 后重跑新测试，8/8 全部正确报红（各自卡在对应的旧询问 phase 上），证明测试确有鉴别力，不是巧合通过；恢复后全部转绿。**连带修复的既有测试**（改动是设计意图内的正确行为变化，不是回归，按惯例更新断言）：`run_lianying_all_paths_test.js`（10个场景全部从"应挂起连营询问+respondLianying(true)"改为"应直接生效摸牌"）、`run_luxun_test.js`（同上）、`run_cixiong_test.js`（"铁骑/烈弓判定后雌雄双股剑仍正确触发"两条断言从"先出现tieqi/liegong询问阶段"改为"一次playCard直接打到cixiongAsk"）、`run_pending_renderer_registry_test.js`（`migrated`清单移除已删除的`luoshen`条目）、`run_pending_timeout_audit_test.js`（`expected`清单移除`luoshen`，它不再需要超时托管——没有"询问"这一步可超时）、`run_skill_registry_test.js`（新增合法枚举值`cap-自动生效`）、`run_ai_bus_l1_test.js`（移除已经测不到任何真实场景的"luoshen 无密钥回退"T8 测试及其 spy）。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：改动触碰 `resolveShaUseNoLiuli`/`continueAfterDamageEffects`/`tryFlushLianying`/`continueTurnStart`/`finishLuoshenJudge` 等被大量既有测试和游戏核心结算链路共用的函数（`resolveShaUseNoLiuli` 是杀结算的核心入口），轴A（修改已有函数体+被多处调用）与轴B（触碰 soak.js 覆盖的核心结算路径）均明确命中——跑全量 `run_all_tests.js`，连续两次 **97/97 全过**（96+1新增）。`index.html`：`game.js?v=427→428`、`sha/sha-resolution.js?v=405→406`、`skills.js?v=407→408`、`skills/late-generals.js?v=403→404`、`bot.js?v=410→411`、`render-controls.js?v=419→420`、`stages/stage-table.js?v=401→402`、`skills/skill-registry.js?v=2→3`。**新的可复用架构约定（补充进CLAUDE.md"武将技能系统的进阶模式"索引）**：见下方 CLAUDE.md 本体更新。
- **CORE-81（issue #128）修复对局结束后诊断dump导出game.over恒为false、与game.phase='over'状态不一致（2026-08-16, issue-fixes-21 分支）**：GAME 模块 P2，用户实测（完整对局dump）确认。**排查发现问题范围比 issue 原文更窄、根因更简单**：issue 原文描述"依赖 over 判定对局是否结束的逻辑（结算展示、房间状态、回房恢复等）会把已结束的对局误判为未结束"，暗示这是游戏状态本身的持久化不一致；但全项目 grep `g.over` 后确认——**`g.over` 从来不是一个真实存在的持久化字段**：`checkWin`（`game.js`）结束对局时只写 `g.phase='over'`（+`winner`/`winSide`），全项目其它每一处判断"对局是否结束"的地方（`render-controls.js:1713`、`bot.js:163`、`ai-bot.js:1633` 自己的 `aiTestLastObservedPhase` 逻辑）全部直接比较 `g.phase==='over'`，从未有任何代码写过 `g.over`——这个字段在真实 Firebase 房间状态里根本不存在（值为 `undefined`），不是"两个字段各自持久化但没同步"。真正的 bug 位于**唯一**读取这个字段的地方：`ai-bot.js:1451` 的 `buildAiDecisionDump()`（CORE-72/73/75 那批任务实现的"一键导出诊断dump"功能，issue 用户附带的 `sgs-dump-*.json` 正是这个函数的导出产物）里写的是 `over: !!g.over`——读一个恒为 `undefined` 的字段，`!!undefined` 恒为 `false`，序列化进导出的 JSON 后就是用户在 dump 里看到的 `"over": false`，和同一份导出里 `"phase": "over"` 自相矛盾。**验收标准的"任意客户端/重进房间读到的状态一致"这条要求，实际结论是从一开始就是满足的**——真实房间状态（Firebase）里根本没有 `over` 这个字段可供不同客户端读出不一致的值，不一致只发生在这一处本地导出计算里，不涉及跨客户端同步问题。**修复**：`buildAiDecisionDump` 的 `over` 字段改为 `g.phase==='over'`，直接从游戏状态里唯一权威的"对局是否结束"信号（`phase`）派生，不再读取那个从未被写过的字段。**刻意不做的另一种修复方向（如实记录取舍）**：没有选择"给 `g` 补一个真实的 `over` 布尔字段，并在 `checkWin` 的每个结束分支里同步写入"这条路线——那会在游戏状态里引入第二份"对局是否结束"的真相，`checkWin` 当前有 3 个独立的结束分支（组队/身份局/乱斗），以及未来任何新增的结束路径都需要记得同步维护这两个字段，这正是"多处持有同一份语义、容易在某处漏写导致状态分裂"这类 bug 的经典温床——现有的单一事实来源（`g.phase`）已经完全够用，没有必要为了凑一个字面上叫 `over` 的字段而制造这种维护负担。**测试**：`testclass/run_ai_decision_panel_test.js` 新增3项——① `phase==='over'` 时导出的 `over` 应为 `true`（不预先设置 `currentG.over`，精确复现真实生产状态"这个字段从不存在"）；②对照组，`phase!=='over'` 时导出的 `over` 仍应为 `false`（无回归）；③破坏性验证——临时用逐字复刻旧 bug 写法（`!!g.over`）的版本替换 `buildAiDecisionDump`，验证确实会重新观察到 issue 描述的症状（`phase==='over'` 但导出 `over:false`），证明前两条断言有真实鉴别力、不是巧合通过。**红绿验证**：`git stash` 还原改动前的 `ai-bot.js` 重新跑测试，新增的第①条正确报红（"phase===over 时导出的 over 应为 true,实际 false"），第②条（对照组）修复前后行为一致、维持通过（符合预期——这条本来就该在改动前后都成立），恢复后全部转绿。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：改动的 `buildAiDecisionDump` 只有一处已知调用点（导出按钮），且改动只涉及单个纯计算表达式（无状态写入、无副作用），不触碰 `pending`/`normalize`/`tx`/机器人决策分发等高连带风险子系统，两条轴均不命中——按 CORE-124 规则可以只跑最小相关测试集，但考虑到 `ai-bot.js` 是被超过10个既有测试文件共享加载的文件（和 CORE-110/CORE-83 那几次"文件本身被广泛共享，从宽判断"同一类先例），出于一致性跑了全量 `run_all_tests.js`，连续两次 **97/97 全过**（中途一次 `run_bot_observability_test.js` 单独失败、单独重跑通过，确认是与本次改动无关的既有 flaky 用例，非本次改动引入）。`index.html`：`ai-bot.js?v=400→401`。
- **CORE-80（issue #127）修复诊断dump导出的玩家列表缺失身份字段（2026-08-16, issue-fixes-22 分支）**：UI 模块 P2，用户实测（完整对局dump）。**issue 原文误判为真实游戏逻辑缺陷**："游戏结束时不揭示所有玩家身份"——核实发现这条逻辑早在 `commit 81b95a5`（2026-07-19，比这次 issue 用到的 dump 早了近一个月）就已经实现：`checkWin`（`game.js`）身份局分支在真正判定出胜负后 `g.players.forEach(p=>{ if(p) p.roleRevealed=true; })` 批量翻转，座位卡靠既有 `canSeeRole` 渲染自动展示全部身份色块（"结束时全员身份揭晓"设计规格§6.5的实现），`testclass/run_identity_mode_test.js` 的 Task10 四项断言（端到端反贼杀主公触发游戏结束→全员`roleRevealed=true`+`canSeeRole`全16组合可见、ffa模式回归、未结束路径不提前翻转、UI数据层验证）本次任务重新跑过一遍，依然全绿——这条链路完好，不是这次的根因。**真正的根因和 CORE-81（同一批 issue，同一份 dump）完全同一类**：诊断dump导出功能（`ai-bot.js` 的 `buildAiDecisionDump`，CORE-72/73/75 那批任务实现）的玩家列表映射从一开始就**只导出了 `seat`/`name`/`general`/`isBot`/`alive`/`hp`/`maxHp`，从未包含 `role`/`roleRevealed` 这两个字段**——不是游戏状态本身缺失身份信息，是这一处诊断导出功能本身的字段映射不完整，issue 用户附带的 dump 里看不到任何身份数据，纯粹是因为导出函数压根没把这个字段搬进去，和游戏结束时座位卡实际渲染出的身份信息（真实存在）毫无关系。**这是同一批 issue 里第三次定位到 `buildAiDecisionDump` 字段映射不完整/有误的问题**（#128/CORE-81 是 `over` 字段读了一个从未被写过的字段；这次是身份字段整个缺失）——这个函数本身自 CORE-72/73/75 实现以来，随着游戏功能持续增加，字段清单没有跟着同步维护，值得作为一个通用教训记录（见下方 `docs/methodology.md` 补充）。**修复**：`buildAiDecisionDump` 的玩家列表映射补充 `role` 字段，**不是无条件导出 `p.role`**——身份是隐藏信息，这份诊断dump可能被转发给别人看（和同一函数里"刻意不导出aiApiKey"同一条既有纪律），必须遵守和真实 UI 完全同一套可见性规则：复用 `canSeeRole(g, mySeat, i)`（`data.js` 里判断身份可见性的唯一入口——自己的身份/主公恒公开/已阵亡揭晓/游戏结束后批量揭晓这几种情况下才可见），只有这名导出者（`mySeat`）此刻在界面上本来就能看到的身份才会出现在导出结果里，看不到的身份导出为 `null`，导出的信息量和真人玩家在界面上实际看到的完全一致，不会因为多了一份"完整dump"就意外泄露游戏进行中尚未合法揭晓的身份；身份文字用已有的全局 `ROLE_LABEL`（`data.js`）转换成"主公/忠臣/反贼/内奸"，和座位卡/日志既有文案一致；非身份局（`gameMode!=='identity'`）`canSeeRole` 恒返回 `false`，`role` 恒为 `null`，无回归。**测试**：`testclass/run_ai_decision_panel_test.js` 新增4项——①身份局对局结束后，导出的玩家列表应带上已揭晓的身份（三个座位分别验证主公/反贼/忠臣三种身份标签正确转换）；②对照组：对局进行中，未揭晓的身份不应导出（验证"主公恒公开"和"自己对自己可见"两条 `canSeeRole` 分支的边界，同时验证真正未揭晓的座位确实导出 `null`，不会因为加了这个字段就意外泄露）；③对照组：非身份局（ffa）不应导出任何 `role`（无回归）；④破坏性验证——临时把导出逻辑换成不经过 `canSeeRole`、无条件导出 `p.role` 的旧写法，验证确实会重新泄露进行中未揭晓的隐藏身份，证明对照组②的断言有真实鉴别力、不是巧合通过。**红绿验证**：`git stash` 还原改动前的 `ai-bot.js` 重新跑测试，①②③三条正确报红（①②因为字段整个不存在，`p.role` 恒为 `undefined`；③因为 `undefined!==null` 同样触发报红，属于同一根因的自然连带），④（破坏性验证本身内联复刻旧写法，不依赖外部改动状态）保持通过，恢复后全部转绿。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：改动的 `buildAiDecisionDump` 只有一处已知调用点（导出按钮），本次改动本身不触碰 `pending`/`normalize`/`tx`/机器人决策分发等高连带风险子系统，两条轴均不命中——但和 CORE-81 同一次判断依据（`ai-bot.js` 是被超过10个既有测试文件共享加载的文件），出于一致性跑了全量 `run_all_tests.js`，连续两次 **97/97 全过**（96+1，新增测试并入既有测试文件，测试文件总数不变）。`index.html`：`ai-bot.js?v=401→402`。
- **CORE-85（issue #132）修复聊天框说话者跨局后错乱：渲染改为消息快照优先，不再优先查当前局座位（2026-08-16, issue-fixes-23 分支）**：UI 模块 P2，源码逻辑确认，issue 正文已把根因/证据/修复方向定位得非常精确（含具体行号），本次任务基本按 issue 建议方向直接实现，过程中发现并修正了 issue 建议写法本身的一个边界缺陷。**根因**（issue 原文已确认，复核无误）：聊天消息跨局保留（存 Firebase，`limitToLast(80)` 全量拉取，`room-lifecycle.js:80` 不清空），发送时 `pushChatMessage`（`render-log.js`）已经把 `playerName`/`general` 当场快照进消息本身，但渲染时 `chatSenderLabel` 优先查**当前局**该座位的玩家/武将（`g.players[msg.seat]`），快照仅作兜底——跨局后座位-玩家映射变化（尤其 #104 开局座位随机重排 `shuffleSeats`，或有人离开/补机器人），上一局的聊天消息会被错标成新局同座位玩家的名字。**修复**：`chatSenderLabel` 改为快照优先。**过程中发现并修正的一处 issue 建议写法本身的边界缺陷**：issue 建议的修复表达式是 `msg.general || (p && p.general)`（用 `||` 做优先级选择）——但这个写法有一个真实缺陷：`pushChatMessage` 发送每条消息时无条件写入 `general: p.general||null`，如果发送者在大厅阶段（尚未选择武将）发消息，快照里 `msg.general` 会是合法的、有意义的 `null`（"发送时确实没有武将"这个事实本身），`||` 运算符会把这个 falsy 值误判成"快照缺失"，转而退回查当前局座位的武将——如果这条消息后来被查询的座位换了个已经选好武将的玩家，会错误地把"大厅阶段发的、本该显示玩家名"的消息渲染成【武将名】，这本质上是同一个 bug 的一个变种（仍然是"当前局状态污染了历史消息快照"），只是没有被 issue 原文的建议写法完全堵住。修复为用 `typeof msg.playerName==='string'` 作为"这条消息是否带着完整快照"的判定依据（`pushChatMessage` 无条件写入 `playerName`，缺省值 `'玩家'`，恒为字符串），一旦确认带快照，快照里 `general` 字段的值（包括合法的 `null`）就是唯一真相，不再用 `||` 退回查当前局；只有真正连 `playerName` 字段都没有的消息（理论上不会出现，纯防御性兜底）才退回查询当前局座位。座位颜色维持 issue 要求的"按 `msg.seat` 取色"不变，调用点（`render-log.js` 渲染聊天气泡处）本身就已经是这个逻辑，未改动。**测试**：`testclass/run_chat_tts_test.js` 新增5项——①跨局座位重排后旧消息应显示发送者本人的快照武将名（精确复现 issue 描述场景）；②快照武将名缺失（大厅阶段发的消息）时应退回快照的玩家名，不查当前局座位（这条断言第一次实现时用 issue 建议的 `||` 写法直接测出了上面那个边界缺陷，随后修正实现和断言）；③对照组：消息本身完全没有快照字段时才退回查当前局座位（防御性兜底路径的回归验证）；④同局内本人消息无回归（快照和当前局一致时正常显示）；⑤破坏性验证——逐字复刻 issue 描述的旧 bug 写法，验证确实会重新观察到"跨局后显示成新局玩家"的症状，证明第一条断言有真实鉴别力。**红绿验证**：`git stash` 还原改动前的 `render-log.js` 重跑测试，前两项（跨局重排、快照名兜底）正确报红，其余三项（防御性兜底路径、同局内无回归、破坏性验证本身）修复前后保持一致（符合预期——这几条本来就该在改动前后都成立），恢复后全部转绿。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：`chatSenderLabel` 只有一处已知调用点（聊天气泡渲染），改动本身是纯函数级的读取优先级调整，不触碰 `pending`/`normalize`/`tx`/机器人决策分发等高连带风险子系统，两条轴均不命中——按规则可以只跑最小相关测试集，考虑到 `render-log.js` 是被多个既有测试文件共享加载的文件，出于一致性仍跑了全量 `run_all_tests.js`，连续两次 **97/97 全过**（96+1，新增测试并入既有测试文件，测试文件总数不变）。`index.html`：`render-log.js?v=396→397`。
- **CORE-86（issue #133）平板端适配：竖屏不再被强制横屏遮罩挡死 + 平板触控目标抬到44px（2026-08-16, issue-fixes-24 分支）**：UI 模块 P2。**issue 描述的两个问题里，第2个（平板横屏未针对大触屏优化）核实后已不成立**：`index.html` 早已有一整块平板专用断点（`@media (min-width:641px) and (max-width:1199px), (min-width:641px) and (hover:none) and (pointer:coarse)`，第二个分支正好覆盖 iPad Pro 横屏 1366px 这种"宽度超出 1199 但仍是触屏"的机型），另有一块 `(min-width:641px) and (max-width:900px) and (orientation:portrait)` 平板竖屏专用规则，均来自更早的 commit `242c54e`「完善多设备牌桌布局与完整日志」——issue 正文写的"平板横屏套基础 `.opp-row` 规则、未针对大屏触控设计"是基于过时状态的判断。**第1个（平板竖屏被遮罩挡死）是真实 bug**，且排查中发现一个强证据：上面那块"平板竖屏专用 CSS"**在这次修复之前是完全执行不到的死代码**——`checkLandscapeGate()` 无条件拦截所有竖屏，那块 CSS 写了也永远没机会生效，说明当初的设计意图本来就是要支持平板竖屏，只是遮罩这一侧漏改了。**产品决策**：issue 正文明确要求"若判定平板竖屏仍提示横屏是可接受现状，需在 issue 中明确拍板"——已向用户确认，选择**允许平板竖屏进入**。**修复1（遮罩）**：`render.js` 新增 `shouldShowLandscapeGate()`，把原来"只看方向"改成"方向 + 宽度"双条件，`checkLandscapeGate` 改调它；`isPortrait()` 本身一个字没改（全项目只有遮罩这一个消费者，语义保持不变）。阈值取 `LANDSCAPE_GATE_MAX_WIDTH=640`，和 CSS 侧手机竖屏断点 `@media(max-width:640px)` **严格互补**（不留空隙也不重叠）：width≤640 恰好是手机竖屏 CSS 生效的范围，遮罩只在这个范围内出现；641 及以上交给平板 CSS、放行竖屏。现实机型不会落进夹缝——竖屏最宽的手机（iPhone Pro Max 一类）约 430 CSS px，折叠屏展开约 768px（本来就该按小平板对待）。拿不到有效宽度的极端环境保持旧行为（保守提示横屏），不因"量不到宽度"就默认放行。**修复2（触控目标）**：真机验证发现"结束出牌"这类 `#controls` 按钮在平板上实测只有 94×40px（基础规则 `padding:9px 16px`+`font-size:14px` 算出来约 40px 高），低于触屏 44px 通行下限，命中 issue 验收标准里"按钮可触控（不误触/不偏小）、≥44px 触控惯例"这一条——补 `#controls button{min-height:44px;}`。**这条规则的第一版实现踩了 CLAUDE.md 规则22 的坑并当场被实测抓出来**：最初直接写进上面那块平板 `@media` 里，但那块的第一个分支 `(min-width:641px) and (max-width:1199px)` **只看宽度**，手机横屏（iPhone SE 横屏 667×375）宽度 667 同样落在这个区间——实测把手机横屏的页面总高从 382px 抬到了 403px，而那个视口只有 375px 高、本来就在溢出边缘，等于让一个已经吃紧的档更吃紧，违反 issue"手机零回归"的要求。改为单独开一条带 `(min-height:521px)` 高度门槛的 `@media`（521 和既有的手机横屏紧凑块 `(max-height:520px) and (orientation:landscape) and (pointer:coarse)` 严格互补），把手机横屏排除在外——手机横屏继续走它自己那套刻意压小的紧凑尺寸，平板（最矮的 iPad 横屏也有 768px 高）才抬触控目标。选择器带 `#controls`（特异性 1,0,1）而不是裸 `button` 也是刻意的：只抬游戏内真正需要手指点的操作/响应按钮，右上角 `.icon-btn`、AI 调试面板 `.aitest-foot-btn` 这些工具类按钮各自有 class 规则（特异性 0,1,0）本来就压得住裸 `button`，写成裸 `button` 反而会造成"有的生效有的不生效"这种看起来随机的结果。**真机验证（Playwright，按 CLAUDE.md 规则18/19/22）**：新建 `tools/verify_responsive_layout.js`，用真实 Chromium 跑 11 档视口（iPad 竖/横、iPad Pro 竖/横、安卓平板竖、641边界内小平板竖、640边界上手机竖、iPhone SE 竖、Pro Max 竖、手机横屏 SE、桌面 1440×900），**宽度和高度两个维度分别拉到下限单独测**；样本一律取最刁钻的（8人局、最长武将名"颜良文丑"、装备槽全满含最长装备名"青龙偃月刀"）；断言全部程序化量取（遮罩显隐、横向溢出 `scrollWidth>innerWidth`、座位卡数量/尺寸下限、武将名是否溢出卡片边界、触控目标最小边长、JS 运行时错误、手机横屏页高回归基线），不靠肉眼看截图。**11/11 全绿**。**两轮破坏性验证**：①把遮罩还原成旧的"无条件拦竖屏"，4 档平板竖屏立即报红（遮罩=显示）；②把触控目标规则的 `(min-height:521px)` 门槛去掉（第一版实现的写法），手机横屏立即报红两条（按钮被误抬到 44px + 页高 403 > 基线 384）——证明这些断言都有真实鉴别力，不是巧合通过。**常规回归测试**：新增 `testclass/run_tablet_landscape_gate_test.js`（13 项，纯 Node 无需浏览器，进常规套件）——平板竖屏4档放行、手机竖屏3档仍被拦、横屏3档一律放行、640/641 边界精确性（证明阈值不是写成 `>=` 或差一位）、宽度无效时的保守兜底分支、以及一条结构断言钉住 `index.html` 里那条触控目标规则必须带 `(min-height:521px)` 门槛（防止以后有人"顺手"把它并回宽度-only 的平板块，重新引入手机横屏回归）。测试刻意走完 `checkLandscapeGate()` 整条链路（而不是直接读判定函数返回值），确保 `toggle('hidden', ...)` 的取反方向也被钉住。`git stash` 验证修复前 13 项里 6 项正确报红。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：改动的 `checkLandscapeGate` 是 `render.js` 顶层加载即执行、并注册 `resize`/`orientationchange` 监听的全局 UI 逻辑，`render.js` 又是被 20+ 既有测试文件共享加载的文件——从宽判断跑全量 `run_all_tests.js`，**98/98 全过**（97+1新增；中途一次 `run_bot_observability_test.js`/`run_wuxie_public_window_test.js` 各失败一次，单独重跑均通过、再跑全量也恢复 98/98，是本次会话中已多次出现的既有 flaky 用例，与本次改动无关——本次改动只碰遮罩判定和 CSS，这两个测试都不加载遮罩逻辑也不读 CSS）。`index.html`：`render.js?v=407→408`。**未做（如实标注）**：没有真实平板硬件实测（环境不具备），验证全部基于 Playwright 真实 Chromium 的视口模拟 + `hasTouch`/`isMobile` 设备能力模拟——对 `@media (hover:none) and (pointer:coarse)` 这类能力查询，模拟和真机的行为一致性是可信的，但真机上的实际手感（字号观感、手指遮挡）仍建议用户在自己的 iPad 上过一遍。
- **CORE-90（issue #137）修复 seatPick 选目标路径的阵营安全策略可被绕过：把"策略禁止"从软评分改成硬过滤，并把 harmful/helpful 分开建模（2026-08-16, issue-fixes-25 分支）**：AI 模块 P1，源码逻辑确认。是 CORE-89（#136）的姊妹问题——issue 正文明确说明"根因相关但修复面不同，不合并"（#136 管手牌/出牌窗口候选，本条管 seatPick 技能目标候选）。**根因**：`pickBestCandidateSeat`（`bot.js`）把 `botTargetScore()===-Infinity`（策略禁止）当成"分数很低的候选"而不是"禁止选择"，有两条确定错误路径：①`if(candidates.length===1) return candidates[0].seat;` 唯一候选完全不做策略检查直接返回；②所有候选都是 `-Infinity` 时，因为 `bestScore` 初值就是 `-Infinity`、没有任何 `s>bestScore` 成立，最终返回初始化时那个 `candidates[0].seat`（一个禁止目标）。`pickHealFallbackSeat`（青囊）则只给"明确敌方"加一个 1000 的**软惩罚**——软惩罚只能保证"有别人可选时不优先选敌人"，唯一候选就是明确敌方时 `1000+hp` 仍是当时最小的 key，机器人照样给敌人回血。**这不是 CORE-89 那次修复的遗漏，是它覆盖不到的另一条路径**（排查后确认，如实区分）：CORE-89 在 `seatPickBuildCandidates` 这个**聚合出口**加了硬过滤，但 11 个 seatPick 技能各自的 `fallbackSeat` 都是直接调 `BOT_SEAT_PICKS.xxx.buildSeatCandidates()`（未过滤的**原始**清单）再交给 `pickBestCandidateSeat`，绕过了那道过滤；太史慈【天义】选拼点目标那处（`bot.js` `tianyiPickTarget` 分支）更是自己现攒候选数组直接调 `pickBestCandidateSeat`，和 seatPick 聚合路径完全无关，一点都没被保护到。**另外发现 CORE-89 遗留的一个方向性缺陷**：那次为了不让"禁止选自己人"的敌我过滤把青囊的合法治疗对象全过滤光，用一张写死的 `HARMLESS_SEAT_PICK_KEYS={qingnang:true}` 名单把青囊**排除在过滤之外**——但"排除在过滤之外"等于**完全不过滤**，青囊照样能把回血送给已知反贼，只是换了个方向出错，本次一并修正。**修复**：①新增 `botTargetHelpfulAllowed(g,seat,targetSeat)`——帮助型效果的敌我边界，是 `botTargetRelationAllowed`（有害型，CORE-89 建的）的镜像：有害效果对已知反贼是"允许"（忠臣该打反贼），帮助型对已知反贼却是"禁止"（忠臣不该给反贼回血），方向正好相反，**两者不能共用同一个谓词**，这正是 issue 要求分开建模的原因。判据只拦"明确的敌对阵营"（忠臣/主公→已知反贼；反贼→已知主公/忠臣），身份未知一律放行（帮助未知身份最多浪费一次技能，不像有害效果打错人那样直接送掉自己阵营的胜利条件，不需要再叠 suspicion 门槛）；组队模式只帮同队；自己给自己回血恒允许。②新增统一分发入口 `botTargetPolicyAllows(g,seat,targetSeat,effectKind)`，候选生成层一律经它问策略，不必各自记住该调哪个谓词；`effectKind` 缺省按 `'harmful'` 处理——漏声明时按更严格的一侧兜底（把帮助型误判成有害，最坏是少发动一次技能；把有害误判成帮助，就会真的打到自己人）。③`pickBestCandidateSeat` 改为**先硬过滤再选择**（禁止目标直接从候选里删掉），过滤后为空返回 `null`；自己作为候选（allowSelf 场景）不参与敌我过滤（对自己用技能不存在打错阵营）。④`pickHealFallbackSeat` 同样改成硬过滤 + 空则 `null`，删掉那个 1000 软惩罚（职责已上移到硬过滤，留着反而是两套重叠的机制）。**调用方不需要改**——`seatPickLocalFallback` 拿到 `null` 会 `continue` 试下一个技能（CORE-113 建立的行为），天义分支拿到 `null` 会 `cancelTianyi`，两处本来就正确处理 `null`。⑤按 issue 要求**审计全部 12 个 `BOT_SEAT_PICKS` 注册项并逐项显式声明 `effectKind`**：`guhuoTarget`/`xuanfeng`/`duanliang`/`qixi`/`guose`/`wusheng`/`longdan`/`shuangxiong`/`tiaoxin`/`fanjian`/`quhuDamage` 共 11 项为 `harmful`，`qingnang`（青囊回血）是全表**唯一**的 `helpful`；`seatPickBuildCandidates` 的过滤改为按声明的 `effectKind` 分发，不再有"某个技能干脆不过滤"这种状态。**内奸零回归**：harmful/helpful 两侧都不设硬边界（issue 明确要求"内奸保持现有动态策略"）。**测试**：新增 `testclass/run_core90_seatpick_policy_test.js`（17 项），逐条对应 issue 的验收标准：唯一 `-Infinity` 候选返回 null、多候选全 `-Infinity` 返回 null、存在允许目标时只从非禁止目标里选（并额外加一条"把禁止目标排在候选第一位"的顺序无关性断言，排除"靠顺序侥幸通过"）、忠臣/主公/反贼三种身份的敌我边界各一条、帮助型不无脑帮敌方 + 两条对照（有自己人时正常选血最低的自己人、给自己回血恒允许）、内奸零回归、非身份局零回归、allowSelf 零回归、**全表 effectKind 声明完整性**（含数量断言，新增技能漏声明会直接报红，防止以后有人加技能时跳过这次审计）、天义这条 CORE-89 未覆盖路径的独立验证、以及破坏性验证（把策略谓词换成恒真，验收①②⑥重新失守）+ 恢复自检（确认破坏性验证的 finally 真的复原了，后续断言可信）。**红绿验证**：`git stash` 还原改动前的 `bot.js` 重跑，17 项里 **11 项正确报红**（覆盖全部验收标准条目），恢复后全绿。**测试范围判断（CORE-124/CLAUDE.md 第29条）**：改动的 `pickBestCandidateSeat`/`pickHealFallbackSeat`/`seatPickBuildCandidates` 属于 `bot.js` 机器人决策核心分发机制（轴 B 逐字点名），且 `pickBestCandidateSeat` 有 12 处调用点（轴 A），两条轴均命中——跑全量 `run_all_tests.js`，连续两次 **99/99 全过**（98+1）。**额外做了 soak 压测**（本次改动会让"候选被全部过滤掉"成为一种新的正常状态，必须确认不会让机器人在某个技能上因为拿不到目标而卡死）：60 局 2~6 人 + 100 局 2~7 人，合计 **160 局零卡死、零步数超限、零崩溃**——`botDecide` 对空候选返回 `false` 让调用方继续往下走（CORE-113 建立的 fall-through 语义）在这条新路径上同样成立。`index.html`：`bot.js?v=411→412`。
