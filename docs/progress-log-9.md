# Progress Log 9（2026-08-09 起）

> 本文件按 CLAUDE.md 防复发规则块维护：CLAUDE.md 本体只记架构约定变化，任务改动记录一律写进
> `docs/progress-log-N.md` 当前最新分段。当前分段=本文件（progress-log-8 已达 148KB，本批记录追加后
> 将超 150KB，故新建 9）。

- **AI托管 prompt 与平时彻底分离（修复"AI 不返回决策理由"）+ AI测试改名AI托管 + 信息窗增量渲染不整窗刷新 + 测试探针语法修复**：用户报告托管模式下 AI"都不返回决策理由了"，贴出真实决策链证据（system prompt 里"只输出 {\"choice\":数字}，不要解释。"+托管附加"返回choice的同时附理由"、user prompt 末尾"只返回 {\"choice\":数字}"三条指令互相矛盾，AI 服从最后一条 → 只回 choice 不回 reason）。**根因**：`buildBotDefaultSystemPrompt`（bot-ai-bus.js）默认模板自带"只输出 {\"choice\":数字}，不要解释。"，`buildBotDefaultUserPrompt` 末尾又加"只返回 {\"choice\":数字}"；托管模式只是往 system prompt **追加**一句"附理由"指令，没有消除前两条，AI 按最后指令行事。**修复（prompt 分离）**：托管命中时用两个托管专用构造函数——`buildAutopilotSystemPrompt(sysText)` 把"只输出 {\"choice\":数字}，不要解释。"**替换**成"只输出 {\"choice\":数字,\"reason\":\"理由文本\"}，并用一句中文解释理由。"并统一在末尾追加托管标记行"(本次为AI托管)返回choice时必须同时附一句中文理由,格式 {\"choice\":数字,\"reason\":\"理由文本\"}。"（自定义 prompt 若仍残留"不要解释"，标记行作为最后指令压过它）；`buildAutopilotUserPrompt(userPrompt)` 把 user 末尾"只返回 {\"choice\":数字}"**替换**成"请按格式返回 {\"choice\":数字,\"reason\":\"理由文本\"}"（正则 `/只返回 \{"choice":数字\}\s*$/`，替换不中/未传 userPrompt 时追加兜底，保证最后一条指令恒为理由格式）。**未托管路径完全不经过这两个函数，行为零变化**（回归锁定：未托管 systemPrompt 应保持默认含"不要解释"、user 保持"只返回 {choice}"）。`aiTestLastCall.prompt` 采集与实际发送逐字一致（构造顺序统一后天然一致）。**改名 AI测试→AI托管**：所有用户可见文案与注释（index.html 按钮 title/信息窗 header/CSS 注释、ai-bot.js 开关 title、bot-ai-bus.js/bot.js/render.js 注释）统一改"AI托管"，prompt 内标记也改"(本次为AI托管)"；**内部标识（变量 aiTestAutopilot/aiTestLastReason、函数 toggleAiTestAutopilot、DOM id aiTestBtn/aiTestModal、CSS 类 aitest-*）刻意保留不动**（纯内部标识，改名只影响用户可见层，避免大范围无收益改动）。**信息窗增量渲染**：原 `appendAiTestRecord`/`aiTestFillPendingRecord` 每次操作都整窗 `innerHTML` 重建——已展开的详情被收起、滚动位置重置回顶部（用户诉求"不要每次追加新消息时刷新窗口"）。改为：抽 `recordHtml(rec,i)`/`recordDetailHtml(rec,i)` 两个纯函数；`appendAiTestRecord` 用 `body.insertAdjacentHTML('beforeend', ...)` 增量插入单条（只动新记录节点）；`aiTestFillPendingRecord` 用 `records.indexOf(aiTestPendingRecord)` 定位该条、`det.outerHTML = recordDetailHtml(...)` 只替换这一条的详情区，替换前记住展开状态、用户正点开看的记录回填后保持展开。**测试探针语法修复（重要，测试文件当时根本跑不起来）**：上一条任务在 `run_ai_test_button_test.js` 文件**顶层**（模板字符串 testCode 之外）追加了 `await check(...)` 探针——node 20.19 的 `--experimental-detect-module` 检测到顶层 await 自动按 ESM 加载 → `require` 未定义报 "require is not defined in ES module scope"；加 `--no-experimental-detect-module` 强制 CJS 又报 "await is only valid in async functions"。**这个探针从未成功跑过**。修复：探针挪进 testCode 模板字符串（Task6 之后、结果打印之前，参与主流程 await 链与 `__testDone`），删除文件顶层残留。探针内容本身保留并补注释——它锁定"确定性决策（单候选路径绕过 callAiChooseIndex）骨架记录 reason 恒 null"是**符合预期的行为**（不是 bug），与本次修的"多候选托管路径 prompt 矛盾"是两条独立原因。测试 stub：`document.getElementById` 返回对象补 `insertAdjacentHTML`（增量渲染路径需要）。**测试**：`run_ai_test_button_test.js` 23 项全过（新增"托管时 systemPrompt 含托管标记且不含不要解释 / 未托管保持默认零变化 / 托管时 user prompt 末尾替换为理由格式"3 项分离断言 + 探针 1 项）；全部回归 ai-bus 10 套件（core 10 / l1 26 / l2 24 / l3 223 / info 17 / part2 11 / c_window 34 / lordskill 45 / summary 15 / timeout 20）全过 0 失败；ai-bot/bot-ai-bus/bot/render 语法检查过。`index.html`: `?v=339→340` ×15。**已知边界**：托管模式下 `maxTokens` 仍是默认 80，超长理由可能被截断（未专门调整，AI 通常 80 tokens 够一句中文理由）；信息窗 DOM 增量行为（滚动位置/展开保留）在 vm stub 环境只保证不抛错，真实浏览器效果需联机验证。
- **AI 决策参考段调整（去保守倾向，杀该打就打）**：用户实测托管后反馈"AI 有些过于保守"——原 `buildBotDefaultSystemPrompt` 的决策参考段里"手牌耗尽裸拼往往替别人火中取栗;多数决策宁可保守不出,也不要打空自己"被 AI 过度执行成"存牌优先于进攻"。用户明确需求："杀这种资源牌消耗掉的好处远大于弃置；不想要 AI 冒进，但不贸然进攻不代表要完全走向反面"。**改动**：把这两句替换为"杀等进攻资源牌有机会就该果断打出——造成伤害/逼出闪/压低血量,收益远大于存着不用(存着也可能被弃置/被顺手牵羊,等于浪费);一直不出牌等于让对手零压力发育;但别冒进:对方有明显克制(技能克制/防具/手牌充裕)时该收手就收手,不冒进不等于不出"。保留"1点体力≈2张手牌"与"关键防御牌留关键时刻"两条原有参考。改动只涉及 `buildBotDefaultSystemPrompt` 一个字符串（bot-ai-bus.js），未托管/托管两条路径共用同一模板，都受影响；托管专用 `buildAutopilotSystemPrompt` 的替换逻辑（"只输出{choice}不要解释"→理由格式）不受影响。**测试**：`run_ai_bus_core_test.js` 10/10（含"含1点体力"断言）、`run_ai_test_button_test.js` 23/23 全过。`index.html`: `?v=340→341` ×15。**注意（可能的自我强化残留）**：aiSummary 是 AI 自己维护的跨回合摘要，若旧摘要里已记录"我选择保守打法"等倾向，会继续注入后续 systemPrompt 几回合——如改后托管/机器人仍显保守，可用信息窗「清除AI记忆」按钮清空摘要再观察。
- **托管 prompt 格式指令去重（省 token）**：用户指出托管模式下 system prompt 里回复格式出现两次——`buildAutopilotSystemPrompt` 把"只输出 {"choice":数字}，不要解释。"替换成理由格式（开头一处），又无条件追加含格式的托管标记行（末尾一处），同一格式指令重复。**修复**：替换改为直接删除整句"只输出 {"choice":数字}，不要解释。"（前面已有"只能选列表内选项。"，语义不丢），格式说明统一收敛到末尾托管标记行，全 prompt 只出现一次；"不要解释"矛盾照旧消除，自定义 prompt 残留"不要解释"仍由末尾标记行压过。**验证**：vm 沙箱实测 `buildAutopilotSystemPrompt()` 输出中 `"choice"` 格式指令出现次数 1（改前 2），且不含"不要解释"、决策参考为最新"杀该打就打"版本；`run_ai_test_button_test.js` 23/23、`run_ai_bus_core_test.js` 10/10。`index.html`: `?v=341→342` ×15。**附带确认**：用户贴出的旧 prompt 里"决策参考"还是"宁可保守不出"旧文案——那是 ?v=340 及更早线上缓存的记录，新决策参考（杀该打就打）与本次去重已随 ?v=341/342 上线，手机端需等缓存刷新。
- **AI 决策参考补充：无用牌处置策略（用户提出的牌理，已确认合理并写入）**：用户提出"注定被弃置的无用牌，打出优于弃置；打出没用又不会被弃置的牌可以保留；诸葛亮空城这类'手牌清零=防御'的技能除外"。评估：①弃置=零价值、打出=潜在价值（逼闪/逼无懈/消耗），正期望；②保留垫手牌稀释"被顺手牵羊随机抽核心牌"的概率（本项目顺手对手牌是随机拿，牌多=核心被抽概率低），还可能被技能转化（龙胆/武圣）；③空城是真实例外（手牌清零=不能被杀/决斗）。**改动**：`buildBotDefaultSystemPrompt` 决策参考段末尾追加一句"注定会被弃置的无用牌,打出优于弃置(至少可能逼出闪/无懈);打出无用且不会被弃置的牌可保留(垫手牌稀释被顺手牵羊抽中核心牌的概率);空城这类'手牌清零=防御'的技能除外。"——与前句"杀该打就打"自洽（有价值进攻牌该打就打，无价值牌按"会被弃→打出/不会弃→保留"分流）。**测试**：`run_ai_test_button_test.js` 23/23、`run_ai_bus_core_test.js` 10/10。`index.html`: `?v=342→343` ×15。
- **弃牌阶段 prompt 两处修复（同一"格式矛盾"bug 的第二处 + 闪的价值定位错误）**：①`buildDiscardSubsetSystemPrompt`（bot.js）原文"闪/酒等价值低的优先弃置"与同函数末尾"先想保留什么(关键防御牌优先)"及系统 prompt"关键防御牌(无懈/闪/桃)要留到关键时刻"**直接矛盾**（闪既被划入低价值又被要求优先保留），AI 无所适从；改"闪/桃/无懈等关键防御牌与装备优先保留,酒/多余的同名牌等价值低的优先弃置"。②该函数的格式句是"请只输出 {"choice":数字},不要解释。"（半角逗号+带"请"前缀），与 `buildAutopilotSystemPrompt` 的 replace 目标"只输出 {"choice":数字}，不要解释。"（全角逗号）**匹配不上**——弃牌阶段托管 prompt 里"不要解释"永远残留、与末尾托管标记行矛盾（和主 prompt 那条同根因，用户贴的 discard 记录里两句话同时在场即为证据）；改为与默认模板逐字一致的"只输出 {"choice":数字}，不要解释。"，托管时被正常删掉。**验证**：vm 沙箱实测托管弃牌 prompt 格式指令出现 1 次、无"不要解释"、无"闪/酒"矛盾措辞；`run_ai_bus_l3_test.js` 223/223、`run_ai_bus_l2_test.js` 24/24、`run_ai_test_button_test.js` 23/23、`run_ai_bus_core_test.js` 10/10 全过。`index.html`: `?v=343→344` ×15。**分支纪律变更（用户 2026-08-09 立下）**："我不说合并main绝对不可以合并，仅可以自由提交wenwen"——CLAUDE.md 三、10 已更新：默认开发分支 `wenwen_dev`（自由提交/推送），`main` 与 `chengcheng`（Pages 源）受保护，只有用户明确说"合并 main"才允许动；本 commit 及后续提交只推 `wenwen_dev`，main/chengcheng 停在 d1d63b3 未动。
- **AI 决策参考补充：装备牌该穿就穿**：用户提出"装备大多数情况下打出就好了"。评估：装备穿上立即获得射程/防御/距离修正或装备能力（诸葛连弩无限杀、仁王盾免疫黑杀、的卢+1马让 2 人局对手够不着），收益即时且长期；即使被顺手牵羊/过河拆桥拆掉，也已生效过，优于留在手里（留手里被顺走反而纯亏）。**改动**：`buildBotDefaultSystemPrompt` 决策参考段追加"装备牌大多数情况直接穿上就对了——立即获得射程/防御/距离修正或装备能力,收益远大于留在手里,别因为怕被拆/被顺手牵羊就不穿"。**测试**：`run_ai_bus_core_test.js` 10/10、`run_ai_test_button_test.js` 23/23。`index.html`: `?v=344→345` ×15。仅推 wenwen_dev。
- **recentLog 降噪 + 同窗意图链（AI 决策 token 效率与连贯性双修）**：用户提出 recentLog 大量消耗 token 但作用不佳。分析：①`buildBotVisibleState` 的 `recentLog` 是 `g.log.slice(-15)` 全量事件，例行事件（"轮到 X"/"X 摸了N张牌"/"X 弃置了N张牌"/"加入房间"/"游戏开始"）占一半以上，手牌数/轮次从局面就能推断、零决策价值；②177 个 pushLog 调用点无 kind 字段，不能按类型过滤，只能按文本规则；③同窗多步循环每步重发全量日志。**改动 A（降噪）**：新增 `buildBotKeyEvents(g)`（bot.js）——按文本规则过滤例行事件（`^轮到 ` / `摸了\d+张牌$` / `弃置了\d+张牌$` / `加入了房间` / `房间已创建` / `已添加机器人` / `^游戏开始`），只保留最近 6 条关键事件；注意拆装备日志格式是"X 弃置了 Y 的武器【…】"不匹配"弃置了N张牌$"规则，实测不被误滤。**改动 B（同窗意图链）**：`runBotActionWindow` 强C 循环维护 `windowHistory`，每步执行后 push 该步 `choice.label`（结束出牌不入链），下一步以 `state.lastActions = windowHistory.slice()` 字段注入可见状态——AI 每次调用虽独立，但明确知道自己上一步干了什么，不再靠 recentLog 猜；token 增量每步仅一条 label。**依赖确认**：aiSummary 更新（bot-ai-bus.js:123）输入同样是 `state.recentLog`——降噪后摘要原料质量反而更高（基于关键事件而非流水账），跨回合记忆不受损。**验证**：vm 实测 18 条混合日志过滤后 6 条关键事件全保留（使用/无懈/伤害/装备/拆装备），例行全滤；回归 l3 223 / c_window 34 / l2 24 / button 23 / core 10 全过。`index.html`: `?v=345→346` ×15。仅推 wenwen_dev。
- **AI 决策参考补充：濒死终结优先**：用户实测托管张飞局面——对手甄姬 1 血 3 手牌，张飞咆哮手里有杀，AI 却选"结束出牌阶段"并给出"节省手牌"的理由（该记录实为旧保守 prompt 跑的，新版"杀该打就打"已修正方向）。但暴露新盲区：决策参考没专门强调"低血/濒死优先终结"，AI 可能被 `shaUsed:true`+日志"已出过两次杀"误导而犹豫停手。**改动**：`buildBotDefaultSystemPrompt` 决策参考段"不冒进"后插入"对手处于低血量(1~2血)时,有伤害手段(杀/决斗/AOE)就该果断出手终结,别给对手留回合翻盘的机会,濒死时宁可多打也不收手"——与"不冒进"形成先抑后扬（有克制收手，但对手濒死时终结优先级最高）。**测试**：`run_ai_bus_core_test.js` 10/10、`run_ai_test_button_test.js` 23/23。`index.html`: `?v=346→347` ×15。仅推 wenwen_dev。
- **AI 输入 token 裁剪（Groq 免费层 TPD 100k 被触发后的降耗）**：用户实测中 Groq 返回 rate_limit_exceeded（TPD 100000，已用 99621）。分析：每次 AI 请求输入局面 JSON 约 1500-2000 tokens 占 96%（输出仅 80），输入裁剪是唯一有效手段。**改动①**：`buildBotDefaultUserPrompt`（bot-ai-bus.js）候选列表 map 去掉 `card:c.card` 字段——label 已含牌名（"出【杀】→是"），花色可从 `myHand[handIndex]` 反查（候选牌都来自手牌，handIndex 在字段里），每候选省 ~40 字符。**改动②**：新增 `botCardBriefMin`（bot.js，手牌投影精简版 `{name,suit}` 去 rank）——花色保留（黑杀打仁王盾/红桃无懈等决策需要），点数对 AI 决策价值低（判定/拼点无需精确规划）却是 token 大头；判定牌/鬼才牌等仍需 rank 的地方继续用原 `botCardBrief` 不受影响。**效果**：典型出牌窗（4 手牌+5 候选）每请求约省 100-200 tokens，强C 8 步循环一次出牌窗省 800-1600；叠加之前 recentLog 降噪（15→6 条）累计省 ~25-35%。**注意**：rate limit 期间 callAiChooseIndex 每次失败返回 null 走本地兜底（不卡死、被拒请求不扣 token），11 分钟后自动恢复。**测试**：l3 223 / c_window 34 / l2 24 / button 23 / core 10 全过。`index.html`: `?v=347→348` ×15。仅推 wenwen_dev。
- **组队模式 Task 5：startGame team 分支（队数≥2且每队≥1 校验 + team 专用日志）**：组队模式实施计划的一环。**改动（room-lifecycle.js startGame 内三处）**：①`gm` 判定从二元 `(gameMode==='identity')?'identity':'ffa'` 扩为三元 `(gameMode==='identity')?'identity':(gameMode==='team')?'team':'ffa'`——team 模式落地；②校验块从二支扩为三支：identity 不变（4~8人、仅pick），新增 team 分支（`n<2` 拒绝、仅 pick/random、`teamSet` 遍历收集有人的队伍号、队伍数≥2 否则 return 静默拒绝），ffa 走原 `MIN_PLAYERS`+pick/random；③`g.gameMode=gm` 后追加 team 专用日志"组队模式开启,队伍分配: 玩家·队N"（队伍号+1 显示、无队者兜底显示队0）。**发武将路径复用既有 pick/random 分支**（finishGeneralAssign 通用，无需新代码）。startGame 校验失败是静默 return（不弹 UI），UI 层拦截在 render-controls（Task 2 已加）。**测试（run_team_mode_test.js 追加 3 条）**：队数<2 拒绝、全员同一队拒绝、两队合法进入 pickingGeneral；14 条旧用例零回归，全套 17/17。**brief 测试构造的两处修正**（实证发现后偏离 brief 逐字）：①测试用例必须传 `gameMode:'team'`（模拟大厅选队锁定后的真实状态）——传 `null` 会被 tx 开头 normalize 清空 p.team（game.js 非 team 清 team 规则），合法用例永远红、非法用例永远绿（测到的是 normalize 不是 startGame 校验）；②brief 用例 2 数据 `{team:0},{team:0},{team:2}` 与其 teamSet 实现自相矛盾（不连续队伍号但队伍数=2 会通过校验），改为"全员同一队"（teamSet 仅 1 队）才真实触发拒绝。**回归**：run_identity_mode_test.js 35/35（identity 分支零变化）、run_ai_bus_core_test.js 10/10、run_ai_bus_l1_test.js 26/26。`?v=` 本次不 bump（实施计划 Task 10 统一收尾）。仅推 wenwen_dev（commit 9e4b30b）。
- **组队模式 9 人局布局修复：适配经典桌面牌桌布局（top6+left1+right1 + nth-child(6) 分档），修复第 7 名玩家重叠不可见**：Task 8 的 9 人局分配 `top5+left2+right1` 在 JS 系统（`assignSeatZones`+`updateDesktopSeatHeights`+data-zone-index 的 grid-row:2/3 槽位）下成立，但**桌面端（≥1024px）实际生效的是 index.html 1705 起"经典桌面牌桌布局"CSS 块**（2026-07-30 加入，用 `!important` 刻意覆盖 render.js 的 inline 尺寸）——经典块只认顶排 `#oppTopRow` 的 1~5 张分档 + 左右侧 `[data-zone="left"/"right"]` 各 1 槽（`grid-row:2!important`），**不识别 data-zone-index**。left 第二槽（data-zone-index=1）在经典块下被同样摆到 `grid-row:2` → 与 left 第一槽完全重叠，第 7 名玩家整个桌面端不可见、不可点击（task-8-report 补测报告已用真实 chromium 渲染确认，1280×720/1440×900/1920×1080 全档重叠）。**修复（沿用方向 2）**：①`render.js` `assignSeatZones` 的 `n>=8`（9 人局）分支从 `top5+left2+right1` 改为 **`top6+left1+right1`**——经典块天然支持的结构（top 行 flex 无上限、左右各 1 槽 grid-row:2）；②`computeOppZoneRowsUsed` 删除 `others>=8 返回 3`，9 人局回退为 2 行（和 6~8 人局同档）；③`index.html` 经典块 nth-child(4)/(5) 后补 **`nth-child(6)` 分档 `max-width:125px`**（6×125+5×8gap+padding≈790px < top 行可用宽 ~890px 起）。**8 人及以下各档零变化**：`n>=8` 分支独立未触碰既有分支，nth-child(6) 只在 top 6 张时匹配。**验证**：纯函数断言 32/32（2~9 人局×全 mySeat 分区数量/区内升序/rows 全符合基线、9 人局 left/right 各 ≤1、8 人及以下与改动前逐档逐 seat 一致）；chromium headless 真渲染**真实 index.html**（CDP 驱动、matchMedia 覆盖模拟桌面指针设备、颜良文丑 4 字名 + machao/daqiao 最亮 + simayi 最暗立绘混排、me 四槽装备全满 + 8 张手牌）四档 1280×720/1440×900/1920×1080/1200×675 全 PASS：9 个座位零重叠、零横向/纵向溢出、`scrollHeight==clientHeight`，第 7 名玩家（left 槽，座位7）rect 可见；1200×675 下限档 top 6 张 110×141.8px 无横向挤压（矮视口 max-height:760px 分档 nth-child(5)=110px 自动接管，6×110+40=700px < 可用宽，无需微调）；8 人局四档抽样真渲染全 PASS（top5+left1+right1 保持、零重叠）。**回归**：`node --check render.js` OK、run_team_mode_test.js 21/21、run_identity_mode_test.js 35/35、run_ai_bus_core_test.js 10/10、run_debug_log_test.js 10/10、run_aclass_fix_test.js 17/17；run_cixiong_test.js 17/20 的 3 项失败经 git stash 确认改动前后完全一致（pre-existing）。`?v=350` 未 bump（实施计划 Task 10 统一收尾）。commit 0ec6800，仅推 wenwen_dev。
- **组队模式完整改动记录（Task 1~10 实施计划汇总）**：本文第 16 条（Task 5 startGame）与第 17 条（9 人局经典块适配）之外的其余 Task 收尾记录，按实施计划 Task 10 要求补全为一条总览。
  - **需求**：新增「组队」对战模式（乱斗/身份局 之外第三模式）——玩家自由选队、队伍数≥2 且每队≥1、以"存活队伍数≤1"判胜负。SEATS 容量 8→9。面向朋友局自选队伍对抗，机器人（房主指定队伍）补齐人数。
  - **数据模型（Task 1, commit 16f5287）**：`data.js` `SEATS` 8→9、新增 `TEAM_COLORS` 9 色常量数组（队号 0~8 各一色）；`game.js` `normalize()` 三处——①`gameMode` 合法值扩为 `ffa/identity/team`；②`players.forEach` 块内新增 `p.team` 防御（非法/非整数→null，非 team 模式清空）；③players 之后推导 `g.teamCount`（team 模式 = 最大队伍号+1，非 team = 0）。`run_team_mode_test.js` 新建组队套件骨架+5 条 normalize/常量断言。
  - **模式按钮与开局入口（Task 2, commit d87663b/2e0c127）**：`render-controls.js`「主公局」按钮改「身份局」+新增「组队」按钮（alert/注释同步）；新增 team 分支「开始组队赛(三选一)」按钮直接 `startGame('pick','team')`；banner 文案 team 专用。2e0c127 追加 UI 层队伍校验（开始按钮在 Task 5 落地前先挡误开 ffa 局）+banner 标点全角化。
  - **大厅选队（Task 3, commit 9e082b1/cdf63b0）**：`room-lifecycle.js` 新增 `joinTeam(team)`/`createNewTeam()` 两个服务端函数（守卫 started/lobby/gameMode==='team'/队伍号 0~8/mySeat 存在，写 `p.team`，**写完再跑一次 normalize(g)**——tx 只在事务开始时 normalize，不重跑则同一次提交快照里 teamCount 停留旧值）；`render-controls.js` 选队面板（逐队色块+队伍N(M人)+加入按钮，队数=max(teamCount,2) 至少显 2 队，已加入变 disabled，"+新建队伍" teamCount>=SEATS 时 disabled）。cdf63b0 修复真实大厅选队不可用（守卫 gameMode 时序冲突——点加入时 gameMode 还是 null，改为选队即锁定 gameMode=team）。
  - **机器人进队（Task 4, commit efea134/6dd59bf）**：`addBot(team)` 加 team 参数（team 模式才写队伍号，非 team 无参调用 team 恒 null 零变化），push 后补 normalize(g)（同 joinTeam 先例）；选队面板房主（mySeat===0）每队行"+机器人"按钮。6dd59bf 收口游离机器人：选队即锁定 + team 房间拒绝无队机器人。
  - **startGame team 分支（Task 5, commit 9e4b30b，见第 16 条全文）**：队数≥2 且每队≥1 校验 + team 专用日志。`?v=` 全程未 bump（Task 10 统一）。
  - **checkWin team 分支（Task 6, commit 23a885c）**：`game.js` checkWin 函数体最前面加 team 分支——存活队伍数≤1 结束；=1 该队胜（`队伍N`，N=队伍号+1）、=0 无胜者（最后两队同时团灭）；置 `phase='over'`/`winSide=null`/`pending=null`/`aoe=null`+日志+返回 true。identity/ffa 分支一行未动。teamCount 由 normalize 推导（存活队伍数从 players 实时算）。
  - **座位卡队伍色块（Task 7, commit aa91b93/ec8186e）**：`render.js` `renderSeatCard` 加 `teamBlock` 角标（`g.gameMode==='team' && Number.isInteger(p.team)` 时输出 `.seat-team` div，色块 TEAM_COLORS[p.team]、内容队号+1），拼接在身份角标之后；`index.html` 加 `.seat-team` CSS（基础规则位、@media 前）。ec8186e 修复重叠：原 `right:6px;bottom:6px` 与右下角 `.seat-order-badge`（出牌顺序号）几乎完全重叠且 z-index 更高会盖住顺序号——移到右上角 `top:6px;right:6px`（team 模式身份块 role 已清不渲染、右上角空闲）。
  - **9 人布局（Task 8, commit 57e4995 + 第 17 条经典块适配 0ec6800）**：`render.js` oppRow 分配扩展 top5+left2+right1（JS 系统成立），随后经典桌面布局块不识别 data-zone-index 导致 left 第二槽重叠不可见——修复为 top6+left1+right1 + nth-child(6) 分档（详见第 17 条）。
  - **AI 提示词适配组队（Task 9, commit be0a198）**：`bot.js` `buildBotVisibleState` return 加 `myTeam`（本人队号，非整数→null）+ players map 项加 `team`（整数队号或 null）；`bot-ai-bus.js` `buildBotDefaultSystemPrompt` 决策参考段末尾追加团队指引（优先攻击/拆解敌方队伍、不浪费伤害/锦囊/拆牌在队友身上、队友濒死优先救援）。非 team 模式恒 null 零变化。
  - **主公局改名身份局**：Task 2 起所有用户可见文案「主公局」→「身份局」（按钮/alert/banner/注释），内部 mode 标识仍是 'identity' 不变。
  - **Task 10 收尾（本 commit 前）**：`game.js` normalize `p.team` 追加越界防御（`Number.isInteger(p.team) && (p.team<0||p.team>=SEATS) → null`）；`room-lifecycle.js` startGame team 分支追加全员 `p.team!=null` 校验（防无队玩家绕过）；`render-controls.js` 胜负文案 team 模式也用「胜方：」（与 identity 同列）。`index.html` `?v=350→351` ×15（Task 9 审查 W2 要求 bot 文件也覆盖，15 处全部脚本标签同步）。
  - **测试**：全量回归 13 套件（core 10/l1 26/l2 24/l3 223/info 16+1 既有失败/part2 11/c_window 34/lordskill 45/summary 15/timeout 20/button 23/identity 35/team 23）——仅 `run_ai_bus_info_test.js` 1 条既有失败（recentLog 长度 15 断言，task 9 降噪后 buildBotKeyEvents 只留 6 条，测试断言陈旧，经 git stash 前后对比确认与组队改动无关），其余全绿。commit 16f5287→be0a198 + 收尾 commit，仅推 wenwen_dev，main/chengcheng 未动。
- **组队模式最终审查修复（1 Important + 4 Minor, commit 见本次收尾）**：审查发现并修复 5 项。**I-1（Important）**：`room-lifecycle.js` `startGame` 校验通过后、`g.gameMode=gm` 之前加模式锁定守卫 `if(g.gameMode && g.gameMode!==gm) return g;`——防"选队已锁 team 后有人点乱斗/身份局开局把房间静默覆盖成 ffa"；旧路径零影响（大厅 gameMode 恒 null 短路不命中）。**M-1**：`render-controls.js` 身份局开始按钮文案 `SEATS` → `Math.min(SEATS,8)`（SEATS=9 后不再误导显示 "/9"，服务端上限本为 8）。**M-2**：`run_team_mode_test.js` 沙箱文件表末尾追加加载 `render.js`（顶层仅 document/window 监听注册与 onclick 绑定，现有 document stub 已支持、无 TDZ/BOT_DECISIONS 依赖），新增 `assignSeatZones` 2~9 人局×各 mySeat 全组合分区断言（数组长度=人数、自己=me、对手数=总-1、top/left/right 计数和=对手数、left/right 各≤1、9 人局 top6+left1+right1、无越界索引）。**M-3**：`addBot` 里从未使用的 `const isTeamRoom = g.gameMode==='team';` 删除。**M-4**：`run_ai_bus_info_test.js` recentLog 长度断言 15→6（task 9 降噪后 `buildBotKeyEvents` 只留最近 6 条关键事件；30 条 `日志1..30` 均不匹配例行过滤规则 → slice(-6) 后首项日志25/末项日志30）。**验证**：`node --check` 三文件语法 OK；`run_team_mode_test.js` 24/24、`run_ai_bus_info_test.js` 17/17、`run_identity_mode_test.js` 35/35 全绿（info 套件既有陈旧断言同步后全绿）。`index.html` `?v=351→352` ×15（改了 room-lifecycle/render-controls 两个部署文件，按惯例 ×15 一起 bump）。仅推 wenwen_dev，main/chengcheng 未动。
- **聊天语音播报 Task 1：语音核心（render-log.js + run_chat_tts_test.js）**：聊天语音播报实施计划的第一环——收到新聊天消息用浏览器 `speechSynthesis` 念出来，按发送者武将性别选声（男低音 pitch0.8/女高音 pitch1.2）。纯客户端本地行为，不写 Firebase。**改动（render-log.js 聊天函数区 `chatSenderLabel` 前新增 57 行）**：①`chatVoiceEnabled`（模块级 let，localStorage key `sgs_chat_voice`，'0' 关闭、默认开，try/catch 静默）；②`spokenChatIds`（Set，已念消息 id 去重，注释写明 enterGame 每次执行时重置、接入归后续 Task）；③`toggleChatVoice()`（切开关+localStorage+更新 `#chatVoiceBtn` 文案 🔊/🔇）；④`pickChatVoice(gender)`（按性别关键词 `female|女|huihui|xiaoxiao|xiaoyi` 或 `male|男|kangkang|yunxi` + `/zh/i` lang 过滤选 voice，无匹配返回 null）；⑤`speakChatMessage(text, gender)`（`SpeechSynthesisUtterance` + lang zh-CN + pitch + `cancel()` 防堆积 + try/catch 静默，复用 announceMyTurn/render.js 同款写法）；⑥`detectAndSpeakNewChat(messages)`（开关关/非数组直接 return；按 id 去重、emoji 跳过、空文本跳过；性别来源 `m.general`→`getGeneral`→`gender==='female'`）。**测试（新建 run_chat_tts_test.js，vm 加载 config/data/debug-log/room-lifecycle/game/weapons/skills/render-log 顺序）**：6 条断言（新消息触发念内容/同一 id 去重/emoji 跳过/开关关-开恢复/male↔female pitch 映射/女→Huihui 男→Kangkang 选声），6/6 全绿。**brief 测试的两处实证修正**（实现代码零偏离 brief）：①沙箱补 `SpeechSynthesisUtterance` 构造器 stub——brief context 给了 `window.speechSynthesis` stub 却漏了其构造器，`new SpeechSynthesisUtterance(...)` 在 vm 沙箱抛 ReferenceError 被实现里的 try/catch 静默吞掉 → speakCalls 恒空、4 用例全红；②test 2 去重用例 id `m1`→`m1a`/`m1b`——brief 原文 test 1 与 test 2 复用同一 id `m1`，而 `spokenChatIds` 是模块级 Set 跨用例保留，test 2 第一次调用 m1 已被 test 1 标记而跳过、计数错位（期望 2 实际 1）。**`?v=` 未 bump**（15 处 script 均 `?v=352`，render-log.js 在列；按组队模式实施计划先例各 Task 不单独 bump、收尾统一）。commit d5c29d0，main 分支。

- **分支纪律二次变更（2026-08-10，用户立下："反映到main，永远不要碰其他分支"）**：用户先于 2026-08-09 立下"我不说合并main绝对不可以合并，仅可以自由提交wenwen"（wenwen_dev 自由提交、main/chengcheng 受保护）；2026-08-10 组队模式开发完成后用户改口"反映到main，永远不要碰其他分支"——**从此所有开发/提交/推送一律在 `main` 上进行，永远不碰 wenwen_dev/chengcheng 等其他分支**。本次操作：把 wenwen_dev 的 32 个提交（d1d63b3..787e4c6，含组队模式全部 19 commits + 之前的 AI prompt 优化/日志降噪/意图链/token 裁剪等）fast-forward 合并进 main 并 push，CLAUDE.md 三、10 同步更新。**注意**：`chengcheng` 是 GitHub Pages 部署源（push main 不触发 Pages 构建、需同步 chengcheng 才能上线的历史机制，见 progress-log-8），既然永远不碰它，线上将停留在最后一次同步的版本（5f6392f，即"弃牌prompt修复+装备决策参考"那批）；组队模式及后续所有 main 上的改动均未上线，日后若需上线由用户另行指示（改 Pages source 配置为 main，或明确说同步 chengcheng）。
- **聊天语音播报 Task 2：接入聊天同步 + 开关按钮 + 收尾修复（M-1/M-2/M-3/M-4/M-6）**：聊天语音播报实施计划的第二环——Task 1 的语音核心（render-log.js）接入真实聊天数据流，并在聊天面板加 🔊/🔇 开关按钮。**改动（room-lifecycle.js / render-log.js / index.html / run_chat_tts_test.js）**：
  - **room-lifecycle.js 接入**：①`enterGame()` 开头加 `if(typeof spokenChatIds!=='undefined' && spokenChatIds.clear) spokenChatIds.clear();`——重进房间重置已念集合（`spokenChatIds` 是 render-log.js 的模块级 Set，加载晚于 room-lifecycle.js，运行时已存在，仍按 CLAUDE.md 跨文件惯例用 typeof 防御）；②`chatQuery.on('value')` 回调内、`renderLogPanel` 调用前加 `if(currentG) detectAndSpeakNewChat(chatMessages);`——聊天同步即语音播报入口，自己发的消息也念（统一走同步回调，不排除自己）。**注意去重口径修正**（计划 Step 1 原文曾写"每次 value 回调开始时 clear spokenChatIds"——那是错的：value 回调每次都是 `limitToLast(80)` 全量重读，若每次 clear 再遍历全部，每条消息每次同步都算"新"的会重复念；正确口径是**已念集合跨同步累积（不清空）**，只念"id 不在集合"的，仅在 enterGame 重进房间时 clear）。
  - **render-log.js 开关按钮**：`renderLogPanel` 的 chat-compose 里、`emojiPicker` 之前插入 `voiceBtn`（`<button id="chatVoiceBtn" class="emoji-toggle" onclick="toggleChatVoice()" title="聊天语音开关">`，文案随 `chatVoiceEnabled` 显 🔊/🔇）——`voiceBtn` 定义在 `el.innerHTML` 拼接之前（`quick`/`emojiPicker` 之后）。
  - **M-6 修复（开关关闭期间消息也应标记已念）**：`detectAndSpeakNewChat` 里把"id 加入 `spokenChatIds`"提前到 `chatVoiceEnabled` 检查之前——原实现 `if(!chatVoiceEnabled || ...) return;` 在关闭语音时直接整体返回，关闭期间到达的消息 id 不进集合，重开语音后全量同步会把它们当"新消息"重念一遍（复读）。改后逻辑：`shouldSpeak=chatVoiceEnabled` 提前取出，遍历时先标记 id（去重）、再按 `shouldSpeak` 决定念不念，关闭时只标记不念；emoji 跳过/空文本跳过行为保持原样。
  - **M-2 注释修正**：`spokenChatIds` 声明处注释更新为"已念消息 id 去重(跨同步累积,不全量clear);enterGame 重进房间时重置(room-lifecycle.js 接入点)"——原注释"enterGame 每次执行时重置"与实现口径不符。
  - **index.html**：`?v=352→353` ×15（改了 room-lifecycle/render-log 两个部署文件）。
  - **run_chat_tts_test.js（M-1/M-3/M-4 + M-6 断言）**：①沙箱 context **顶层**加 `localStorage` stub（与 `window.localStorage` 共享同一份 `context.__ls` 存储）——render-log.js 模块级 IIFE 用裸 `typeof localStorage!=='undefined'` 读开关持久化，之前沙箱只有 `window.localStorage`，IIFE 读取路径从未被激活；②test 2 错误消息文案 `m2/m1`→`m1b/m1a`（去重用例实际用的 id 是 m1a/m1b，文案与代码一致）；③test 5 名称改"male→pitch 0.8 / female→pitch 1.2（voice 匹配时）"并补一条"getVoices 返回空→voice null 但 pitch 仍生效"断言（临时替换 `sandbox.window.speechSynthesis.getVoices` 返回 `[]`，验证 `pickChatVoice` 返回 null 时 `u.pitch` 照常设置）；④追加 M-1 真实持久化断言：`toggleChatVoice()` 关闭后 `localStorage.getItem('sgs_chat_voice')==='0'`、重开后 `'1'`；以及 IIFE 读取路径断言（set '0' 后重新求值 IIFE 返回 false=默认关闭、set '1' 恢复 true）。**测试**：`run_chat_tts_test.js` 10/10 全绿（旧 6 + 新增 4：M-6 重开不重放 / getVoices 空 fallback pitch / localStorage 持久化 / IIFE 重载默认关闭）；`run_ai_bus_info_test.js` 17/17 回归通过。**注意**：`run_ai_bus_info_test.js` 沙箱未加载 render-log.js 或沙箱无顶层 localStorage 时 IIFE 走 `typeof` 防御回退默认开、行为零变化。commit 见本次收尾，仅推 main（永远不碰其他分支纪律）；线上未部署——Pages 源仍是 chengcheng，main 上改动停留在最后一次同步版本。
- **修复组队模式机器人打队友（防误伤，commit 42152b7）+ 主动助攻现状盘点**：用户反馈"不同队之间互相战斗、队内互帮互助"的组队系统里，机器人经常打队友。**根因**：`botTargetScore`（bot.js:525，几乎所有目标选择的统一打分入口，21处调用点）只按身份局的 `role` 分支（zhong/fan/zhu/nei）dispatch，完全没有读组队模式的 `team` 字段——组队模式下 `p.role` 恒为 `null`（normalize() 保证非 identity 模式清空 role），于是每次都落进最后的兜底 else 分支，队友和敌人在打分上完全等价（只加个随机数）。`buildBotVisibleState` 里已有 `myTeam`/`players.team` 供 AI 提示词用，但本地兜底打分逻辑此前完全没接上这份信息。**修复**：①`data.js` 新增 `sameTeam(g,seatA,seatB)`——唯一的判队友入口（和 `canSeeRole` 同一位置/同一写法风格），仅 `g.gameMode==='team'` 时才可能为 true。②`botTargetScore` 最前面加 `if(g.gameMode==='team' && sameTeam(g,seat,targetSeat)) return -Infinity;`，逐一确认过所有21处调用点实际传的 kind（'damage'/'steal'/或杀/决斗/火攻/铁索连环/顺手牵羊/过河拆桥这类action名）全部是有害操作，没有一处是"帮"目标，team模式下"同队一律不可选"可以无差别套用在所有 kind 上。身份局(identity)/乱斗(ffa)既有分支不变。③`jiedaoTwoStep`(借刀杀人)/`lijianTwoStep`(离间)两个两步技能的本地兜底候选枚举：阶段A的 `hasSomeB` 额外要求"B和A不同队"（保证选中的A一定能在阶段B找到非同队B）；阶段B额外过滤"和A同队"的B。**踩坑**：一开始给这两个决策加了 `onEmpty:function(){resetBotTwoStep();}` 想防止"阶段B候选为空时卡住"，结果改变了"候选为空→botDecide返回false→runBotDecision继续走runBotActionWindow(不卡死，继续打别的牌)"这个既有约定（`run_ai_bus_l3_test.js`两条既有测试锁定的正是这个契约），造成了一次真实回归（2条测试从PASS变FAIL）——移除onEmpty后确认阶段A的hasSomeB已经足够保证阶段B正常不会遇到空候选，不需要额外处理。**顺带主动助攻第一项**：`botCanSave`（唯一决定"是否用桃救这个濒死的人"的判断函数，供`dyingLocalFallback`用）同样只有身份局role分支、team模式恒`return false`（机器人永远不会用桃救队友，只能自救）——加一条 `if(g.gameMode==='team') return sameTeam(g,seat,dyingSeat);`。**测试**：`run_team_mode_test.js`新增11个场景（botTargetScore三种模式各一条、jiedao/lijian各2条、sameTeam边界4条、botCanSave 2条），git stash差分验证6条在还原前失败（同队目标打分45.2而非-Infinity/候选含队友组合/sameTeam未定义/救队友返回false），修复后33/33全过；`run_ai_bus_l3_test.js`223/223（含修正onEmpty回归后的两条既有测试）；`run_ai_lordskill_test.js`45/45、`run_identity_mode_test.js`35/35（identity模式回归）；全量测试套件28/29文件全过（仅`run_cixiong_test.js`既有历史失败，与本次无关）。`index.html` `?v=354→355`。仅推 `wenwen_dev`，未碰main。
  - **主动助攻现状盘点（第二部分，未实现，列清单交用户决定）**：梳理了项目里"对目标有益"的操作现状——①**桃救人**（botCanSave）：本次已修复（见上）。②**仁德renDe**（刘备"出牌阶段可以把手牌交给其他角色"）：`rendeTwoStep`阶段A的目标选择完全靠`localFallback`取`candidates[0]`（座位枚举顺序里第一个非自己的存活角色），从未走`botTargetScore`，没有任何team感知——组队模式下有可能把仁德的牌白送给敌方（这甚至比"不助攻队友"更糟，是在资敌），值得作为后续任务优先修，但需要先确认"给队友"应该怎么排序（体力最低的队友优先？还是手牌最少的？），不是简单加个`sameTeam`过滤就完事，需要额外设计一版打分。③**郭嘉遗计yijiAssign**（获得判定牌后可分配给任意角色包括自己）：本地兜底固定给自己（改动前行为），团队模式下没有"分给队友"的逻辑，但也没有"分给敌方"的风险（默认自己，无害但也没有助攻价值）。④**桃园结义**：全场无差别回血，不涉及目标选择，不适用这类改动。⑤本次盘点不是穷举——项目里可能还有其它"给指定角色益处"的技能未逐条排查（比如是否还有类似仁德的赠牌类技能），如果后续要做，建议先用类似之前A/B/C类审计的方法系统扫一遍所有"能让机器人主动选择另一个角色作为受益对象"的决策点，再分批处理，不要假设这次盘点已经覆盖全部。
- **修复仁德renDe组队模式资敌问题**：上次盘点（见上一条"主动助攻现状盘点"②）指出仁德阶段A的目标枚举完全没有team感知，`localFallback` 固定取`candidates[0]`（座位顺序第一个非自己存活角色），组队模式下可能把牌白送给敌方——比"没帮上队友"更糟，是主动资敌。**先确认仁德给目标的效果性质**：`skills.js renDe(cardIdx,targetSeat)` 是纯单向转移——`me.hand.splice`+`target.hand.push`，目标没有任何反向代价/回报，累计给出2张后的回体力也是刘备自己触发、和收牌人是谁无关。结论：这是纯粹的增益赠予，不存在"给敌方也有条件合理"的场景，**采用一刀切排除**，不做"降权仍可选"的暧昧处理，和伤害类操作的`-Infinity`同一处理力度。**实现**：`bot.js` `BOT_DECISIONS.rendeTwoStep.buildCandidates` 阶段A枚举里加 `if(g.gameMode==='team' && !sameTeam(g,seat,i)) return;`（复用`botTargetScore`修复引入的唯一判队友入口`sameTeam`），未改`match`——候选为空时`match`仍返回true，交给`botDecide`统一处理空候选（返回false，`runBotDecision`继续走其它决策，"这一轮不发动仁德"优于"发动仁德资敌"），不需要额外的`onEmpty`兜底（复用上次`jiedaoTwoStep`/`lijianTwoStep`踩过的坑：加`onEmpty`会破坏"候选为空→不卡死→继续走其它决策"这个既有契约）。身份局/乱斗模式的分支未动（`gameMode!=='team'`时候选逻辑完全不变）。**测试**：`run_ai_bus_l3_test.js`新增3条（team模式候选只剩同队目标、无队友时候选为空且match仍为true、非team模式候选不受影响回归），226/226全过；`run_team_mode_test.js`33/33回归通过。**盘点郭嘉遗计等其它未实现项是否有同类资敌风险**：`yijiAssign`本地兜底固定分给自己（改动前行为），不存在"分给敌方"的路径，属于"无助攻价值但无害"，和仁德性质不同，维持原状即可，不需要同批处理。经过这次核对，确认上次盘点里**只有仁德这一项属于"主动资敌"性质**，其余（遗计给自己、桃园结义无目标）均无此风险。仅推 `wenwen_dev` 后同步进 `main`。
- **修复聊天语音朗读英文/韩文消息不发声**：用户反馈聊天框朗读功能只有中文能正常发声，英文/韩文消息静默不出声（不报错）。**根因**：`render-log.js` `speakChatMessage(text,gender)` 里 `u.lang` 写死 `'zh-CN'`，不管消息文本实际是什么语言——很多浏览器的语音引擎在 `SpeechSynthesisUtterance.lang` 与文本实际语言不匹配时会静默跳过整句、不报错也不出声，中文消息因为声明语言和内容一致所以能正常念，英文/韩文消息因为"内容是英文/韩文但声明是中文"而被跳过。和项目里已有的 `announceMyTurn`（render.js:143-147，"轮到你了"回合提示音）同一类写死 `lang` 的写法，但那处文本恒为固定中文字符串，不存在"内容语言和声明不匹配"的问题，不需要一并改。**修复**：新增 `detectChatLang(text)`——按字符集区间判断：先查韩文（谚文音节 U+AC00-D7A3、谚文字母 U+1100-11FF、兼容字母 U+3130-318F，需先于中文判断，避免被中文规则截胡）、再查中文（CJK统一表意文字 U+4E00-9FFF），都不含则默认英文 `en-US`——简单字符区间判断，不引入语言检测库。`speakChatMessage` 改用 `u.lang = detectChatLang(text)` 而非写死。`pickChatVoice(gender,lang)` 同步改造：原来的中文性别关键词（huihui/xiaoxiao/kangkang/yunxi）只适用于中文声库命名习惯，英文/韩文声库无统一命名规则，改为按 `lang` 前缀匹配 voice（`langRe.test(v.lang)`），性别关键词仍优先尝试匹配、匹配不到再退化为"只匹配语言前缀"，找不到返回 null（调用方已有 pitch fallback，找不到 voice 不影响是否发声——发声与否由 `u.lang` 是否匹配文本内容决定，不是由是否选中了具体 voice 决定）。**混合语言消息**（一句话中英夹杂）本次按"整句判一次主要语言"处理，不做分段拆分朗读——分段需要按语言切分文本+逐段调用speak，复杂度明显超出这次修复范围，列为后续优化项，不强求这次做完。**环境限制说明**：代码层面只负责把正确的 `lang` 传给浏览器，特定浏览器/操作系统是否装有对应语言的语音引擎是用户设备资源，不是代码能控制的——如果 `u.lang` 设对了但用户设备没装韩文语音包，浏览器可能仍然不出声或退化成默认语音，这属于环境限制，不在这次修复范围内。**测试**：`run_chat_tts_test.js` 新增6条（voicesMock 补一条韩文 voice；`pickChatVoice` lang=en-US/ko-KR 两条；`detectChatLang` 中/英/韩三种判断一条；`speakChatMessage` 端到端验证中/英/韩三种消息 `u.lang` 分别为 zh-CN/en-US/ko-KR 一条），14/14 全过（含既有8条回归）；`run_ai_bus_info_test.js`（render-log.js 相关既有回归）17/17 通过。`index.html` `?v=356→357`。仅推 `wenwen_dev` 后同步进 `main`。
- **聊天语音朗读扩展日语支持**：参照上次加韩文的实现方式，`detectChatLang(text)` 新增日语判断。**规则**：先查韩文（谚文特征区间，逻辑不变）→ 再查日语（平假名 U+3040-309F、片假名 U+30A0-30FF，日语的强特征字符，**必须放在中文 CJK 判断之前**——日语句子常混杂汉字，和中文 CJK统一表意文字区间 U+4E00-9FFF 重叠，若先判中文会把带假名的日语句子误判成中文）→ 再查中文（逻辑不变）→ 默认英文。韩文和日语的字符区间互不重叠，两者相对顺序不影响准确性，本次把日语判断放在韩文之后、中文之前。命中日语返回 `'ja-JP'`。**已知局限（明确不解决）**：纯汉字、不含任何假名字符的日语句子（如「今日天気」），字符区间层面和中文完全没有区别，无法可靠区分，会按现有优先级规则回退判为 `zh-CN`——这和韩文/中文之间不存在此类歧义（谚文区间和CJK区间互斥）是不同性质的局限，是"用字符区间判断语言"这个方法本身的固有边界，不引入分词/语言检测库解决，用测试断言明确记录这条预期行为（而非当作待修的 bug）。`pickChatVoice(gender,lang)` 未改代码——原本就是通用的"按 `lang` 前缀正则匹配 voice、性别关键词优先尝试、匹配不到再退化"逻辑，`lang='ja-JP'` 时 `prefix='ja'` 自动生效，无需专门适配。**测试**：`run_chat_tts_test.js` 新增4条——voicesMock 补一条日文 voice；`pickChatVoice` lang=ja-JP 选中日文voice一条；`detectChatLang` 纯假名（平假名+片假名各一句）/假名+汉字混合三种均判 `ja-JP`（验证优先于中文）一条；纯汉字日语句子回退 `zh-CN` 的局限性场景一条（断言即预期，非失败用例）；`speakChatMessage` 端到端验证中/英/韩/日四种消息 `u.lang` 分别对应四种语言一条。17/17 全过（含既有13条回归，中文/英文/韩文检测场景未受影响）。`index.html` `?v=357→358`。已合并进 `main`（commit 81c7b76，用户确认后合并）。
- **修复贾诩【完杀】濒死轮询未跳过限制外座位**：用户反馈贾诩回合内用普通杀让人濒死，游戏依次询问了所有人是否用桃，而完杀理论上应该只问贾诩自己和濒死者两人。**根因**：`game.js` 的 `respondDying`（约4122行）里已有完杀拦截（约4130行），但只在"某人真的选择用桃"时才生效并静默拒绝；濒死流程"按座位顺序依次问是否救援"的推进逻辑（`nextAskee`，"不救"分支里 `nextAskee(g, g.pending.seat, mySeat)` 算下一个该问的座位）完全没有感知完杀限制，导致限制范围外的角色依然被正常问一轮、问了也白问（respondDying 会拒绝他们的桃）。**修复**：新增 `nextDyingAskee(g, dyingSeat, current)`（紧邻 `nextAskee` 之后，约4903行）——内部先调用既有 `nextAskee` 算出下一个候选座位，若完杀生效中（`g.wanshaActive && g.wanshaDyingSeat===dyingSeat`）且回合仍是贾诩本人（`jiaxuSeat===g.turn`，与 `startDying`/`respondDying` 里判定完杀是否生效的条件完全一致），则用 `while` 循环持续调用 `nextAskee` 跳过既不是贾诩本人、也不是濒死者本人的座位，直到落在贾诩/濒死者/或绕回濒死者本人返回 `null`（问完一圈）为止；非完杀场景（或完杀未命中当前濒死者）行为与 `nextAskee` 完全一致，零变化。`respondDying` 的"不救"分支里 `nextAskee(...)` 改调 `nextDyingAskee(...)`（仅这一处调用点改动，其它场景如借刀杀人二问、济危四问等使用 `nextAskee` 的调用点均未动）。**未改动 `startDying` 的起点逻辑**——濒死流程本来就是从濒死者本人开始问（`asking:seat`），完杀允许濒死者自救，这条逻辑无需调整，只调整"濒死者不救之后往下一个座位推进"这一步。**边界场景（贾诩本人恰好是濒死者）**：此时 `jiaxuSeat===dyingSeat`，`nextDyingAskee` 的 while 循环条件 `nxt!==jiaxuSeat && nxt!==dyingSeat` 恒为真（因为 `nextAskee` 除非绕回 `dyingSeat` 变成 `null` 否则永远不会返回 `dyingSeat` 自身），于是会持续跳过所有其他座位直至耗尽绕回、返回 `null`——等价于"只问了贾诩自己这一个人，其余全部自动跳过，问完直接死亡结算"，符合预期（完杀允许濒死者自救，贾诩自己不救就没有其他任何人能救）。**未改动 bot.js**：`botCanSave`（约3671行）本身已正确感知完杀限制（只有贾诩/濒死者本人才判定为可救），本次轮询顺序调整只是让机器人根本不会被问到限制外的座位，`botCanSave` 无需改动、也不受影响。**测试**（新建 `run_wansha_dying_poll_test.js`，走 `tx`/`respondDying`/`startDying` 真实函数而非 stub）：4个场景——①贾诩(座位0)回合内座位2濒死、场上还有座位1/3/4，验证轮询只问了座位2→座位0两人（中间三个座位全程未被 `asking` 命中），两人都不救后濒死者正确阵亡；②同场景逐一断言 asked 序列不含1/3/4（排除巧合）；③边界场景贾诩本人(座位0)即濒死者，验证只问了这一人、不救后贾诩本人正确阵亡；④对照场景 `turn:3`（非贾诩回合），验证完杀不生效、`wanshaActive` 为 false、依次问了座位2→3→0→1全部存活玩家（原有逻辑不受影响）。4/4 全过。**回归**：`run_niepan_dying_bot_test.js`（庞统涅槃濒死自救）10/10、`run_ai_bus_l3_test.js`（含郭嘉遗计等 L3 决策）226/226、`run_yijiask_bot_test.js`（遗计/慷慨等 ask 类）10/10 全过；`run_cixiong_test.js` 17 passed/3 failed——用 `git stash` 验证这3条失败（`EQUIP_SLOT_LABEL is not defined`、丁奉短兵取消断言）在改动前就已存在，与本次改动无关。已直接推送到远程 `main`（`git push origin wenwen_dev:main`，用户要求这次不经本地合并、直接推远程分支），未走本地 `git merge`。
- **修复周泰【不屈】触发次数：按"体力降到0或以下"的扣血点数逐次询问，不再不管扣几点血都只问一次**：用户反馈周泰体力已经是0时被酒杀（2点伤害）命中，游戏只问了一次是否发动不屈，正确规则应该连续问两次。**根因**：`dealDamage`（约3927行）判断 `p.hp<=0` 时无条件只挂起一次 `buquAsk` 询问，完全没有考虑这一下伤害本身有几点是"发生在体力≤0"的区间内。**触发次数公式**（写在 `dealDamage` 判断分支的注释里，含完整推导）：把 `amount` 点伤害看成 `amount` 次连续的 -1，第 k 次（k=1..amount）扣完后的体力是 `hpBeforeThisDamage-k`；结果仍 >0（不落在"降到0或以下"区间）的次数 = `Math.max(hpBeforeThisDamage-1, 0)`，其余次数（`amount` 减去这部分）才是真正需要逐次询问的次数。最终公式：`overkillPoints = Math.max(0, Math.min(amount, amount - Math.max(hpBeforeThisDamage-1, 0)))`（`hpBeforeThisDamage` 是扣血前的体力，扣血这行原有代码 `p.hp = p.hp - amount` 之前新增一行 `const hpBeforeThisDamage = p.hp;` 捕获）。**"体力1血挨2点伤害打到-1"这个场景**：`overkillPoints = 2 - max(1-1,0) = 2 - 0 = 2`，触发2次——因为把2点伤害拆成两次连续的-1来看，第1次扣完后体力是0（已经落在"降到0或以下"区间），第2次扣完后体力是-1（同样落在区间内），两次结果都≤0，所以两点都算，不是只算"跨越0那一下"的1点；这样处理和"体力已经是0再挨2点全部算2次"是同一套一致的规则，不需要为"从正数一次性打穿到负数"单独开例外。**单点伤害（amount=1）验证零回归**：任意 `hpBeforeThisDamage` 值代入公式，只要命中 `p.hp<=0` 分支就必然算出 `overkillPoints=1`，和改动前"只问一次"的行为完全一致。**多次询问流程设计**：不新增队列字段/新阶段，直接在现有 `g.pending`（`type:'buquAsk'`）上加一个 `remaining` 计数器（复用 CLAUDE.md「改变响应数量要求」这条既有 seam，和吕布无双的 `shanCount`/`shaCount` 同一套写法，不新增架构）。`respondBuqu` 每次响应（不管选择放置还是不放置）只消耗一次机会：`remainingAfter = remaining - 1`；若 `remainingAfter>0` 且牌堆还有牌，重新设置 `g.pending={type:'buquAsk',...,remaining:remainingAfter}` 继续问下一次；否则才真正调用 `startDying` 进入濒死流程。`normalize` 里 `buquAsk` 校验分支追加 `remaining` 防御（`!Number.isInteger(d.remaining)||d.remaining<1` 时回退为1，等价于旧行为，不影响单点伤害/旧存档）。**边界情况处理**：①**中途凑够点数唯一、提前回体力**——`checkBuquUnique` 的判定和回体力逻辑（原有 `p.hp=0` + `resumeAfterInterrupt`）完全不变、且检查发生在**每次放置后**（不是等 `remaining` 问完再统一检查一次），一旦成功就 `return`，**不再管 `remaining` 是否还有剩余**——因为体力已经回正，没有继续问"是否放置不屈牌"的意义，这个提前退出路径本身也保证了"回复到体力>0之后不会继续问剩余次数"。②**牌堆中途耗尽**——`remainingAfter>0` 的继续条件里同时要求 `(g.deck||[]).length>0`，牌堆耗尽即使还有 `remaining` 剩余也不再继续问，直接进 `startDying`（复用原有"牌不够就走濒死"分支逻辑，只是判断点从"进入询问前一次性检查"变成"每次继续询问前都检查一次")。**测试**（新建 `run_buqu_multitrigger_test.js`，走 `tx`/`dealDamage`/`respondBuqu` 真实函数而非 stub）：8个场景全过——体力0血受2点伤害连续问2次；体力1血受2点伤害打到-1同样算出2次（并验证 `zt.hp===-1`）；对照场景体力3血受2点伤害打到1（不落在≤0区间）不屈完全不触发；中途放置1张牌就凑够"点数唯一"提前回体力、不再继续问剩余次数；首次放置即和已有不屈牌点数重复（永久无法再凑齐唯一）、验证问完 `remaining` 次后老实进入濒死而不是错误判定成功；牌堆恰好在两次询问之间耗尽、提前进入濒死不卡住；牌堆一开始就是空的、直接濒死（原有防御分支）；单点伤害（amount=1）仍只问1次的零回归验证。**回归**：`run_ai_bus_l3_test.js`（含既有"不屈固定发动"用例，该用例整体 mock 掉了 `respondBuqu`，不受内部实现变化影响）226/226、`run_niepan_dying_bot_test.js` 10/10、`run_wansha_dying_poll_test.js`（上一条完杀濒死轮询修复的回归）4/4 全过；`run_cixiong_test.js` 17 passed/3 failed（与上一条记录的3条既有失败相同，已用 `git stash` 复核过和本次改动无关）。`index.html` `?v=359→360`。**已直接 fast-forward 合并进 `main` 并推送**（用户明确指示从这次起测试通过后不再等待确认，直接合并）。
