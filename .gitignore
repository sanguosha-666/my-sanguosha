# Windows
Thumbs.db
Desktop.ini
*:Zone.Identifier

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Logs
*.log
.claude/

# SDD 内部跟踪文件(进度/审查/任务简报/报告)——不入库
.superpowers/

# 会话进度文件(不入库)
TASKS.md

# ---- 验证/排查过程的临时工件 ----
# 【为什么要有这一段】本项目没有 package.json、没有构建流程,长期也就没有真正的
# .gitignore 兜底;而改动收尾惯用 `git add -A`,两者相加的结果是**任何落在项目根的
# 临时工件迟早会被误提交**——已经发生过一次(指向 scratchpad 的 node_modules 软链被
# 提交进 main)。所以这里不是"堵掉那一个洞",而是把这几轮实际产生过的几类工件一次列全:
# 依赖软链、截图/视频输出、Playwright 自己的产物、一次性验证脚本的输出目录。

# 跑 Playwright/sharp 时会在项目根建一条指向 scratchpad 的 node_modules 软链,
# 那些包只装在 scratchpad、不是本项目的依赖。
node_modules

# 验证脚本的截图/录像输出(tools/verify_*.js、布局对比、对比度取样等)
#
# 【为什么是 /*.png 而不是 *.png + !assets/ 例外】
# `*.png` 是**递归**忽略(仓库任何深度的 png 都中招),再用 `!assets/**/*.png` 开一个洞
# 补回来 —— 那等于把"图片默认不入库"设成全局默认,只对当前已知的一个目录网开一面。
# 将来一旦新增 assets/ 之外的素材目录(比如 img/、public/、docs/ 里的示意图),它会被
# **静默忽略**:`git add` 不报错、`git status` 不显示,只是文件永远进不了库,而且这种
# 失败没有任何提示,比误提交更难发现。
# 开头的 `/` 把匹配锚定在仓库根目录、不递归,正好覆盖"临时截图散落在项目根"这个真实
# 场景(验证脚本都是在根目录下跑的),同时任何子目录里的素材都不受影响,不需要例外规则。
/*.png
/*.jpeg
/*.webp
/*.gif
# 明确的输出目录(这些目录本身就是给工具写产物用的,整个目录递归忽略)
screenshots/
shots/
__screenshots__/

# Playwright 自己的产物
test-results/
playwright-report/
blob-report/
.playwright/
playwright/.cache/

# 一次性验证/排查脚本及其输出(scratchpad 里的东西不该出现在项目根;
# 真正要长期保留的验证脚本请放进 tools/ 并起正式名字)
scratchpad/
tmp/
.tmp/
*.tmp
diag.js
edge.js
*.out.txt
*.dump.json
