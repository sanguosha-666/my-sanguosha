# Windows
Thumbs.db
Desktop.ini
*:Zone.Identifier

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Logs
*.log
.claude/

# SDD 内部跟踪文件(进度/审查/任务简报/报告)——不入库
.superpowers/

# 会话进度文件(不入库)
TASKS.md

# 跑 Playwright/sharp 时会在项目根建一条指向 scratchpad 的 node_modules 软链,
# 那些包只装在 scratchpad、不是本项目的依赖(本项目无构建流程、无 package.json)。
node_modules
